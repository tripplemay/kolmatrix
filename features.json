{
  "sprint": "BL-026-asset-ux-redesign",
  "features": [
    {
      "id": "F001",
      "title": "ADR-012 + BL-026 spec 起草（Planning artifact）",
      "priority": "high",
      "executor": "generator",
      "status": "completed",
      "acceptance": "docs/adr/ADR-012-assets-ux-redesign-outreach-first.md 已写入并入 git，Status=Accepted；docs/adr/README.md 加 ADR-012 + ADR-011 索引；docs/specs/BL-026-asset-ux-redesign-spec.md 完整 §S1 4 段（按 v0.9.6 [#5] 自审 checklist）+ §F001-F006 + §S2-7 全段。0 LoC。本 feature 在批次启动时已由 Planner 落地完成。详见 ADR-012 + spec §F001。"
    },
    {
      "id": "F002",
      "title": "/assets 三栏 → 双栏 + Detail right drawer + Filter top dropdown",
      "priority": "high",
      "executor": "generator",
      "status": "completed",
      "acceptance": "删除 AssetsFilterSidebar (lines 416-556 of AssetsClient.tsx)；ActionBar 加 Filter ▾ GhostButton + Dialog popover 浮层（含 search Input debounce 300ms / Product Combobox / Type ChipButton 多选 / Status radio Archived 默认折叠 / Source ChipButton 多选 / Clear all + Done 按钮）；Detail panel 改 right slide-over drawer 520px + 半透明 backdrop 点击关闭 + Esc 关闭；mobile <768px drawer 全屏；grid 列数改 1/2/3/4 (xl:4)；e2e tests/e2e/assets-page.spec.ts 既有 8 case 重新跑通 + 新增 filter dropdown / drawer / mobile 全屏行为 case；visual baseline 4 个新（en-assets / en-assets-drawer-open / en-assets-filter-dropdown / en-assets-empty-system-seed 后者 F004 提供数据）；估 1585 → ~1100 LoC AssetsClient.tsx；coverage ≥80% lib/assets 不退化。详见 spec §F002 + ADR-012。"
    },
    {
      "id": "F003",
      "title": "variant tree 折到 Preview tab 顶部下拉 + 4 → 3 tabs",
      "priority": "high",
      "executor": "generator",
      "status": "completed",
      "acceptance": "删除文件 src/app/[locale]/(app)/assets/_panel/VersionsTab.tsx；TAB_CONFIG 从 4 项改 3 项 (preview/edit/used_in)；AssetTabId type 移除 versions；DetailPreview 顶部加 VariantSwitcher 子组件（仅 totalVariants > 1 时显示，base-ui Menu primitive 列出 v1/v2/...，每项含 v 编号 + name + source TagChip + status StatusDot + Restore button）；EditTab 'Save as new version' 改 GhostButton size=sm（视觉降级）；删除 'Compare with current — coming with F005 polish' 假按钮（VersionsTab 删除时自然消失）；修 BL-025 saveAssetAsVariantAction Restore content 空对象 bug：Generator 选 Option A — server action SaveAsVariantInputSchema content 改 optional，content !== undefined 时用 caller 传值，否则 fallback parent.content（VariantSwitcher Restore 不传 content 自然走 fallback；EditTab 真实编辑路径仍传值）；新单元 test AssetsClient.test.tsx 验证 tabs 仅 3 个 + variant switcher 渲染条件；e2e drawer 内 tabs 切换 + variant switcher 行为。详见 spec §F003。"
    },
    {
      "id": "F004",
      "title": "Empty state 改 system_seed 模板展示（welcome mode）",
      "priority": "high",
      "executor": "generator",
      "status": "pending",
      "acceptance": "AssetsEmptyState 删除 'Create blank' 假按钮（onCreate prop 一并删）；page.tsx Server Component 加 mode='normal'|'welcome' 字段（empty tenant fallback 到 system_seed 展示）；welcome mode 顶部 banner 'Welcome — these 5 templates ship with KOLMatrix...' + Generate from product CTA 浮在右；Grid 显示 5 套 system_seed 模板（emerald TagChip + System label）；system_seed asset hover quick actions 仅显示 Duplicate（AssetCard 加 readOnly?: boolean prop）；drawer 内 system_seed asset 'Send to outreach' 按钮改 'Save to my library'（Duplicate 到本 tenant）；守门 test tests/integration/assets-empty-welcome.test.ts 验证 fallback 逻辑；e2e empty tenant 进 /assets 看到 5 套模板 + welcome banner。详见 spec §F004。"
    },
    {
      "id": "F005",
      "title": "/outreach composer 加 search + product filter（轻量增强）",
      "priority": "high",
      "executor": "generator",
      "status": "pending",
      "acceptance": "OutreachComposer template dropdown 升级为 'search + filter row'：顶部 product filter Combobox（All products 默认）+ search Input（debounce 300ms 按 name/subject 模糊匹配）+ list of templates max 20 visible，每项 name / source TagChip / product label / 1-line preview snippet；视觉用 GlassPanel + 现有控件，不新增公共组件；loadAssetsForComposer(tenantId, type, locale?, search?, productId?) 加 2 可选参数，WHERE 加 ILIKE 模糊匹配 + productId 过滤；AssetsClient DetailPanelInner footer 'Send to Outreach' 按钮 GradientButton → GhostButton（视觉降级，反向流弱化）；新增 tests/e2e/outreach-composer-template-select.spec.ts；扩展 src/lib/assets/__tests__/queries.test.ts + tests/integration/composer-load-templates.test.ts 加 search/productId case。详见 spec §F005 + ADR-012 心智重排。"
    },
    {
      "id": "F006",
      "title": "UX 细节修（AssetCard 信息层次 / Search 实时 / Sort used_most / UsedIn 名字 / Wizard 成本+失败路径）",
      "priority": "medium",
      "executor": "generator",
      "status": "pending",
      "acceptance": "AssetCard 标题截 ' v\\d+$' 后缀防 product 名重复 + 副标题去 product 名（仅 relativeTime） + footer 加 product link cyan 风格 + status 加 StatusBadge 文字 + AI badge 改 purple TagChip 替 cyan；Filter dropdown 内 search 改 useDebouncedCallback 300ms 实时查询替 onBlur；ASSET_LIST_SORTS 加 'used_most' + SORT_LABEL 'Most used' + queries.ts loadAssetsForListing 加 sort 分支按 _count.referencedInEmailLogs desc；loadUsedIn 扩展 JOIN campaign.name + kol.name 返回，UsedInTab 显示真实名字 + 点击跳转 /campaigns/{id} 和 /kols/{id}；Wizard Step 3 Discard button 上方加 cost 提示 'Discards generated draft (this cost ~$0.001-0.005)'；Wizard Step 3 error state 加双 button (Back to Step 2 SecondaryButton + Try again GradientButton) + 友好 error message；守门 test 覆盖 AssetCard.test.tsx (后缀截除/StatusBadge/purple AI) + use-filter-state.test.ts (used_most) + loadUsedIn (campaignName/kolName) + e2e wizard 失败路径。详见 spec §F006。"
    }
  ]
}
