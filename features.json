{
  "sprint": "BI4-architectural-guardrails",
  "features": [
    {
      "id": "F001",
      "title": "Async Job Queue interface + in-memory stub executor",
      "priority": "high",
      "executor": "generator",
      "status": "pending",
      "acceptance": "src/lib/jobs/queue.ts 定义 JobQueue interface + JobPayload/JobOptions/JobHandler 类型 + InMemoryJobQueue 实现类（同步执行 handler，支持 idempotencyKey 去重、delay 延迟、tenantId scope）；导出全局单例 jobQueue；register<P>(name, handler) / add<P>(name, payload, options) / stats() 三 API；src/lib/jobs/handlers/register.ts 作为 handler 注册中心（MVP 为空留给 BM2）；instrumentation.ts import handlers/register 确保注册；tests/unit/jobs/queue.test.ts ≥ 5 cases 覆盖 register+add+handler 执行 / idempotencyKey 去重 / stats 准确 / handler throws 不崩主流程 / 未注册 name 给 warn log；TypeScript 泛型推导正确（jobQueue.add<SendEmailPayload> 推 payload type）"
    },
    {
      "id": "F002",
      "title": "event_log 表 + logEvent() helper",
      "priority": "high",
      "executor": "generator",
      "status": "pending",
      "acceptance": "prisma/schema.prisma 追加 model EventLog（id cuid / tenantId? / actorId? / type 64 / resourceId? 64 / payload Json / createdAt；@@index([tenantId,type,createdAt]) + @@index([resourceId]); @@map event_log）；migration 20260424000000_event_log 含完整 ROLLBACK SQL（F007 校验）；无 RLS（平台级记录）；src/lib/events/log.ts 导出 logEvent(data: EventData) fire-and-forget helper（失败 console.error 不抛）；tests/integration/event-log.test.ts ≥ 3 cases：logEvent → DB 记录 / DB 不可达时主流程不崩 / 查询 by tenant+type+时间范围；prisma.eventLog Client 生成后 TS 可用"
    },
    {
      "id": "F003",
      "title": "audit_log 表 + logAudit() helper",
      "priority": "high",
      "executor": "generator",
      "status": "pending",
      "acceptance": "prisma/schema.prisma 追加 model AuditLog（id cuid / tenantId? / actorId 必填 / action 64 / targetType 32 / targetId 64 / before? Json / after? Json / ipAddress? 45 / userAgent? Text / createdAt；@@index([tenantId,actorId,createdAt]) + @@index([targetType,targetId]); @@map audit_log）；migration 20260424000100_audit_log 含 ROLLBACK SQL；src/lib/audit/log.ts 导出 logAudit(data: AuditData) helper（actorId TypeScript 强制必填；失败仍 log stderr 但不抛主流程）；tests/integration/audit-log.test.ts ≥ 3 cases：成功记录 / actorId 缺失编译报错 / before-after diff 保留 / 查询 by actor+时间+by target"
    },
    {
      "id": "F004",
      "title": "Cursor pagination util createCursorPaginator<T>()",
      "priority": "high",
      "executor": "generator",
      "status": "pending",
      "acceptance": "src/lib/pagination/cursor.ts 导出 CursorPaginationParams + CursorPaginationResult<T> 类型 + createCursorPaginator<TModel, TWhere>(config) factory；封装 Prisma cursor + pageSize + orderBy + direction；cursor 用 URL-safe base64 包装 {id, sortField, sortValue}；defaultLimit 20 / maxLimit 100 可配置；tests/unit/pagination/cursor.test.ts ≥ 4 cases：分页前 3 条 / cursor 解出并继续 / hasMore false at end / limit cap 到 maxLimit / orderBy asc+desc / where filter 叠加；TypeScript 使用时泛型推导 items 为 TModel[]"
    },
    {
      "id": "F005",
      "title": "KOL tsvector 搜索索引 migration + helper",
      "priority": "high",
      "executor": "generator",
      "status": "pending",
      "acceptance": "migration 20260424000200_kol_tsvector 含 ALTER TABLE kol ADD COLUMN search_vector tsvector + UPDATE 填充现有行 + CREATE GIN INDEX + trigger function kol_search_vector_update + BEFORE INSERT OR UPDATE trigger；权重：name A / handle B / tags+knownBrandCollabs C；ROLLBACK SQL 含 drop trigger + function + index + column（F007 校验通过）；prisma schema 加 searchVector Unsupported('tsvector')? 字段声明；src/lib/search/tsvector.ts 导出 buildKolSearchQuery(userInput) + searchKols(tenantId, query, limit?) helper（后者用 prisma.$queryRaw）；tests/integration/kol-tsvector.test.ts ≥ 3 cases：insert KOL → search_vector 自动填充 / update name → 自动更新 / searchKols('dota') 命中 / 空 query 不报错 / EXPLAIN ANALYZE 验证 GIN 索引被用（不顺序扫描）"
    }
  ]
}
