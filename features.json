{
  "sprint": "BIx-mvp-polish-pass",
  "features": [
    {
      "id": "F001",
      "title": "/crm Header 3 个 disabled 控件清理（time toggle real / Export CSV impl / Manual log delete）",
      "priority": "high",
      "executor": "generator",
      "status": "pending",
      "acceptance": "/crm 时间 toggle（thisQuarter / Last 90d / allTime）3 个 button 全可点，按 created_at 范围 filter 4 个 CRM 组件（CrmKpiStrip/CrmPipelineBars/CrmFunnel/CrmRecentChanges），URL 参数 ?range=thisQuarter|last90d|allTime，default last90d；runCrmOverview 接受 range 参数；Export CSV 按钮真做 Server Action 生成 CRM 数据 CSV（KOL 列表 + 阶段 + kolCampaign + 金额）含 Content-Disposition; +Manual log 按钮从 CrmHeader 移除并删除 i18n key crm.header.manualLog*；tests/integration/crm-time-range.test.ts 验证 3 range × 4 组件；tests/integration/crm-export-csv.test.ts 验证 CSV 内容；visual baseline 重生（dashboard/crm 受影响）；staging git_sha 与本 commit 一致。"
    },
    {
      "id": "F002",
      "title": "Misc 6 项 polish（campaigns Owner filter / database Email btn / PDF 文案 / mock_sent fail-fast / AiSuggestions 已删 / dashboard newCampaign 按钮修复）",
      "priority": "high",
      "executor": "generator",
      "status": "pending",
      "acceptance": "/campaigns Owner filter 真做（取自 Campaign.ownerUserId，URL 参数 ?owner=<userId>，单 tenant 用户时 hide filter）；/database BulkActionBar Email 按钮 onClick → router.push('/outreach?kolIds=' + selectedKolIds.join(','))，/outreach 接收 ?kolIds=... 自动预选；/weekly-report PDF 帮助文案（button title='Save as PDF in the print dialog'，click 后 toast 提示）；邮件 mock_sent fail-fast（src/lib/email/resend.ts 在 NODE_ENV=production 下无 RESEND_API_KEY 时 throw 'RESEND_API_KEY missing in production'，dev 仍允许 mock_sent）；/campaigns AiSuggestionsCard 'Coming with B2' badge 已在 MVP-internal-demo-prep F007 处理（不重复）；Dashboard newCampaign 按钮修复（用户 2026-05-01 报告：GreetingBar GradientButton 无 onClick 也无路由跳转 → 点击无效；用户决议方案 A：GradientButton 加可选 href prop，render 分支 `href` 存在渲 next/link <Link> / 无 href 渲 <button>，disabled 或 loading 时 always 渲 <button>；GreetingBar 改用 <GradientButton href={`/${locale}/campaigns/new`} ...>；新增 GradientButton.test.tsx 2 个 it block 验证 href + href+disabled fallback；grep 全代码 <GradientButton 抓其他类似失效；staging 浏览器走查 zh+en 各一次跳转 PASS）；existing tests 不破坏；staging git_sha 与本 commit 一致。"
    },
    {
      "id": "F003",
      "title": "11 页 critical paths edge states + error.tsx 兜底",
      "priority": "high",
      "executor": "generator",
      "status": "pending",
      "acceptance": "11 页（dashboard/discovery/database/kols-detail/knowledge-base/campaigns/campaigns-detail/outreach/crm/roi/weekly-report）每页加 error.tsx 兜底（display friendly error + reload CTA），关键 empty state（spec §F003 表格列出每页 critical empty + critical error）有 friendly 引导（不出现纯白屏 / Lorem ipsum）；tests/integration/edge-states-coverage.test.ts 验证 11 页 error.tsx 存在性；手工 spot check：dev 模式停 PG → 11 页全部 graceful（非崩溃）；staging git_sha 与本 commit 一致。"
    },
    {
      "id": "F004",
      "title": "YouTube sync 配额优化（P1 ~89% + 真 engagement batch + Top 100 KOL latestVideos）",
      "priority": "high",
      "executor": "generator",
      "status": "pending",
      "acceptance": "search.list maxResults 10→50；DAILY_REGIONS 6→14（+GB/DE/BR/MX/TH/ID/IN/ES）；KEYWORD_POOL 每 region ≥ 12 词，dayOfYear%6 轮转 6 词；page 轮转 cursor 持久化 kol_sync_cursor 表 + migration + ROLLBACK SQL；publishedAfter 4 切片 dayOfYear%4 轮转 6 region × 100u = 600u/day；分层 refresh（Tier 1 top 50 by valueScore 每 3 天 / Tier 2 51-500 每 7 天 / Tier 3 500+ 每 21 天 / Flagged 必入），总日 ~150-200 KOL ~4u；Top 100 engagement batch（channels.list 2u + playlistItems.list 100u + videos.list 12u = 114u/day）写回 Kol.engagementRate + Kol.metadata.latestVideos；DailyLogLine schema 加 perMatrix + engagementBatchStats；tests/integration/sync-quota-optimization.test.ts 验证 14 region matrix + 池轮转 + 分层 refresh + engagement batch；staging 跑 7 天连续观察：每日 inserted ≥ 30 + 配额 8500-9200u + 0 errors；env var KOL_SYNC_PUBLISHED_AFTER_SLICES（默认 6，告警时降 4）；FILTER_MIN_SUBSCRIBERS 决议待 Planner 收用户答复（10K / 1K / env var 三选一）；staging git_sha 与本 commit 一致。"
    },
    {
      "id": "F005",
      "title": "前端 perf Critical + High 六件套（next.config.ts / next/font / next/image / recharts dynamic / markdown dynamic / AppShellLayout island）",
      "priority": "high",
      "executor": "generator",
      "status": "pending",
      "acceptance": "Part A next.config.ts 配 images.remotePatterns（YouTube/Twitch/TikTok/Bilibili CDN）+ formats avif/webp + optimizePackageImports recharts/@base-ui/react + serverExternalPackages @prisma/client/bcrypt/googleapis/resend + headers 6 项（X-Frame DENY / X-Content-Type nosniff / Referrer-Policy / Permissions-Policy / HSTS / CSP-Report-Only 一周观察期）；Part B Material Symbols 改 next/font 自托管子集（生成脚本 grep 全代码 icon name + 100+ 处 material-symbols-outlined 全渲染无字符方框 + 删 fonts.googleapis.com link）；Part C 7 处原生 <img> 替换 next/image（CampaignKolRow/KolHero/RecentVideosGrid/AvatarWithPlatformBadge/CampaignRow/SidebarUserChip/UserAvatarMenu，全部含 width/height 防 CLS + KolHero priority + 其他 lazy 默认）；Part D recharts 三处 dynamic（roi/RoiTrendChart + campaigns/[id]/EmailPerformanceChart + features/dashboard/EmailPerformanceChart）；Part E markdown dynamic 两处（weekly-report/WeeklyReportRenderer + outreach/templates/TemplateWorkspaceClient）；Part F AppShellLayout 拆 ActiveNavClient island（usePathname 仅在 island，外壳 server，sidebar 子树全 server）；npm run build + @next/bundle-analyzer 验证初始 JS 减 ≥ 200KB gzipped（audit 估 400-500KB 留余量）；curl -I https://staging.kol.guangai.ai/ 验证 6 头存在；Lighthouse 移动 perf ≥ 75；CSP Report-Only 一周观察期 violation log 0；visual baseline 重生（dashboard/campaigns/roi/kols-detail/login）；existing tests 不破坏；staging git_sha 与本 commit 一致。"
    }
  ]
}
