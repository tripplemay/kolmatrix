{
  "sprint": "B7-mvp-launch-ready",
  "features": [
    {
      "id": "F001",
      "title": "Embedding Pipeline + pgvector + KOL/Product Embed（一次性 + B6 daily 增量）",
      "priority": "high",
      "executor": "generator",
      "status": "pending",
      "acceptance": "**架构：** Embedding cosine + pgvector（替代原 LLM ranking 方案）\n\n**1. 数据库基础设施：**\n- 新 migration：启用 pgvector extension\n- Kol 表加 `embedding` 列（vector(1024) nullable）\n- Product 表加 `embedding` 列（vector(1024) nullable）\n- 加 IVFFlat 索引（KOL 库 < 10K 用 IVFFlat）\n- migration 含 ROLLBACK SQL（database-patterns.md §3）\n\n**2. 新建 src/lib/embedding/ 模块：**\n- types.ts — EmbedRequest / EmbedResponse / SimilarityResult\n- client.ts — 调 aigcgateway /v1/embeddings (bge-m3, multilingual, 1024 dims, $0.084/M)\n- kol-embed.ts — 一次性 embed 全部 Kol（batch 100/call）+ B6 daily 增量\n- cosine.ts — pgvector 内置 `<=>` operator 算余弦相似度\n\n**3. B6 cron 接力：**\n- 每日新爬的 KOL 自动 embed（修改 B6 F002 YouTube adapter，加 embed hook）\n\n**Cost：** 一次性 $0.0063 + 月 < $0.02（vs 原 LLM ranking $1-5/月）\n\n**Acceptance：** pgvector + Kol/Product embedding 列 + 索引；一次性 embed 1500+ KOL（staging）；B6 daily 增量接入；模块完整 + tests；event_log 'embedding.invoked' 埋点；migration ROLLBACK SQL；详见 docs/specs/B7-mvp-launch-ready-spec.md §F001"
    },
    {
      "id": "F002",
      "title": "/discovery AI Smart Match 实装（embedding 版，毫秒级响应）⭐⭐⭐",
      "priority": "high",
      "executor": "generator",
      "status": "pending",
      "acceptance": "**核心震撼点 — 当前 disabled placeholder 改为真实装。**\n\n1. 重写 /discovery Header：移除 disabled，加 onClick 打开 SmartMatchDialog\n2. 新建 SmartMatchDialog.tsx：选 Product → 极速 loading（< 200ms）→ top 10 KOL with match score (RingProgress) + 1 句 AI reasoning（可选 LLM 二次调用）\n3. API: POST /api/kols/smart-match { productId } → 拿 Product.embedding（无则即时 embed）→ SQL `ORDER BY embedding <=> $1 LIMIT 10` → < 100ms\n4. 'Save All to Campaign' 一键加入新 campaign\n5. 埋点 smart_match.invoked / kol_saved / batch_saved\n\n**Acceptance：** Smart Match 按钮**不再 disabled**；< 200ms 显示 top 10 KOL；'Save All' 跳转预填；visual baseline 重捕；tests/integration + tests/e2e；用户 spot check：staging demo 真实匹配 + 极速响应；详见 §F002"
    },
    {
      "id": "F003",
      "title": "/database AI Intelligence + Coverage Gap 实装",
      "priority": "high",
      "executor": "generator",
      "status": "pending",
      "acceptance": "**当前：** AI Intelligence Card disabled CTA + Coverage Gap 硬编 'Coming in B6'\n\n1. 新建 aigcgateway Action `kol-database-intelligence`（gemini-2.5-flash-lite，JSON mode，24h 缓存）\n2. 修改 InsightsPanel.tsx：AI Intelligence 真功能 + Coverage Gap 真计算（tenant categories vs gaming baseline）\n3. baseline 硬编：FPS 20% / MOBA 15% / RPG 15% / 手游 15% / Casual 20% / Esports 10% / 其他 5%\n\n**Acceptance：** AI Intelligence 真 AI 调用；Coverage Gap 真计算；localStorage 24h 缓存；tests/integration/database-intelligence.test.ts；staging spot check"
    },
    {
      "id": "F004",
      "title": "/campaigns/:id AI Suggestions 实装",
      "priority": "high",
      "executor": "generator",
      "status": "pending",
      "acceptance": "**当前：** AiSuggestionsCard hardcoded 静态 + 'Run AI match' disabled\n\n1. 新建 aigcgateway Action `campaign-next-action-suggest`（claude-haiku-4.5，24h 缓存）\n2. 修改 AiSuggestionsCard.tsx：真 'Generate Suggestions' 按钮 → 显示 3 个 next action（priority 标签 + 跳转）\n3. 移除 'Run AI match' 按钮（已被 Smart Match 替代）\n\n**Acceptance：** 真 AI 调用；'Run AI match' 隐藏；24h 缓存；tests/integration/campaign-suggest.test.ts"
    },
    {
      "id": "F005",
      "title": "/database Tier+Game filter 实装 + /discovery Save Search 实装",
      "priority": "high",
      "executor": "generator",
      "status": "pending",
      "acceptance": "**Tier filter：** Kol.valueScore 分桶 (high ≥80 / medium 60-79 / low <60 / unrated null)；Select dropdown 改 enabled；SQL `WHERE valueScore BETWEEN`\n\n**Game filter：** Kol.categories array 筛选（避免 schema 改）；multi-select；SQL `categories && ARRAY['FPS', ...]`\n\n**Save Search：** 新建 SavedSearch model (id, tenantId, userId, name, filters JSON)；migration ROLLBACK；API POST/GET /api/saved-searches；UI 'Save Search' 按钮 + '我的搜索 (3)' 下拉\n\n**Acceptance：** 3 项不再 disabled；visual baseline 重捕；tests/integration + filter combination；i18n 4 语言新 keys"
    },
    {
      "id": "F006",
      "title": "Polish 5 处 placeholder + tests + spec 链 + i18n + 守门",
      "priority": "high",
      "executor": "generator",
      "status": "pending",
      "acceptance": "**1. Placeholder polish：**\n- /campaigns/:id 'Run AI match' → F004 已隐藏 ✓\n- /database Email + Delete bulk → tooltip\n- /roi B4 buttons → tooltip 'Available in B4 Email System Extended'\n- /weekly-report B4 buttons → 同上\n- /discovery AI suggestion chips → 保持静态\n\n**2. i18n 4 语言新 keys：** F002-F008 全套 → npm run i18n:translate\n\n**3. 守门 tests：**\n- tests/unit/no-disabled-without-tooltip.test.ts\n- tests/unit/no-hardcoded-coming-soon-without-issue.test.ts\n\n**4. Spec 链 grep 验证：** PRD §7 已更新（commit 2312bfb）+ B5/B6/B7 cross-reference\n\n**5. 完整 tests：** 全套 unit + integration + E2E 全绿；typecheck/lint 不退化"
    },
    {
      "id": "F007",
      "title": "🆕 KOL 相似推荐（embedding 解锁，类比 Spotify 找到下一个）",
      "priority": "high",
      "executor": "generator",
      "status": "pending",
      "acceptance": "**1. /kols/[id] 底部'相似 KOL' section（5 个推荐）：**\n- SQL `ORDER BY embedding <=> (subquery) LIMIT 5`\n- < 50ms（pgvector 索引）\n- UI: 5 个 KOL 卡片（水平排列）+ '查看更多相似' 跳 /discovery\n\n**2. API: GET /api/kols/:id/similar?limit=5**\n\n**3. 埋点：** event_log kol_similar.viewed / kol_similar.clicked\n\n**Acceptance：** 详情页底部 5 个相似 KOL；点击跳详情生成新一组；< 100ms；tests/integration/kol-similar-api.test.ts；staging spot check（同类目 + 相似规模）；工时 ~2 day"
    },
    {
      "id": "F008",
      "title": "🆕 多语言 KOL 跨区匹配（embedding 解锁差异化能力）",
      "priority": "high",
      "executor": "generator",
      "status": "pending",
      "acceptance": "**业务场景：** 中文 marketer 找日本 FPS gaming KOL（bio 是日文）\n\n**1. /discovery 加'跨语言搜索'toggle（默认关）：** 打开后 query embed → cosine 匹配所有 KOL（不分 language）\n\n**2. API: GET /api/kols/cross-lang-search?q=...&limit=20** — query embed (bge-m3 多语言) → SQL `ORDER BY embedding <=> $1 LIMIT 20` → < 100ms\n\n**3. 演示价值：** '主播' query 命中日韩 KOL；'esports streamer' 命中中文 KOL（同义跨语言）\n\n**4. 埋点：** event_log cross_lang_search.invoked\n\n**Acceptance：** toggle 工作；中文 query 命中日韩 KOL（5 例 spot check）；tests/integration/cross-lang-search-api.test.ts；演示文档：MVP-seed-demo-prep F002 用户文档加跨语言 demo 章节；工时 ~1 day"
    }
  ]
}
