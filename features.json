[
  {
    "id": "F001",
    "title": "aigcgateway `kol-brief-parse` action 注册 + 复用 BL-067 runAigcAction SDK + BL-068 prompt v3 模式",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 4.0,
    "status": "completed",
    "completion_notes": "Action `kol-brief-parse` 注册成功 action_id=`cmp9wbt7q05xjbno11fuoim9l`，v1 (initial) input=4241 token 超 spec ceiling 2500 → v2 (active, version_id `cmp9wh9iz05xnbno1tyxxe6g7`) 裁剪后 input=2495 ✓ / output=414 ✓ / cost=$0.0046/call (与 spec §6 估算 $0.0045 一致)。LLM 输出 7 字段全正确：productId 严格 pool 内 / markets 标准化 (东南亚→SEA) / budget 提取 (10000 USD) / categories 小写连字符 / dates Q2 推断 (2026-04-01~06-30) / 5 locale feedback 全 / target_audience 提取。docs/specs/BL-069-F001-prompt-design.md 起草完整 (8 段含 §0 版本 + 调用契约 + token 估算 + SSH ops)。SSH 落 `AIGCGATEWAY_BRIEF_PARSE_ACTION_ID` 到 prod + staging .env (backup `.env.{production,staging}.bl069-f001.20260517-150531`，pm2 reload --update-env，health 200 healthy)。environment.md Actions 清单已更新。F001 spec ↔ codebase drift 上报：v1 prompt token 量超 spec ceiling，Generator 自决裁剪到 v2 (与 BL-067/BL-068 F001 处理 spec drift 同模式)，实测值落 §0 版本表 + §5 估算表。仅 docs + env 改动 → staging deploy 硬要求豁免 (per generator.md)。trace `trc_oc5ngzk11ouvox154akgweek`。",
    "acceptance": [
      "创建 `kol-brief-parse` action via MCP `create_action`：model=`claude-haiku-4-5`（per 决策点 #5 沿用 v0.9.22 模型）/ variables=`raw_brief` `available_products_json` `user_locale` / response_format=`json_object` / prompt（system，中文骨架，含 BL-068 v0.9.22 #11 prompt v3 模式：§⚠️ 自检 + 末尾 reminder）要求 LLM 解析自然语言 brief → 输出 JSON `{ productId?, markets: string[], budget: { amount, currency } | null, target_audience: string, categories: string[], start_date?, end_date?, feedback_summary: { en, zh, ja, ko, es }, unparsable: false }`；若不可解析输出 `{ unparsable: true, reason_locale: { en, zh, ja, ko, es } }`",
      "Action ID 落 `.env.staging` + `.env.production` 变量 `AIGCGATEWAY_BRIEF_PARSE_ACTION_ID`（SSH ops 落地，同 BL-067/BL-068 F001 模式 5 处 sync 协议）",
      "MCP `run_action` dry_run 验证 prompt 渲染正确 + token 估算（实测 ceiling：≤2500 input + ≤1200 output）",
      "F001 commit 含 prompt 原文 + 输入/输出 schema 文档化落 `docs/specs/BL-069-F001-prompt-design.md`",
      "**直接复用 BL-067 F001 沉淀的 `src/lib/aigc/run-action.ts` `runAigcAction<T>` SDK**（不新建抽象层）",
      "**直接复用 BL-068 fix-round 3 v3 prompt 模式**（自检 § + 末尾 reminder）",
      "L1 PASS（无代码改动，仅 action 注册 + env 文档化）",
      "staging git_sha 与本 commit 一致"
    ]
  },
  {
    "id": "F002",
    "title": "brief-actions.ts server action — LLM 解析 + 自动填表 + audit_log",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 6.0,
    "status": "completed",
    "completion_notes": "新文件 src/app/[locale]/(app)/brief/brief-actions.ts (use server) parseBriefAction 11 步流程，复用 BL-067 F001 SDK + BL-068 F002 audit/dedupe 模式。3 audit action types ai_brief.parse_{cap_exhausted,unparsable,applied} dot 形式；actionLabel 'ai_brief_parse' snake_case (event_log group-by 一致)。spec ↔ codebase drift 自决 2 项：(1) Product.id 是 cuid 不是 UUID → 验证用 Set membership 不用 UUID regex (2) Product.category 是单 String 字段不是数组 → mapper 时 wrap [product.category] 让 prompt 不动。__tests__/brief-actions.test.ts 9 case 全 mock 跨模块依赖 (auth/cost-cap/aigc SDK/audit/rate-limit/prisma): success + cap pre-check + unparsable + productId 跨 tenant + rate limit + 5 locale (zh) + race cap + malformed + unauthorized。L1 PASS: lint 0 errors / tsc 0 / vitest 19/19 (F002 9 + BL-068 F002 10 无 regression)。F002 含运行时代码 → staging deploy 5 步硬要求 (git pull + npm ci + prisma migrate + GIT_SHA build + pm2 reload --update-env) → curl staging /api/health git_sha == 本 commit short.",
    "acceptance": [
      "新文件 `src/app/[locale]/(app)/brief/brief-actions.ts`（\"use server\"）含 `parseBriefAction({ rawBrief, locale }): Promise<{ parsed: BriefFields | null; feedback: string; unparsable: boolean; capExhausted: boolean }>` 流程：(1) session auth + tenant scope / (2) checkLlmCostBudget 满 → capExhausted=true + audit_log ai_brief.parse_cap_exhausted / (3) rateLimitBatchSend block → throw / (4) fetch available products via withTenant RLS / (5) 拼装 input + 调 runAigcAction(BRIEF_PARSE_ACTION_ID, variables, tenantId) / (6) unparsable=true → audit_log ai_brief.parse_unparsable / (7) unparsable=false → 验 productId 跨 tenant + 返回 parsed + audit_log ai_brief.parse_applied",
      "audit_log shape: `logAudit({ actorId, action: 'ai_brief.parse_*', targetType: 'brief', targetId: 'draft', tenantId, after: { raw_brief?, parsed_fields?, unparsable?, locale } })`",
      "3 audit action types: `ai_brief.parse_cap_exhausted` / `ai_brief.parse_unparsable` / `ai_brief.parse_applied`",
      "**productId 跨 tenant 验证（§5 不变量 #5）**：LLM 输出 productId 必须在用户 tenant 内（withTenant RLS）；防 LLM 幻觉跨 tenant id → fallback unparsable + audit_log refine_unparsable payload { reason: 'productId_cross_tenant' }",
      "Rate limit 沿用 `rateLimitBatchSend (20/min/user)`（per 决策点 #5）",
      "单测 ≥6 case：success / cap 满 capExhausted / unparsable / productId 跨 tenant 验证失败 / rate limit / 5 locale 输入 feedback 输出对应 locale",
      "L1 PASS",
      "staging git_sha 与本 commit 一致"
    ]
  },
  {
    "id": "F003",
    "title": "/brief 页面 layout — 表单 + 顶部 AI brief input bar + product 选择器嵌入",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 8.0,
    "status": "completed",
    "completion_notes": "新建 4 文件: brief/page.tsx (server, fetch products + i18n labels + tab routing) + brief/BriefPageClient.tsx (client wrapper, ref-based wiring) + brief/BriefAiInputBar.tsx (RefineInputBar 模式, soft 5s timer + 6 toast variants含errorKind 3 子路径区分) + brief/CampaignForm.tsx (forwardRef + useImperativeHandle 暴露 applyParsed, 受控 state, 8 字段: product/name/budget(amount+currency)/dates/markets/targetAudience/categories + diff hint per §5 不变量 #6 仅填空字段). i18n 5 locale 完整 brief.* 命名空间 (pageTitle/tabCampaign/tabProducts/aiInputBar.*/form.*/markets.*) en/zh/ja/ko/es 全 cover. F005 submit 暂为 console.log stub. F004 ?tab=products 占位待 F004 实装 ProductListPanel. __tests__/BriefPageClient.test.tsx 7 case (spec ≥5): mount 空表单 / Generate 成功填空字段 / unparsable 表单不变+toast / cap 满 toast / 已填字段不被覆盖 + diff hint 显 / network 错 / product_cross_tenant distinct toast. L1 PASS: lint 0 errors / tsc 0 / vitest brief/ 16/16 (F002 9 + F003 7) + 全量回归无 regression. staging deploy 5 步 + curl /api/health 验 git_sha 一致.",
    "acceptance": [
      "新文件 `src/app/[locale]/(app)/brief/page.tsx`（server component）：顶部 BriefAiInputBar 组件 (client) + 中部 CampaignForm 组件 (client) + 默认 tab 渲 form, ?tab=products 渲 F004 ProductListPanel",
      "BriefAiInputBar (client) `src/app/[locale]/(app)/brief/BriefAiInputBar.tsx`：Props `{ locale, onParsed }` / input bar (placeholder=`brief.aiInputPlaceholder`) + 'Generate' button + Loading state 5s timeout (per BL-067 F004 同模式) / 点击 Generate → 调 F002 parseBriefAction → 成功 (unparsable=false) → onParsed(parsedFields) 自动填 CampaignForm 字段 / unparsable → toast `brief.aiUnparsableToast` (BL-068 silent fallback 同模式) / capExhausted → toast `brief.aiCapExhaustedToast` / 5s timeout → toast `brief.aiNetworkError`",
      "CampaignForm (client) `src/app/[locale]/(app)/brief/CampaignForm.tsx`：product 选择器 (radio list, 含 'Manage products' 链 ?tab=products) + markets multi-select + budget input + dates picker + target_audience textarea + 'Submit' button → 调 F005 createCampaignFromBriefAction",
      "**不覆盖用户已填字段（§5 不变量 #6）**：onParsed 仅填空字段，已填字段保留 + 显 'AI 建议' diff hint（小图标 + tooltip 说明 'AI 建议 X (你已填 Y)'）",
      "page.tsx getTranslations('brief') 拿 i18n keys 传给 client components",
      "单测 ≥5 case：mount 空表单 / Generate 成功 → 字段自动填 / Generate unparsable → 表单不变 + toast / Generate cap 满 → toast + 表单不变 / 已填字段不被 LLM 覆盖 + diff hint 显",
      "L1 PASS",
      "staging git_sha 与本 commit 一致"
    ]
  },
  {
    "id": "F004",
    "title": "/brief?tab=products product list CRUD — 从 KB 迁移",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 6.0,
    "status": "completed",
    "completion_notes": "新建 brief/ProductListPanel.tsx (~75 LOC, server) 作 thin wrapper: fetch products via withTenant RLS + loadProductAssetCounts → 渲染 KB ProductsClient (零重写)。给 KB ProductsClient 加 optional initialEditingProductId prop (back-compat: KB /knowledge-base 不传, 现有行为不变)，用 lazy useState initializer + resolveInitialDeepLink helper 替代 useEffect (避开 react-hooks/set-state-in-effect lint 规则)。brief/page.tsx tab=products 分支 ProductsPanelPlaceholder → ProductListPanel(initialEditingProductId=sp.productId), 支持 /brief?tab=products&productId=:id deep link 自动打开 edit modal。BL-070 二次清理时 git mv KB ProductsClient + ProductCard + ProductModal + types + actions 到 brief/, 删 /knowledge-base 整目录。__tests__/ProductsClient-deep-link.test.tsx 4 case (spec ≥4): initialEditingProductId 匹配→modal opens 字段 prefilled / undefined (KB caller)→modal closed grid visible / 不在 list (silent fallback)→modal closed / Add new product click 仍 create mode (regression guard). L1 PASS: lint 0 errors / tsc 0 / brief+KB 5 files 40 tests 全 PASS (F004 4 + F003 7 + F002 9 + KB ProductCard/actions 20). i18n 复用 knowledgeBase.* 命名空间 (无需新加 brief.products.* — F006 给 KB i18n 加 _deprecated_by_BL-069 marker). F004 含运行时代码 → staging deploy 5 步硬要求.",
    "acceptance": [
      "/brief 页 + `?tab=products` query param 渲不同 view：默认 (无 tab / tab=campaign) → F003 CampaignForm + BriefAiInputBar；tab=products → ProductListPanel",
      "ProductListPanel `src/app/[locale]/(app)/brief/ProductListPanel.tsx`：product table (name / categories / created_at / actions edit/delete) + 'New product' button → ProductFormDialog",
      "ProductFormDialog 复用 KB 现有 `src/app/[locale]/(app)/knowledge-base/ProductFormDialog.tsx`（不重写，仅 import 路径切换或 git mv 到 brief/）",
      "路由切换：`/brief` ↔ `/brief?tab=products` 不重新 mount 整页（仅切换 tab content 区域 + URL state）",
      "deep link `/brief?tab=products&productId=xxx` 支持自动打开对应 product edit dialog",
      "单测 ≥4 case：默认 tab 渲 CampaignForm / ?tab=products 渲 ProductListPanel / tab 切换 URL 状态保持 / ProductFormDialog 编辑 product 后 refresh list",
      "L1 PASS",
      "staging git_sha 与本 commit 一致"
    ]
  },
  {
    "id": "F005",
    "title": "提交 brief 后自动跳 /match?campaignId + 触发 BL-067 F005 pre-warm worker",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 4.0,
    "status": "completed",
    "completion_notes": "决策点 (用户 5/18 ack option B + auto-name option A): brief-actions.ts 新建 createCampaignFromBriefAction 复用 createCampaignRecord 沉淀, kpiTarget JSON stash {brief, targetAudience, categories} 0 schema migration. createCampaignRecord 加 optional 3rd 参 CreateCampaignExtras {budgetCurrency?, briefMeta?} (back-compat BL-066 callers 不变). BriefPageClient onSubmit stub → 真 createCampaignFromBriefAction 调用 + router.push(/{locale}/match?campaignId=...) 成功后 / submitError state + brief-submit-error banner 失败时. auto-name 规则: 用户填名 → 用; 留空 → '{productName} — {markets[0].toUpperCase()}'. i18n brief.submitErrors.{unauthorized,validationFailed,productNotFound,internalError} 4 keys × 5 locale = 20 entries. 单测: brief-create-campaign.test.ts 5 case (success + auto-name + user-typed name override + unauthorized + product_not_found + db_error 5/5 PASS) + BriefPageClient.test.tsx 加 3 case (router.push success / product_not_found banner / 客户端 validation guard) 10/10 PASS. L1: lint 0 errors / tsc 0 / vitest brief+KB+campaigns+i18n 17 files 133 tests PASS. /match?campaignId mount 已 wire BL-067 F005 prewarm (不动 BL-066/067 实装). F005 含运行时代码 + Campaign budgetCurrency 字段从硬编码升级 → staging deploy 5 步硬要求.",
    "acceptance": [
      "F003 CampaignForm 提交流程 server action `createCampaignFromBriefAction({ productId, markets, budget, dates, target_audience })`（新建在 brief-actions.ts 或 reuse BL-066 现有 createCampaignAction，必要时扩字段 + 同 commit 加 migration）",
      "成功 → router.push(`/match?campaignId=${newCampaign.id}`) (client component, 用 next/navigation)",
      "/match?campaignId mount 时（BL-065 已建）→ AiRecommendationPanel mount → smart-match → enqueue BL-067 F005 pre-warm worker（已 wire, 不动 BL-066/BL-067 实装）",
      "端到端链路连贯（< 30s 含 prewarm）：用户从 brief 提交 → 跳 /match → 30s 内看到 top 30 KOL with short LLM explanation",
      "e2e 验证：提交 brief → 自动跳 /match → top 30 KOL 显（cache hit 后 reload 验 short）",
      "单测 ≥3 case：createCampaignFromBriefAction 成功 / unauthorized / db insert 失败 throw",
      "L1 PASS",
      "staging git_sha 与本 commit 一致"
    ]
  },
  {
    "id": "F006",
    "title": "老路由 redirect (KB+Campaigns/new→/brief) + 5 语言 i18n + e2e brief-flow.spec.ts 6 case",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 6.0,
    "status": "pending",
    "acceptance": [
      "**老路由 redirect（per 决策点 #2 完全 301 redirect）**：(1) /knowledge-base → /brief?tab=products / (2) /knowledge-base/[productId] → /brief?tab=products&productId=xxx (保 deep link) / (3) /campaigns/new → /brief?action=new",
      "middleware-helpers.ts 加 3 条 REDIRECT_RULES + `src/__tests__/middleware-helpers.test.ts` 同步加 case",
      "tests/e2e/ia-refactor-redirects.spec.ts 加 3 条 REDIRECT_CASES 验 301 + URL preservation (deep link)",
      "`messages/{en,zh,ja,ko,es}.json` 加 `brief.*` 完整 keys：`pageTitle` / `tabCampaign` / `tabProducts` / `aiInputPlaceholder` / `generateButton` / `aiUnparsableToast` / `aiCapExhaustedToast` / `aiNetworkError` / `submitButton` / `productSelectorLabel` / `manageProductsLink` / `aiDiffHint` 等",
      "5 语言全 cover 不留 `_deprecated_by_*` marker（新 keys）",
      "老 KB / Campaigns/new i18n keys 加 `_deprecated_by_BL-069` 子 key marker（留 BL-070 二次清理删）",
      "新 `tests/e2e/brief-flow.spec.ts` 6 case：(1) /brief 默认 tab 渲 CampaignForm + BriefAiInputBar / (2) /brief?tab=products 渲 ProductListPanel / (3) AI brief Generate 成功 → 表单自动填 + toast feedback / (4) AI brief unparsable → toast + 表单保留空 / (5) 提交表单 → 自动跳 /match?campaignId 验 URL / (6) 老路由 redirect 301 verify × 3 条",
      "L1 PASS（lint + tsc + vitest + e2e）",
      "staging git_sha 与本 commit 一致"
    ]
  },
  {
    "id": "F007",
    "title": "staging deploy + 视觉 baseline + 24h cost 监控 + signoff prep",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 4.0,
    "status": "pending",
    "acceptance": [
      "staging deploy via `deploy-staging.yml`（含 `AIGCGATEWAY_BRIEF_PARSE_ACTION_ID` 新 env var via SSH 落 `.env.staging`，与 prod 配齐 sync 协议）",
      "视觉 baseline regen via `update-visual-baselines` workflow：`en-brief.png` 新生成（含 BriefAiInputBar + CampaignForm）+ `en-brief-products.png` 新生成（?tab=products view）",
      "Planner 在 building 后期出 staging dogfood 清单：≥10 个 brief query 实测（覆盖 markets / budget / target_audience / locale 4 维度）+ ≥3 个 unparsable case + cap 满模拟 + 端到端: brief → submit → /match prewarm hit 链路，commit 落 `docs/test-reports/BL-069-staging-spot-check.md`",
      "24h aigcgateway dashboard cost 监控：daily total cost ≤ 团队 dogfood 实际 cost × 1.5",
      "`scripts/bl069-cost-audit.ts` 拉 audit_log type='ai_brief.parse_*' 24h 累计 cost / token / call 数报告 + parse success rate（≥80% gate per roadmap §11 Phase 4 沿用 Phase 3 标准）",
      "`docs/test-reports/BL-069-signoff-2026-05-XX.md` Reviewer 写最终结论：parse success rate ≥80% + cost 在 cap 内 + 5 locale 全绿 + 6 e2e PASS + 老路由 redirect 验证",
      "Reviewer 复验全部 acceptance + signoff，progress.json `status: reverifying → done`"
    ]
  }
]
