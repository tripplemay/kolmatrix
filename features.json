{
  "sprint": "BL-027-asset-followup-icon-hotfix",
  "features": [
    {
      "id": "F001",
      "title": "BL-027 spec 起草（Planning artifact）",
      "priority": "high",
      "executor": "generator",
      "status": "completed",
      "acceptance": "docs/specs/BL-027-asset-followup-icon-hotfix-spec.md 起草完成（含 §S1-S8 + §F001-F007 详细 acceptance + §S2-S6 数据/部署/30+ checklist/风险/时间线/Out-of-Scope）；features.json sprint=BL-027 含 7 features F001=completed/F002-F007=pending；progress.json status=building / completed_features=1 / generator_handoff 含执行顺序+约束+风险；.auto-memory/project-status.md 反映 BL-027 starting + prod a9c4ef8 含 icon bug。0 LoC。本 feature 在批次启动时已由 Planner 落地完成。详见 spec §F001。"
    },
    {
      "id": "F002",
      "title": "Icon hotfix: woff2 regen + 4 visual baseline 重生（filter_alt + arrow_drop_down）",
      "priority": "high",
      "executor": "generator",
      "status": "pending",
      "acceptance": "跑 bash scripts/regenerate-material-symbols-subset.sh，验证 stdout 含 'discovered N unique icons' 且 N 包含 filter_alt + arrow_drop_down（Pattern 2 命中）；commit src/app/fonts/material-symbols-outlined.woff2 增量（9716 → 9976 bytes ≈ 2 新 glyph）；commit message 'fix(BL-027-F002): regenerate material-symbols woff2 to include filter_alt + arrow_drop_down'；触发 gh workflow run update-visual-baselines.yml --ref main 后 4 baseline 重生（en-assets / -drawer-open / -filter-dropdown / -empty-system-seed）入 main；workflow GITHUB_TOKEN push 不触发下游 CI per deploy-patterns.md §4.1，按需 chore retrigger；staging deploy 后浏览器开 /en/assets 走查 ActionBar Filter 按钮渲染 filter_alt icon glyph + Sort 渲染 arrow_drop_down + DevTools Inspect computed style font-family='Material Symbols Outlined'。详见 spec §F002。"
    },
    {
      "id": "F003",
      "title": "F009 守门 test 加反向覆盖 case (woff2 ≡ script output)",
      "priority": "high",
      "executor": "generator",
      "status": "pending",
      "acceptance": "tests/integration/material-symbols-coverage.test.ts 加新 case #7 'woff2 is up-to-date — regen script run produces no diff vs committed woff2'：用 fs.readFileSync(WOFF2) Buffer.equals 比较 script 跑前后 woff2 字节相同；失败信息明确 'Run bash scripts/regenerate-material-symbols-subset.sh and commit the updated woff2'；本批次 F002 commit woff2 后跑 case #7 → PASS；手动 mv woff2 woff2.bak 验证 → 应 FAIL with 清晰信息（仅本地，不 commit）；npm run test:integration 跑全 7 case 全 PASS（原 6 + 新 1）。详见 spec §F003。"
    },
    {
      "id": "F004",
      "title": "Pre-commit hook 自动 regen woff2 when icons change",
      "priority": "high",
      "executor": "generator",
      "status": "pending",
      "acceptance": "扩展 framework/templates/pre-commit-hook.sh（现 38 行 STATE_FILES 校验之后加 'Material Symbols subset coverage' section）：检测 git diff --cached --name-only 含 src/**/*.tsx|*.ts|scripts/material-symbols-icons-manifest.txt 改动 + 该文件 grep 含 material-symbols-outlined → 跑 script + sha256sum diff woff2，woff2 改变但未 staged → reject commit + print 'git add src/app/fonts/material-symbols-outlined.woff2'；woff2 改变且已 staged → PASS；woff2 不变 → PASS；新建 tests/integration/pre-commit-hook.test.ts 至少 3 case (happy no-icon / icon staged / icon unstaged 拒) 通过 child_process.execSync mock git diff；docs/dev/rules.md 加 hook 启用说明 'cp framework/templates/pre-commit-hook.sh .git/hooks/pre-commit && chmod +x'；hook 顶部 comment 块加 v0.9.7 BL-027-F004 来源标注。详见 spec §F004。"
    },
    {
      "id": "F005",
      "title": "PR template Material Symbols checklist 强化（统一 static + dynamic 都需 script）",
      "priority": "medium",
      "executor": "generator",
      "status": "pending",
      "acceptance": ".github/pull_request_template.md 删除当前 §'Material Symbols icon changes' 三选一结构（lines 18-44 的 Static usage only 路径），改 2 选一 + 子 checkbox：(1) Not applicable / (2) Has icon changes 含 3 子项（manifest 更新如果 dynamic / 跑 script + commit woff2 / npm run test:integration material-symbols-coverage 全过）；模板顶部 comment 块更新来源标注 'BL-025-F009 + BL-027-F005'；commit message 含 BL-027-F005 标签；GitHub 自动渲染新 template 在新 PR 中验证。详见 spec §F005。"
    },
    {
      "id": "F006",
      "title": "BL-026 Soft-watch test backfill (S2 + S3 + S4)",
      "priority": "medium",
      "executor": "generator",
      "status": "pending",
      "acceptance": "S2: tests/e2e/assets-page.spec.ts 加 3 类新 case (filter dropdown 行为 / drawer 打开关闭 backdrop+Esc / mobile <768px viewport drawer 全屏)，既有 8 case 跑通；S3: 新建 tests/integration/assets-empty-welcome.test.ts 5 case (empty tenant→mode='welcome' / 含 user_created→mode='normal' / welcome 含 5 套 system_seed / banner 文案 / system_seed readOnly=true quick actions 仅 Duplicate)；S4: 新建 tests/e2e/outreach-composer-template-select.spec.ts 3 case (happy path / product filter / search debounce 验证 ≤1 fetch in 300ms)；S4: queries.test.ts + composer-load-templates.test.ts 各加 2 case (loadAssetsForComposer search 匹配 name + productId filter)；CI 全绿。详见 spec §F006。"
    },
    {
      "id": "F007",
      "title": "environment.md 修正 (S10 staging RAM + S11 NODE_OPTIONS 注释)",
      "priority": "low",
      "executor": "generator",
      "status": "pending",
      "acceptance": ".auto-memory/environment.md 加 §'Staging 服务器'（如无）含 RAM 8GB（不是 16GB）+ 部署路径 + Env 文件 + PM2 + Postgres + Redis；§'扩容信号'之前加 §'Staging build OOM 兜底'：'NODE_OPTIONS=--max-old-space-size=4096 npm run build（staging RAM 8GB，默认 1.6GB heap 在 build OOM；prod 16GB 不受此限）'；同步验证 framework/harness/deploy-patterns.md §3.2 step 6 已含 NODE_OPTIONS（已沉淀仅 environment 需更正）。详见 spec §F007。"
    }
  ]
}
