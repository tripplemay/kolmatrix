[
  {
    "id": "F001",
    "title": "mapper 接 fork emails 字段 + types.ts + 单测",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 1.5,
    "status": "done",
    "acceptance": [
      "src/lib/kol-sync/types.ts RawKolData 加 emails?: string[] | null",
      "src/lib/kol-sync/adapters/apify-kol.ts mapper 持久化 item.emails → RawKolData.emails (zod schema 已 passthrough, 显式加 emails: z.array(z.string()).nullable().optional())",
      "mapper try/catch + zod validation: fork 返非 array 或含 non-string → fallback null + console.warn log (不阻塞 batch)",
      "现有 6 个 kol.email 不动 (mapper 不覆盖 single-email 字段, 只填新加的 emails array)",
      "单测 ≥4 case: (1) fork emails=['a@b.com'] → kol.emails=['a@b.com'] (2) 多 email → array 完整 (3) emails undefined → null (4) emails 含非 string → fallback null + warn",
      "L1 PASS: lint 0err warn≤3 / tsc=0 / npm test 1423+ pass (新增 apify-kol-mapper emails case)"
    ]
  },
  {
    "id": "F002",
    "title": "Schema migration: kol 表加 emails JSONB + email_source 复用/新增",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 1.0,
    "status": "done",
    "acceptance": [
      "prisma/schema.prisma Kol model 加 emails Json? @map('emails') @db.JsonB",
      "现 kol 表已有 email_source 字段 (VARCHAR(20) default 'manual') - 复用 vs 新加 由 Generator 实地决定, 若复用确保 'business-unlock' 'bio-regex' 值与现有 'manual' 并存",
      "migration 手写 SQL (BL-082 同 ops 模式避免 prisma migrate dev checksum 漂移) 放 20260605XXXXXX_bl_083_add_emails_jsonb/",
      "ROLLBACK SQL header: DROP COLUMN emails;",
      "BACKFILL: 不需 (新字段允许 NULL, 现有 2371 KOL 全 NULL 即 'mapper 未填')",
      "RLS policy 不变",
      "L1 PASS: prisma validate / migrate deploy / tsc=0"
    ]
  },
  {
    "id": "F003",
    "title": "import.ts upsert: 写 emails array + email_source = 'business-unlock'",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 1.5,
    "status": "done",
    "acceptance": [
      "src/lib/kol-sync/import.ts KolUpsertPayload 加 emails?: string[] + emailSource?: string",
      "upsert data (create + update path) 带这两字段; emails 来自 mapper 的 RawKolData.emails, emailSource='business-unlock' 当 mapper.emails 非空时",
      "primary email 兼容: 现 kol.email 字段不覆盖 (BL-031 bio-regex 残留 6 行保留); 新写入的 KOL 如 mapper.emails=['a@b.com'] 仅写 emails=['a@b.com'], kol.email 留 NULL (下游 outreach 改读 emails 而非 email — 在 F005 outreach 调整)",
      "单测扩充 ≥3 case: (1) mapper.emails 非空 → emails 写入 + email_source='business-unlock' (2) mapper.emails null → 不写 (3) update existing KOL 时新增 emails 不破坏 kol.email",
      "L1 PASS"
    ]
  },
  {
    "id": "F004",
    "title": "UI /kol/[id] + /discovery + /database: emails 显示 + source chip + has-business-email filter",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 3.0,
    "status": "done",
    "acceptance": [
      "src/app/[locale]/(app)/kols/[id]/page.tsx detail 页加 'Contact emails' 段: 列出 emails array, 每个 email 旁边 chip 标 source ('已通过 YouTube 商务邮箱解锁' 绿色 / 'Bio 文本提取' 灰色)",
      "/discovery + /database 列表加 filter chip 'has business email' (筛 emails 数组非空)",
      "i18n 5 locale (zh/en/ja/ko/es) 加 keys: emailSource.businessUnlock / emailSource.bioRegex / filter.hasBusinessEmail",
      "兼容现有 KOL 无 emails 字段: detail 页 fallback 'Email 未公开' placeholder",
      "Vitest UI 组件单测 ≥2 case (display + filter)",
      "L1 PASS"
    ]
  },
  {
    "id": "F005",
    "title": "Outreach composer 优先级: business-unlock 高亮 + bio-regex tooltip 警告",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 2.0,
    "status": "pending",
    "acceptance": [
      "src/app/[locale]/(app)/outreach/composer 等路径 (Generator grep 实定位): KOL 选择 dropdown 显示 emails 时, business-unlock 绿色高亮 + 默认选中, bio-regex 灰显 + tooltip '此 email 来自 bio 文本提取, 请二次确认'",
      "如 KOL 只有 bio-regex email, composer 入口加警告 banner '该 KOL 仅有 bio 提取 email, 投递准确率较低'",
      "outreach send 路径如 KOL.emails 多个, 默认用 emails[0] (优先 business-unlock 排序后的 primary)",
      "i18n 5 locale 加 outreach.tooltip.bioRegexWarning / outreach.banner.bioOnlyWarning",
      "单测 ≥2 case (composer KOL 选择 + send default email)",
      "L1 PASS"
    ]
  },
  {
    "id": "F006",
    "title": "prod backfill script: 219 个 metadata.raw.emails → kol.emails + email_source='business-unlock'",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 1.5,
    "status": "pending",
    "acceptance": [
      "新建 scripts/kol-emails-backfill.ts: 遍历 prod KOL platform='youtube', WHERE metadata->'raw'->'emails' IS NOT NULL AND jsonb_array_length(metadata->'raw'->'emails')>0 AND kol.emails IS NULL → UPDATE kol.emails = metadata.raw.emails, email_source='business-unlock'",
      "idempotent: 已填的 kol.emails 不动 (WHERE kol.emails IS NULL)",
      "dry-run (统计可回填行数) + apply 模式; 纯 DB UPDATE 无 LLM 无 fork 调用",
      "runBackfill 抽为可测函数; 单测 ≥3 (dry-run 不写 / apply 写 / 已填行跳过)",
      "Staging 验证: dry-run 预期 N>0, apply 后抽样 ≥3 KOL 验 emails 写入",
      "Prod 验证 (用户授权后跑): dry-run eligible ≥200 (期望 219+), apply 后 SQL 抽样 ≥5 KOL emails 填充 + email_source='business-unlock'",
      "L1 PASS"
    ]
  },
  {
    "id": "F007",
    "title": "Codex Reviewer L1+L2 + signoff (executor:codex)",
    "executor": "codex",
    "priority": "high",
    "estimated_hours": 1.5,
    "status": "pending",
    "acceptance": [
      "L1 自动化 5 项 PASS: (1) npm run lint 0 error warning≤3 (2) npx tsc --noEmit (3) npm test 含 mapper/import/backfill/UI/outreach 单测 (4) npx prisma validate + migration ROLLBACK SQL valid (5) i18n 5 locale 新 key grep 一致 (无 missing)",
      "L2 staging 实测 6 项 PASS: (1) emails backfill 抽样 ≥5 KOL kol.emails 非空 + email_source='business-unlock' (2) /kol/[id] UI 显示 emails 列表 + source chip 正确 (3) /discovery + /database has-business-email filter 工作 (4) outreach composer business-unlock 高亮 + bio-regex tooltip (5) 0 daily-sync errors (mapper 接 emails 不破 BL-082 refresh) (6) 现有 6 个 kol.email 字段值不变",
      "Prod 只读核验: backfill 后 SQL 抽样 ≥5 KOL emails 填充率合理 + outreach send 试发 (用户授权下) business-unlock email",
      "signoff doc docs/test-reports/BL-083-signoff-2026-06-XX.md 含 L1/L2 结果 + 量化数据 (kol.emails 填充率 0.8% → 30%+) + 终签"
    ]
  }
]
