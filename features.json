[
  {
    "id": "F001",
    "title": "cost-cap 改累加真实 costUsd + 排除后台 source (A+B 查询侧) (generator)",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 1.5,
    "status": "pending",
    "acceptance": [
      "cost-cap.ts assertDailyCostBudget(+checkLlmCostBudget): count(events)×0.01 → sum(payload->>costUsd)(今日/本租户/ai.usage)与 limit 比; null/缺失 costUsd 按 0 或保守小额",
      "B 查询侧: sum 排除后台标记(payload->>source='system')→后台 AI 不计入用户前台配额",
      "保持 fail-open(limit≤0 返回)+ 抛 AiDailyCostExceededError(金额用真实 sum)",
      "单测(sum 真实成本 / system source 排除 / 真实达$5才触顶 / null costUsd / fail-open); L1 全绿"
    ]
  },
  {
    "id": "F002",
    "title": "后台 AI 调用打 source=system 标 (B 调用侧) (generator)",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 1.5,
    "status": "pending",
    "acceptance": [
      "run-action.ts runAigcAction opts 加可选 flag(如 costBucket:'system'|'user' 默认 user)→透传 recordAiUsage extras source",
      "后台 call sites 标 system: kol-sync/enrichment-stage.ts(kol_country_enrichment)+ explain prewarm worker(explain-recommendations-worker/prewarm-actions)+ grep 识别其它系统cron/worker/backfill触发的 runAigcAction",
      "前台不动(默认 user): match_rerank/explain dialog/email_customize/brief_parse/refine/roi_insights 等用户主动触发",
      "单测(后台调用 recordAiUsage 带 source=system / 前台默认 user); L1 全绿"
    ]
  },
  {
    "id": "F003",
    "title": "Codex L1+L2 + signoff (executor:codex)",
    "executor": "codex",
    "priority": "high",
    "estimated_hours": 1.0,
    "status": "pending",
    "acceptance": [
      "L1: lint 0err warn≤3 / tsc=0 / npm test",
      "L2 部署后 prod/staging: ① AI Match 重排恢复(不再'AI重排暂不可用') ② KOL 问号解释恢复(不再'配额已用尽') ③ 后台 country enrichment 不再吃前台配额 ④ cap 反映真实 USD(sum costUsd) ⑤ fail-open/真实达$5仍触顶",
      "signoff docs/test-reports/BL-113-signoff-2026-06-XX.md"
    ]
  }
]
