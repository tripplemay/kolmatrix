[
  {
    "id": "F-MIG-01",
    "title": "容器化部署基座 + 入口配置（next standalone / Dockerfile / compose+pgvector+migrate / .env 模板 / nginx / cron）",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 4.0,
    "status": "done",
    "completed_note": "2026-07-12 Generator Kimi: 9 文件落地(next.config standalone / Dockerfile base→runner+migrate / .dockerignore / docker-compose.prod.yml pgvector+redis+migrate / postgres init 00-init.sh vector+kolmatrix_app / migrate-and-sync-role.sh / .env.production.example 全量键 / nginx kolmatrix.conf kol+kolquest301 / cron 模板). tsc=0 + npm run build PASS(standalone server.js+BUILD_ID 生成, --webpack 路径验证). 未改任何 src/**.",
    "source": "spec §4 D1-D4/D6/D8, §5 F-MIG-01；用户裁决容器化 GHCR-pull + 直连拓扑",
    "acceptance": [
      "next.config.ts 加 output:'standalone'（D2）",
      "新增 Dockerfile（standalone 多阶段，app + migrate 两 target 或两镜像）+ .dockerignore",
      "新增 docker-compose.prod.yml：app(127.0.0.1:3001, HOSTNAME=0.0.0.0) + postgres(pgvector/pgvector:pg17 匹配旧机, 实操前确认 major) + redis:7-alpine + 一次性 migrate 服务；pg/redis 不发布 host 端口；命名卷；env_file .env",
      "migrate 路径（D4/H1/H3/H4）：pg init 建 kolmatrix_app role + DB kolmatrix；migrate 服务 CREATE EXTENSION vector + prisma migrate deploy + ALTER ROLE kolmatrix_app WITH PASSWORD（从 env）",
      "新增 .env.production.example（deploysvr 版，仅键名占位禁真值）：DATABASE_URL(postgres:5432)/REDIS_URL(redis:6379/1)/NEXTAUTH_SECRET/AUTH_SECRET/KOLMATRIX_APP_PASSWORD/AIGCGATEWAY_BASE_URL/AIGCGATEWAY_API_KEY/RESEND_API_KEY/APIFY_KOL_BASE_URL/APIFY_KOL_BUSINESS_API_KEY/YOUTUBE_API_KEY/全部 AIGCGATEWAY_*_ACTION_ID/NEXTAUTH_URL",
      "新增 deploy/nginx/kolmatrix.conf：kol.guangai.ai 公网 80/443 反代 127.0.0.1:3001 + ws upgrade + 安全头 + kolquest.com 301 块",
      "cron 容器化（D8）：kolmatrix-kpi-snapshot + backup-retention 改 host cron 调 docker compose exec，脚本/文档就绪",
      "不改任何 src/** 产品代码；npx tsc --noEmit + npm run build PASS；独立 commit"
    ]
  },
  {
    "id": "F-MIG-02",
    "title": "CI/CD 管道改造 — 镜像构建推 GHCR + deploy-prod 改写为 pull+compose",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 2.5,
    "status": "done",
    "completed_note": "2026-07-12 Generator Kimi: build-push.yml(GHCR app+migrate 两 target)+deploy-prod.yml 改写 docker compose pull/up+staging/seed 标 deprecated. build-push CI 首跑失败(next build collect-page-data 读 DATABASE_URL, Dockerfile 缺 build-time 占位)→fix commit 2505fc2 补 DATABASE_URL/NEXTAUTH_SECRET/NEXTAUTH_URL 占位→本地 docker build runner PASS→build-push CI 绿(镜像入 ghcr.io/tripplemay/kolmatrix/{app,migrate}:sha+latest). deploy-prod 首跑待 F-MIG-04 P4(新 secrets 就位后).",
    "source": "spec §4 D5, §5 F-MIG-02",
    "acceptance": [
      "新增 build-push workflow：main push → docker build app + migrate → 推 ghcr.io/tripplemay/kolmatrix/{app,migrate}:<git-sha>+:latest（permissions packages:write, 登录 GHCR）",
      "改写 .github/workflows/deploy-prod.yml：workflow_dispatch 保留 → SSH deploysvr → cd /opt/apps/kolmatrix → 设 IMAGE_TAG → docker compose pull → migrate → up -d → 健康检查 curl -sf http://127.0.0.1:3001/api/health（重试）",
      "文档化 GitHub secrets 更新清单：PROD_HOST=194.238.26.173 / PROD_USER=root / PROD_SSH_KEY(deploysvr 私钥)，标注旧值 34.180.93.185 作回滚",
      "deploy-staging.yml / seed-prod.yml 标注 deprecated（决策 A 弃 staging）",
      "ci.yml 保持不回归（lint+tsc+vitest+build）；独立 commit"
    ]
  },
  {
    "id": "F-MIG-03",
    "title": "apify-kol-service 随迁 deploysvr（修 crash-loop + 迁 docker 栈含 pg:15432）",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 3.0,
    "status": "deferred",
    "deferred_note": "2026-07-12 用户决策: 推迟到 KOLMatrix 割接后处理. 诊断: apify-kol-service-postgres-1 Exited 3 周前→service crash-loop, apify 实际已坏 3 周(KOL 日更本就停滞, 非迁移致). 不阻塞割接(app 无它也起, KOLMatrix .env APIFY_KOL_BASE_URL 暂留旧 localhost:3004 容器内不可达=采集停, 与现状同). ⚠️必须在 P6 旧机退役前完成(否则 apify 永久消失). 届时: deploysvr 重建 apify 栈(含自带 pg)+接 shared docker network+改 KOLMatrix APIFY_KOL_BASE_URL 为容器名.",
    "source": "spec §4 D9, §5 F-MIG-03；用户决策 B",
    "acceptance": [
      "诊断并修复旧机 apify-kol-service 现有 crash-loop（Restarting）根因",
      "把 apify-kol-service docker 栈（含自带 postgres:15432）迁到 deploysvr；.env(TIKHUB/APIFY/BUSINESS/ADMIN token 从旧机原样迁, 禁入仓)",
      "host 端口暴露 3004:3003 供 KOLMatrix 内网 localhost:3004 调用",
      "验证 curl http://localhost:3004/health 200 + 一次 read GET /kol 正常",
      "KOLMatrix 侧无代码改动；实操记录写入 migration runbook",
      "须在 KOLMatrix prod 割接（F-MIG-04 P3/P4）之前完成（H7 先行）"
    ]
  },
  {
    "id": "F-MIG-04",
    "title": "迁移 runbook + 生产割接实操（P0-P6，受监督，不可逆门禁 go/no-go）",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 4.0,
    "status": "done",
    "progress_note": "2026-07-13 P0-P4 割接完成(用户 go). P0: clone+.env 构造. P2: 起栈+灌快照演练+冒烟全绿, 捕获并修 2 bug(Dockerfile build-time DATABASE_URL / migrate db execute --url). P3(🔴 用户 go): 停旧机(23:26:17Z)+终态 pg_dump+clean restore+12 表逐行 parity 一致(零丢失, migrate 应用 pending bl115 lead 表 41→42). P4(🔴 用户 go): certbot 预签 kol.guangai.ai(kolquest 推迟-CF token 不覆盖其 zone)+装 nginx vhost+Cloudflare A 34.180.93.185→194.238.26.173(proxied=False/TTL60)+更新 GitHub secrets+装 cron(CRON_TZ=UTC, kpi-snapshot 实测 2/2). 公网 https://kol.guangai.ai 200 healthy+5 locale+TLS 有效. ⚠️停机 ~32-37min 超预估(根因 终态 dump 经本机管道慢, 教训入 runbook). runbook 实测记录已回填. P5 观察期中(旧机 STOPPED+DB 冻结作回滚), P6 旧机退役待用户验收.",
    "source": "spec §4 D7/D10/D11, §5 F-MIG-04, §7 回滚；对标 aigc deploysvr-migration-runbook.md",
    "acceptance": [
      "新增 docs/ops/deploysvr-kol-migration-runbook.md：P0 准备(建 /opt/apps/kolmatrix + .env 600 + certbot DNS-01 复用) / P2 起新栈(compose up + 灌旧库快照演练 + loopback 冒烟 /api/health+登录+AI定制+KOL流程) / P3 数据终态同步(停旧写→pg_dump kolmatrix→clean restore→逐表行数 parity；Redis 弃旧起新) / P4 边缘割接(Certbot DNS-01 预签 kol+kolquest→装 nginx→Cloudflare A 切→更新 GitHub secrets) / P5 观察期+回滚就绪 / P6 退役门禁(旧 VM 整机退役)",
      "按 runbook 执行 P0-P5 受监督实操",
      "H6 三个不可逆门禁(数据终态同步/DNS 切换/旧机停写)执行前取得用户显式 go/no-go",
      "H1/H2 secrets(kolmatrix_app 密码/NEXTAUTH/AUTH)从旧机原样迁移，禁写入仓库",
      "割接完成回写 runbook 实测 live state/parity/rollback controls"
    ]
  },
  {
    "id": "F-MIG-05",
    "title": "Codex 验收 — 新栈冒烟 + 数据 parity + RLS 隔离 + 直连可达 + 回滚演练 + signoff",
    "executor": "codex",
    "priority": "high",
    "estimated_hours": 1.5,
    "status": "pending",
    "source": "spec §5 F-MIG-05, §6 验收标准",
    "acceptance": [
      "前置：F-MIG-01~04 完成、新栈已割接",
      "冒烟：https://kol.guangai.ai 公网 → /api/health(db/redis ok) + 登录(prod 账号) + AI 邮件定制(验公网 aigcgateway) + 一条 KOL 流程(discovery/detail, 验 apify localhost:3004) + 发一封邮件(验 Resend+BullMQ)",
      "数据 parity：新库关键表行数与旧库一致；抽查 RLS 多租户隔离生效(kolmatrix_app role)",
      "直连可达 + TLS 有效 + 5 locale 抽查",
      "回滚演练可行性(旧机可快速拉起 + 旧 DNS/PROD_HOST 回退路径成立，不真正回滚)",
      "输出 docs/test-reports/BL-PROD-MIGRATE-DEPLOYSVR-signoff-YYYY-MM-DD.md 含命令/日志证据 + PASS/FAIL"
    ]
  }
]
