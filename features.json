[
  {
    "id": "F001",
    "title": "flaky 网络测试隔离 + glyph 覆盖 guard (BL-054, kolmatrix 测试/CI)",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 3.0,
    "status": "done",
    "acceptance": [
      "方向 A: tests/integration/pre-commit-hook.test.ts woff2 网络型用例隔离 — 默认 test:integration 排除 + 新增 test:integration:network 串行 config + CI 独立 job(允许 retry 2). 根因=拉 Google Fonts subset 并发抖动",
      "方向 D: tests/integration/material-symbols-coverage.test.ts 字节级比对 guard 改 glyph-coverage 断言(验 subset 含全部 icon 字形, 而非字节相等), 根治 Google Fonts 上游 build 漂移脆弱",
      "F005-grep: 全仓搜其他网络依赖型 integration test 一并隔离(v0.9.14 完整模式 grep 防漏)",
      "docs/dev/ 加 testing-strategy 段说明网络型 integration test 隔离策略",
      "L1 PASS: lint 0err warn≤3 / tsc=0 / npm test; CI 默认套件连跑不再因网络/上游漂移偶红"
    ],
    "note": "完成。根因: 仅 2 个 integration 用例打不可控外部端点(Google Fonts via 重生脚本)并发抖动。方向D根治: 脚本加 DISCOVER_ONLY + coverage #7 改 fontkit ligature 字形覆盖(网络无关+免疫字节漂移, 顺带抓修 discovery 假阳性 children); coverage 整套件网络无关。方向A隔离: pre-commit-hook 2 真fetch用例拆 *.network.test.ts + 共享 fixture, 默认 config exclude, 新 network config(串行+retry2), test:integration:network script, ci.yml 独立 job(continue-on-error)。F005-grep 全仓确认仅这两类。docs/dev/testing.md §5.4。验证: 默认11用例2s绿/.network 2用例绿/tsc0/lint0。commit d359f8b 已推。fontkit devDep。"
  },
  {
    "id": "F002",
    "title": "apify_cost_usd 成本记账 (BL-090-cost, 价格表 或 usage-delta, 路径 B)",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 2.0,
    "status": "pr_open",
    "acceptance": [
      "TikHub 不返回每请求成本(BL-042 实证)→ 用端点价格表(per TikHub 端点单价 × 调用次数)或 usage-delta(定期抓账户用量差额)估算, 回填 scrape_jobs.apify_cost_usd(现全 0)",
      "配合 BL-086 F004 余额告警, 让'预算→KOL'换算有数据支撑; yt-email 已有 unitCostUsd=0.12 可参考",
      "爬虫代码改动走路径 B: PR → guang-tech/apify → merge → fork-sync /opt rebuild(谨慎, OOM 用 NODE_OPTIONS=4096)",
      "含单测; 不破坏既有 scrape 流程",
      "验证: 新 scrape_jobs 行 apify_cost_usd 写入非 0"
    ],
    "note": "代码完成 + 单测(sdk cost.test 5)。B 方案: sdk core/cost.ts(ENDPOINT_PRICES 估算价格表 + AsyncLocalStorage 按 job 归集 + priceForPath/runWithCostTracking/currentCostUsd) + tikhub-client handleResponse 成功分支 recordRequestCost + scrape-worker dispatch 包 runWithCostTracking 写 apifyCostUsd。PR #10 → guang-tech/apify(base 4d102f1)。sdk 54 + service 120 绿, tsc 0。⚠️ 价格是估算(runbook Apify 期折算, 非真实 TikHub 单价)→ ENDPOINT_PRICES 是唯一校准点, 建议爬虫团队用真实账单校准后再 merge。⏳ 路径 B 待 merge + fork-sync /opt rebuild。归档 docs/upstream-patches/BL-094-F002-apify-cost-accounting.md"
  },
  {
    "id": "F003",
    "title": "BL-088 量化 (只读分析: 806 回收价值 + 2584 硬删风险 → 决策报告)",
    "executor": "codex",
    "priority": "medium",
    "estimated_hours": 1.0,
    "status": "pending",
    "acceptance": [
      "只读 prod(kolmatrix Kol 表 + 上游)量化 806 质量门拦截的可回收价值: 按 平台 × 粉丝段 × 是否有邮箱 拆; 尤其 ≥1万粉 / 有邮箱占比(判断量换质是否值得)",
      "2584 软删旧源硬删风险评估: (tenant,platform,handle)/(tenant,platform,external_id) 非 partial 唯一索引撞键风险 + 外键/依赖检查 + 是否已被 BL-091 收割消费(避免丢种子)",
      "产出报告 docs/test-reports/BL-094-F003-bl088-quantify-2026-06-XX.md + 建议(是否放宽/放宽到什么档 / 是否硬删)",
      "⚠️ 只读, 不实装放宽/硬删 — 留 follow-up 待用户据报告决定"
    ]
  },
  {
    "id": "F004",
    "title": "Codex L1+L2 + signoff (executor:codex)",
    "executor": "codex",
    "priority": "high",
    "estimated_hours": 1.0,
    "status": "pending",
    "acceptance": [
      "L1: F001 lint/tsc/test 绿(含隔离后默认套件稳定); F002 PR merge+sync + 脚本单测绿",
      "L2: F001 CI 默认套件连跑不再偶红 + 网络型 job 独立; F002 部署后 scrape_jobs.apify_cost_usd 开始写入非 0",
      "F003 量化报告结论纳入 signoff(BL-088 放宽/硬删决策待办标注)",
      "signoff doc docs/test-reports/BL-094-signoff-2026-06-XX.md"
    ]
  }
]