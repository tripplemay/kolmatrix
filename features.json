[
  {
    "id": "F001",
    "title": "Material Symbols 8 漏 ligature 加 manifest + 重生 woff2（Issue #1 快修）",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 0.5,
    "status": "completed",
    "acceptance": [
      "scripts/material-symbols-icons-manifest.txt 追加 8 行: forward_to_inbox / refresh / article / attach_money / error_outline / hourglass_empty / mark_email_unread / verified_user (含 file:line + 'bare multi-line span' label)",
      "运行 bash scripts/regenerate-material-symbols-subset.sh 重生 src/app/fonts/material-symbols-outlined.woff2",
      "woff2 size 略增 (~11008B → ~11800B 加 8 glyphs); ICON_COUNT 增 8",
      "commit 同 commit 含 manifest 8 行 + woff2 文件",
      "staging 实测 /campaigns/[id]: 'forward_to_inbox' (启动触达) + 'refresh' (换一批) icon glyph 正确渲染 (不再字面文字); 同时 /reach + /insight/weekly-report 区域使用其他 6 漏 icon 正常显示"
    ]
  },
  {
    "id": "F002",
    "title": "regenerate script Pattern 7 — bare ligature in multi-line span grep + 排除清单升级",
    "executor": "generator",
    "priority": "medium",
    "estimated_hours": 1.5,
    "status": "completed",
    "acceptance": [
      "scripts/regenerate-material-symbols-subset.sh 加 Pattern 7: 匹配 material-symbols-outlined -A 5 上下文内独立成行的 bare ligature (排除关键字 / prop name / closing tag / 注释)",
      "Pattern 7 false-positive 排除清单与 Pattern 6 同步增量 (单一清单 not 分叉)",
      "Pattern 7 跑后自然发现 8 漏 ligature (即便 manifest 临时去掉这 8 行 Pattern 7 仍命中)",
      "script 跑后 ICON_COUNT 不 regress (与原 quoted-only 输出比对 ≥ 100)",
      "framework/harness/checklists/material-symbols-pattern.md 更新 §Pattern 6→7 进化路径 + 维护惯例 (≥20 LOC 新)",
      "scope tag 保 'scope: project-specific'"
    ]
  },
  {
    "id": "F003",
    "title": "BriefPageClient.tsx max-w-3xl 删除 + 全仓嵌套 max-w grep 防御（Issue #2 快修）",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 0.5,
    "status": "completed",
    "acceptance": [
      "src/app/[locale]/(app)/brief/BriefPageClient.tsx:120 改 <div className='mx-auto max-w-3xl space-y-6'> → <div className='space-y-6'>",
      "staging 实测 /brief?tab=campaign 和 /brief?tab=products 在宽屏 (1600+) 渲染时 form 区域与 tab nav 同宽对齐 4 路由",
      "CampaignForm 内 grid (md:grid-cols-2 / md:grid-cols-4) 在 ≥768px 自然展开多列",
      "若单 field 行 (如 '活动名称（可选）') 变得过长不易读, 给该 field 父容器加 max-w-2xl 局部约束 (而非全 form max-w)",
      "grep 全仓嵌套 max-w 防御: `grep -rn 'max-w-' src/app/\\[locale\\]/\\(app\\)/{brief,match,reach,insight}/ --include='*.tsx'` 输出 review, 确认无意外嵌套 max-w 二级约束破坏外层意图",
      "L1 PASS (lint + tsc + vitest)"
    ]
  },
  {
    "id": "F004",
    "title": "i18n match.emptyState.body 补 5 locale + 区分两态文案 + 扫其他 MISSING_MESSAGE",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 1.5,
    "status": "pending",
    "acceptance": [
      "messages/{zh,en,ja,ko,es}.json match.emptyState 加 body + bodyDefault 两 key (5 locale 完整翻译; body = 'filter 命中空 - 请尝试调整筛选条件', bodyDefault = '默认空 - KOL 库正在同步中')",
      "src/app/[locale]/(app)/match/page.tsx 区分 hasActiveFilters (filters.search || filters.regions.length || filters.platforms.length || filters.languages.length || filters.categories.length 等) → 选 t('body') 或 t('bodyDefault')",
      "孤儿 tipHeading + tipBody key 5 locale 删 (grep src/ 全仓确认 0 caller)",
      "SSH prod log 扫近 7 天 MISSING_MESSAGE 全量 + 本地 grep src/ 的 t() 调用对比 messages key existence, 输出待修 key 清单 (含 weeklyReport.title 等), 同 commit 补全 5 locale",
      "tests/unit/i18n-locale-coverage.test.ts 8/8 PASS",
      "staging 实测 /zh/match 默认空 + filter 命中空两种情况文案都正确中文显示"
    ]
  },
  {
    "id": "F005",
    "title": "i18n-page-side-consumption test v2 — 加 page.tsx 调用 t(key) 实际 exist in messages 检测",
    "executor": "generator",
    "priority": "medium",
    "estimated_hours": 1.5,
    "status": "pending",
    "acceptance": [
      "tests/unit/i18n-page-side-consumption.test.ts 扩展: 现有 raw English literal scan 保留 (advisory); 新加 key existence check",
      "扫所有 page.tsx + *Client.tsx + *Panel.tsx + *Bar.tsx 的 t('<key>') / t(\"<key>\") 调用",
      "拼 namespace (getTranslations('match.emptyState') + t('body') → 'match.emptyState.body') 验该 key 在 messages/en.json 实际 exist (en 为 source of truth)",
      "不 exist → fail test 列 file:line + missing key (第一版 advisory STRICT_MODE=false, 仅 warning)",
      "BL-073-F004 修复后跑 test 0 fail (advisory)",
      "CI ci.yml 含该 test job",
      "framework/harness/evaluator.md 更新 §「i18n page-side test 模式」加 v2 key existence (与 v0.9.24 sediment 候选 #3 配对)"
    ]
  },
  {
    "id": "F006",
    "title": "filter UX 防御 — country/language 数据 NULL 时 UI disable + 提示 (no data)",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 2.5,
    "status": "pending",
    "acceptance": [
      "src/lib/kol/filters.ts 加 getDataCoverage(tx, tenantId): Promise<{ regions: number, languages: number, platforms: number, categories: number, etc. }> — 返每维度有数据的 distinct values count (非 null + 非空字符串)",
      "src/app/[locale]/(app)/match/page.tsx server-side 调用 getDataCoverage + 传 coverage prop 给 MatchFilterSidebar",
      "MatchFilterSidebar.tsx 对 coverage[dim] === 0 维度 UI disabled (按钮 / 选项灰显, aria-disabled, pointer-events-none) + 显示 'no data' 提示文案 (5 locale 翻译, 用 t('match.filterSidebar.noData') 或类似 key)",
      "src/app/[locale]/(app)/match/search.ts runMatchSearch 加 early-return: 若 filter 包含 coverage=0 维度 → 直接返 { items: [], total: 0, hasMore: false } 不发 SQL (省去无意义 query)",
      "单元测试 ≥4 case: coverage 0 / coverage >0 / 选了 disabled filter / early-return",
      "staging 实测: /zh/match filter sidebar country 选项灰显 + 显示 '(暂无数据)' 提示文案; 用户即使强行通过 URL ?regions=USA 进入 → page 服务端识别 + emptyState 显示 bodyDefault (而非 body 'filter 命中空' 误导)"
    ]
  },
  {
    "id": "F007",
    "title": "material-symbols-coverage test v2 + STRICT_MS_ICONS flip 升级",
    "executor": "generator",
    "priority": "medium",
    "estimated_hours": 1.0,
    "status": "pending",
    "acceptance": [
      "tests/unit/material-symbols-coverage-unit.test.ts 加 Pattern 7 bare ligature detection (与 F002 同步规则)",
      "STRICT_MODE 拆分: STRICT_MS_ICONS (新, 默认 true) + STRICT_I18N (false, 不动) + STRICT_LINK_TARGET (false, 不动)",
      "Material Symbols 维度 flip strict: 若 src/ 提及 ligature 不在 manifest 也不被 Pattern 7 命中 → CI fail",
      "BL-073-F001 修复后跑 test PASS (STRICT_MS_ICONS=true)",
      "framework/harness/checklists/material-symbols-pattern.md 加 §STRICT_MS_ICONS 启用记录 + 未来加 icon 必更 manifest 强制要求",
      "framework/CHANGELOG.md v0.9.24 候选段记 STRICT_MODE 渐进升级路径"
    ]
  },
  {
    "id": "F008",
    "title": "Reviewer L1+L2 抽样验证 + signoff（executor:codex）",
    "executor": "codex",
    "priority": "high",
    "estimated_hours": 3.0,
    "status": "pending",
    "acceptance": [
      "L1 自动化 6 项 PASS: (1) npm run lint 0 error warning≤3 (2) npx tsc --noEmit (3) npm test 含 F005/F007 v2 (4) grep manifest 8 漏 ligature 全命中 (5) woff2 size 增 ~800B (6) messages/zh.json match.emptyState.body 存在",
      "L2 staging 抽样实测 6 项 PASS: (1) /zh/campaigns/[id] 8 icon 正确渲染 (2) /zh/brief form 与 tab nav 同宽 1600px (3) /zh/match emptyState 中文文案非字面 key (4) /zh/match filter sidebar country/language 灰显 (5) /zh/match 默认进入显示 20 KOL 卡片 (6) CI Material Symbols strict 验证拦截能力 (临时 PR 加一个 unknown icon 验 CI 红)",
      "0 broken cross-reference / 0 hardcoded English (除 brand) / 0 字面 ligature / 0 stale link",
      "signoff doc docs/test-reports/BL-073-signoff-2026-05-XX.md 含 L1/L2 结果 + sample 引用 + 终签"
    ]
  }
]
