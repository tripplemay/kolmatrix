[
  {
    "id": "F001",
    "title": "aigcgateway 2 action 注册 + run-action.ts SDK 统一抽象层（per F001 audit §6:A）",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 6.0,
    "status": "done",
    "done_commit": "dd7870c",
    "done_at": "2026-05-15T14:15+0800",
    "actions_registered": {
      "short": "cmp6ifb5w0035bnrrljflmtcn",
      "detailed": "cmp6ihdt109jebnrqdj215aft"
    },
    "acceptance": [
      "创建 `kol-recommendation-explain-short` action via MCP `create_action`：model=`claude-haiku-4-5` / variables=`kol_json` `campaign_json` `value_score_breakdown_json` `locales_json` / response_format=`json_object` / prompt（system，中文骨架）要求每个 locale 生成 ≤80 字一句话解释（命中 follower/engagement/category/内容质量 4 维度信号）→ 输出 JSON `{ en: '...', zh: '...', ja: '...', ko: '...', es: '...' }`",
      "创建 `kol-recommendation-explain-detailed` action：同模型 / 同变量结构 / response_format=`json_object` / prompt 要求输出 5 段结构化 JSON 每段 ≤200 字 per locale → 顶层 JSON `{ en: { matchScore, categoryFit, recentActivity, audienceFit, brandHistory }, zh: {...}, ja: {...}, ko: {...}, es: {...} }`",
      "两 action ID 落 `.env.staging` + `.env.production` 变量 `AIGCGATEWAY_EXPLAIN_SHORT_ACTION_ID` 和 `AIGCGATEWAY_EXPLAIN_DETAILED_ACTION_ID`（SSH ops 落地，参考 B5-F004 模式）",
      "两 action 走 MCP `run_action` dry_run 验证 prompt 渲染正确 + token 估算（实测 ceiling：short ≤2000 input + ≤800 output；detailed ≤2000 input + ≤3500 output；如 dry_run 超 ceiling 则裁 input 字段或更严限定字数）",
      "F001 commit 含 prompt 原文（中文系统提示）+ 输入 schema 文档化落 `docs/specs/BL-067-F001-prompt-design.md`",
      "**新建 `src/lib/aigc/run-action.ts` 统一 SDK 抽象层**（per F001 audit §6:A 裁决）：`runAigcAction<T>(opts: { actionId, variables, tenantId, actionLabel, timeoutMs? }): Promise<{ output: T; usage: { totalTokens, costUsd }; traceId }>` 内部 `assertDailyCostBudget` → POST `/actions/run` → `parseFencedJson` → `recordAiUsage` → typed return；复用现 `fetchWithRetry` / `xml-escape` / `parseFencedJson` 三个 BL-034/BL-035 基础设施；单测 ≥4 case (success / cost-cap throw / aigcgateway 5xx error / JSON 解析失败)；**约束**：不动 customize.ts / topic-cloud.ts 现有 caller（向后兼容），仅 BL-067 新 caller 用新抽象",
      "L1 PASS（lint + tsc + vitest）",
      "staging git_sha 与本 commit 一致"
    ]
  },
  {
    "id": "F002",
    "title": "asset 缓存层 + checkLlmCostBudget boolean 包装 + cleanup cron（per F001 audit §1:A §5:A）",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 8.5,
    "status": "done",
    "done_commit": "a39087c",
    "done_at": "2026-05-15T14:45+0800",
    "migration": "20260515065000_bl067_f002_explanation_asset_types",
    "acceptance": [
      "Migration `prisma/migrations/20260516XXXXXX_bl067_f002_explanation_asset_types/migration.sql`：加 enum 值 `ai_recommendation_explanation_short` / `ai_recommendation_explanation_detailed` 到 `AssetType` enum（**起工前 grep schema.prisma 确认现有 enum 值仅 `email` + `video_script`**）+ DOWN rollback 注释（per `scripts/validate-rollback-sql.sh` 铁律）+ schema.prisma 同步加 enum",
      "新文件 `src/lib/explainability/cache.ts` 封装：`readShortExplanation(tenantId, campaignId, kolId, locale): Promise<string | null>` 查 asset 表 type=short + createdAt > now-24h；`writeShortExplanation(tenantId, campaignId, kolId, locale, content): Promise<void>` 写一行一 locale（payload 含 short 文本 + metadata { kolId, campaignId, generatedAt, tokenUsage, costUsd }）；`readDetailedExplanation(...)` 返回 5 段结构对象；`writeDetailedExplanation(...)` 同模式",
      "**cost cap 复用（per F001 audit §1:A 裁决修订）**：在现有 `src/lib/ai/cost-cap.ts`（BL-034 F005，**非** spec 原写的 `src/lib/cost-cap/check.ts`）加 ≤15 LOC 包装函数 `checkLlmCostBudget(tenantId): Promise<{ allowed: boolean }>` — 复用同一 count 查询逻辑，`assertDailyCostBudget` 不动保 customize.ts / topic-cloud.ts 向后兼容；BL-067 4-5 处新 caller 用 boolean API 减 try/catch 噪音",
      "**Cleanup cron（per F001 audit §5:A 裁决修订）**：F002 同 commit 新建 `scripts/cleanup-expired-explanation-assets.ts`（仅删 type IN (ai_recommendation_explanation_short, ai_recommendation_explanation_detailed) + createdAt < now - 24h 行）+ GitHub Actions workflow `.github/workflows/cron-cleanup-explanation-assets.yml`（**Generator 起工前必 grep `scripts/kol-sync-daily.ts` 或 `.github/workflows/cron-*.yml` 确认 daily sync schedule，cleanup cron 选不冲突时段 — 推荐 06:30 BJT 或 14:30 BJT，避开 04:00-06:00 cron 静默窗**）",
      "单测 ≥6 case：read hit / read miss / read expired (>24h) / write success / write RLS denied / TTL boundary (exactly 24h - 1ms)",
      "L1 PASS（lint + tsc + vitest）",
      "staging git_sha 与本 commit 一致"
    ]
  },
  {
    "id": "F003",
    "title": "AiRecommendationPanel C2 → C3 升级 + `?` icon 入口",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 4.0,
    "status": "done",
    "done_commit": "cdebf38",
    "done_at": "2026-05-15T15:00+0800",
    "acceptance": [
      "升级 BL-066 F003 中的 `_explanation_placeholder` 占位段：mount 时 batch 调 `Promise.all(top30.map(kol => readShortExplanation(tenantId, campaignId, kol.id, locale)))` → 渲染 per kol：HIT → 显 LLM short / MISS → 显 C2 fallback 文案（`cosine match {matchScore} | valueScore {valueScore}` 保持现状）",
      "每个 KOL 卡片右上加 `help_outline` Material Symbols icon：`data-testid='explain-trigger-{kolId}'` + aria-label 取自 i18n key `campaigns.detail.explainability.queryButtonLabel` + 点击打开 `DetailedExplanationDialog`（F004）",
      "不在 F003 内主动触发生成（生成在 F005 队列侧）",
      "单测 ≥4 case：mount batch read 调用 / hit 显 LLM short / miss 显 C2 fallback / icon 渲染锁（aria-label + testid）",
      "不破坏 BL-066 F003 现有 e2e（accept / skip / show-next / 三段 layout）",
      "L1 PASS",
      "staging git_sha 与本 commit 一致"
    ]
  },
  {
    "id": "F004",
    "title": "DetailedExplanationDialog UI + 详细版 server action + cost cap fallback",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 8.0,
    "status": "pending",
    "acceptance": [
      "新组件 `src/app/[locale]/(app)/campaigns/[id]/DetailedExplanationDialog.tsx`（\"use client\"）：Props `{ kolId, campaignId, kol, valueScore, open, onClose }` / 首次打开调 server action `requestDetailedExplanationAction({ campaignId, kolId, locale })` / Loading skeleton 渲染 5 段骨架 / 5s 超时 fallback 显 `campaigns.detail.explainability.unavailable` 文案 + 关闭按钮 / 渲染 5 段结构（标题 i18n + LLM 内容）：matchScore / categoryFit / recentActivity / audienceFit / brandHistory",
      "新文件 `src/app/[locale]/(app)/campaigns/[id]/explainability-actions.ts`（\"use server\"）含 `requestDetailedExplanationAction({ campaignId, kolId, locale }): Promise<{ segments | null; fallbackToC2: boolean }>` 流程：（1）查 cache → HIT 返回；（2）MISS → `checkLlmCostCap(tenantId)` → 满 → 返回 `{ segments: null, fallbackToC2: true }`；（3）未满 → 拼 input 调 aigcgateway `run_action(AIGCGATEWAY_EXPLAIN_DETAILED_ACTION_ID, input)` → 解析 JSON → `writeDetailedExplanation` per locale → 返回当前 locale segments",
      "audit_log：`logAudit({ actorId, action: 'ai_recommendation.explain_detailed_generated' or '_served_from_cache' or '_cap_exhausted', targetType: 'kol_campaign', targetId: '${campaignId}:${kolId}', tenantId, after: { locale, tokenUsage?, costUsd?, segmentCount } })`",
      "Rate limit 沿用 `rateLimitBatchSend` (20/min/user)",
      "单测 ≥5 case：dialog 首次打开 cache miss 调 LLM / cache hit 不调 LLM / cap 满返回 fallback / LLM error → 5s timeout 文案 / 5 locale switch 各返回对应 segments",
      "L1 PASS",
      "staging git_sha 与本 commit 一致"
    ]
  },
  {
    "id": "F005",
    "title": "InMemoryJobQueue pre-warm worker (fire-and-forget delay:1) + AiRecommendationPanel mount trigger（per F001 audit §3:A §4:B §6:A）",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 12.0,
    "status": "pending",
    "acceptance": [
      "新 worker `src/lib/queue/explain-recommendations-worker.ts` 处理 job type `recommendation-explain-prewarm`（**InMemoryJobQueue 模式 per F001 audit §4:B**）：注册 worker handler via 现有 `src/lib/jobs/queue.ts` 的 `register(name, handler)` API；Payload `{ tenantId, campaignId, kolIds[] }`；idempotencyKey `prewarm-{tenantId}-{campaignId}` 同进程内幂等（进程重启后 mount self-heal 自然重新 enqueue）；handler 内手写 1 次 retry（仅网络错误 retry，JSON 解析失败不 retry）",
      "Worker 主循环：for each kolId → `checkLlmCostBudget(tenantId)` `{ allowed: false }` → break / 查 5 locale 全 hit 则 skip / **inline import `computeKolValueScore from '@/lib/kol/value-score'`**（per F001 audit §3:A）拿 breakdown.{follower, engagement, category} / 拼 input { kol, campaign, valueScoreBreakdown, locales[5] } → **走 F001 新 `runAigcAction({ actionId, variables, tenantId, actionLabel })`**（per F001 audit §6:A，**非** inline POST）→ 解析 5 locale JSON → for each locale writeShortExplanation → audit_log `ai_recommendation.explain_short_generated` payload { kolId, locales: 5, tokenUsage, costUsd }",
      "新文件 `src/app/[locale]/(app)/campaigns/[id]/prewarm-actions.ts`（\"use server\"）含 `enqueueExplanationPrewarmAction({ campaignId, kolIds }): Promise<void>` server 端 **`void jobQueue.add('recommendation-explain-prewarm', payload, { idempotencyKey, delay: 1 })`** fire-and-forget（delay:1 让 LLM 跑入下一 tick，server action 立即 return 不阻塞 mount）",
      "AiRecommendationPanel（F003 改造的延续）mount + smart-match 返回 top30 后调 `enqueueExplanationPrewarmAction({ campaignId, kolIds: top30.map(k => k.id) })`；server 端 enqueue 幂等：同 idempotencyKey 已存在 → noop（per InMemoryJobQueue 行为）；用户进出页面多次不会触发重复 LLM 调用（同进程内）",
      "单测 ≥5 case：success / cap 满静默 break / LLM error skip 当前 KOL / 幂等 idempotencyKey / 空 kolIds noop",
      "L1 PASS",
      "staging git_sha 与本 commit 一致",
      "**Dogfood 期监测**：BL-067 dogfood 期间若 PM2 reload 频次 > 2 次/day 或用户报 short explanation 频繁 miss → BL-067 done 评估升级 BullMQ（fix-round 或下批次）"
    ]
  },
  {
    "id": "F006",
    "title": "5 语言 i18n + e2e campaign-explainability-flow.spec.ts 6 case",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 8.0,
    "status": "pending",
    "acceptance": [
      "`messages/{en,zh,ja,ko,es}.json` 加 `campaigns.detail.explainability.*` 完整 keys：`queryButtonLabel` / `dialogTitle` (带 KOL handle 占位) / `loading` / `unavailable` (5s timeout fallback) / `capExhaustedToast` (仅 F004 dialog 路径 cap 满才显，pre-warm 静默) / `segments.matchScore.title` / `segments.categoryFit.title` / `segments.recentActivity.title` / `segments.audienceFit.title` / `segments.brandHistory.title` / `c2Fallback` (BL-066 现 C2 文案集中到 i18n)",
      "5 语言全 cover 不留 `_deprecated_by_*` marker（新 keys）",
      "新 `tests/e2e/campaign-explainability-flow.spec.ts` 6 case：(1) panel mount → 等 short 渲染 (pre-warm 完成后 reload 验 hit) / (2) cache miss → 显 C2 fallback / (3) `?` icon click → DetailedExplanationDialog 打开 → 5 段渲染 / (4) 同 KOL dialog 二次打开 → cache hit (no LLM call via network mock) / (5) cap 满 → dialog 打开显 fallback 文案 + capExhaustedToast / (6) locale switch (en → zh) → 同 KOL short/detailed 显对应 locale 内容",
      "L1 PASS（lint + tsc + vitest + e2e）",
      "staging git_sha 与本 commit 一致"
    ]
  },
  {
    "id": "F007",
    "title": "staging deploy + 视觉 baseline + 24h cost 监控 + signoff prep",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 4.0,
    "status": "pending",
    "acceptance": [
      "staging deploy via `deploy-staging.yml`（含 `AIGCGATEWAY_EXPLAIN_SHORT_ACTION_ID` + `AIGCGATEWAY_EXPLAIN_DETAILED_ACTION_ID` 两个新 env var via SSH 落 `.env.staging`，与 prod 配齐 5 处 sync 协议风格）",
      "视觉 baseline regen via `update-visual-baselines` workflow：`en-campaign-detail.png` 必新生成（含 short explanation 显示态 + `?` icon）+ `en-campaign-detail-detailed-dialog.png` 新增（Dialog 打开态）",
      "Planner 在 building 后期出 staging dogfood 清单：≥5 个不同游戏品类 campaign 实测 short 渲染质量 / ≥3 个 detailed dialog 实测 5 段质量 / 5 locale 切换 spot check / cap 满模拟（mock $5 cost）测 silent fallback，commit 落 `docs/test-reports/BL-067-staging-spot-check.md`",
      "24h aigcgateway dashboard cost 监控：daily total cost ≤ 团队 dogfood 实际 cost × 1.5（防 prompt 偏调用 token 暴增）",
      "`scripts/bl067-cost-audit.ts` 拉 audit_log type='ai_recommendation.explain_*' 24h 累计 cost / token / call 数报告",
      "`docs/test-reports/BL-067-signoff-2026-05-XX.md` Reviewer 写最终结论：覆盖率 100% + cost 在 cap 内 + 5 locale 全绿 + 6 e2e PASS",
      "Reviewer 复验全部 acceptance + signoff，progress.json `status: reverifying → done`"
    ]
  }
]
