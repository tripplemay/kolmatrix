[
  {
    "id": "F001",
    "title": "quick-fix detail.ts 去 isSaved filter + AddKolDialog tooltip + 5 语言 i18n（unblock 内部 dogfood）",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 0.5,
    "status": "done",
    "acceptance": [
      "src/lib/campaigns/detail.ts:209 runAvailableKolsForCampaign query: where { isSaved: true, deletedAt: null } → where { deletedAt: null }",
      "src/app/[locale]/(app)/campaigns/[id]/AddKolDialog.tsx 加 disabled 状态 tooltip 文案 + 触发按钮 hover tooltip",
      "messages/{cn,en,ja,ko,es}.json 加 campaigns.kolPanel.aiNativeMigrationTooltip key（文案：'全量 KOL 池，AI native 完整版即将上线'/EN: 'Full KOL pool — AI native experience coming soon'）",
      "L1 lint 0 / tsc 0 / unit + integration test PASS（含 BL-061 既有 6 case）",
      "staging deploy + curl /api/health git_sha 与本 commit 一致",
      "现 e2e database-fidelity.spec.ts 不受影响（F001 仅改 detail.ts 一行 + tooltip 文案）",
      "staging 实地：进 /campaigns/[id] 任一 active campaign '添加 KOL' 按钮 enabled（除非 status=completed）"
    ]
  },
  {
    "id": "F002",
    "title": "schema migration: 删除 is_saved 字段 + 数据 backup 表",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 4.0,
    "status": "done",
    "acceptance": [
      "prisma/schema.prisma Kol 模型移除 isSaved Boolean 字段",
      "prisma/migrations/202605XX_remove_is_saved_from_kol/migration.sql 新建：CREATE TEMP TABLE _bl063_is_saved_backup AS SELECT id, is_saved FROM kol WHERE is_saved=true; ALTER TABLE kol DROP COLUMN is_saved;",
      "migration 含 ROLLBACK 注释（手动 ALTER ADD COLUMN + 从 backup 恢复）",
      "npx prisma migrate dev local 跑通",
      "npx prisma generate 重新生成 client（commit 含自动生成的更新）",
      "L1 lint 0 / tsc 0（删字段后所有引用必须清，否则 tsc 会报错）"
    ]
  },
  {
    "id": "F003",
    "title": "移除 9+ 处 query 的 isSaved filter 引用",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 6.0,
    "status": "done",
    "acceptance": [
      "全仓 grep 'isSaved\\|is_saved' 仅残留在 BL-063 commit message / migration / docs 引用，src/ 路径全清",
      "src/lib/campaigns/list-kpis.ts:98 valueScoreAgg 移除 isSaved（保留 deletedAt: null）",
      "src/lib/crm/overview.ts:209 tx.kol.findMany 移除 isSaved",
      "src/app/[locale]/(app)/database/stats.ts 3 处（L26/L27-32/L33-37）移除 isSaved（整页将在 BL-064 删除，本批次先去 filter）",
      "src/lib/kol/filters.ts（如 grep 命中）移除",
      "src/app/[locale]/(app)/database/actions.ts:106 addKolAction 移除 isSaved: true 字段写入",
      "其它 grep 命中处全清",
      "TypeScript 编译通过（删字段后所有引用必须清）",
      "L1 lint 0 / tsc 0",
      "staging git_sha 与本 commit 一致（curl https://staging.kol.guangai.ai/api/health | jq .git_sha 验证）"
    ]
  },
  {
    "id": "F004",
    "title": "测试更新：移除 isSaved 相关 case + 加全量池逻辑测试",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 4.0,
    "status": "done",
    "acceptance": [
      "tests/integration/dashboard-kpi.test.ts BL-060 加的 3 case 重写为不依赖 isSaved",
      "tests/integration/database-stats.test.ts BL-060 加的 2 case 整文件标 deprecated（BL-064 删 /database 时一起删）",
      "tests/integration/kpi-snapshot.test.ts BL-060 加的 1 case 重写",
      "src/app/[locale]/(app)/database/__tests__/addKolAction.test.ts 移除 isSaved 期望",
      "加 ≥3 新 case 验证全量池：(a) runAvailableKolsForCampaign 返回所有非软删 KOL (b) addKolAction 创建 KOL 不再设 isSaved (c) schema 验证 is_saved 列不存在",
      "e2e database-fidelity.spec.ts 仅适配 isSaved 移除（如有 case 验证 isSaved=true 显示，删除）",
      "L1 全套 PASS（含新 ≥3 case）",
      "staging git_sha 与本 commit 一致"
    ]
  },
  {
    "id": "F005",
    "title": "staging dry-run + 数据 audit",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 2.5,
    "status": "pending",
    "acceptance": [
      "staging 跑 F002 migration（npx prisma migrate deploy on staging）",
      "staging SQL audit: SELECT column_name FROM information_schema.columns WHERE table_name='kol' AND column_name='is_saved' → 0 rows",
      "staging 跑 BL-061 F003 SQL 验证 engagement_rate 非 NULL 比例不退化（应仍 6.7%）",
      "staging 全量 e2e suite PASS（marketer-dashboard / database-fidelity / login-cinematic / 等）",
      "staging /campaigns/[id] 实地：'添加 KOL' 按钮 enabled + dialog 显完整 KOL 池（>0 候选）",
      "staging _bl063_is_saved_backup 表存在（含 4 行原 isSaved=true 数据）",
      "实测 output 记录在 docs/test-reports/BL-063-F005-staging-dryrun-2026-05-XX.md"
    ]
  },
  {
    "id": "F006",
    "title": "prod migration ops（用户手动触发 + 24h 监控 + signoff）",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 1.5,
    "status": "pending",
    "acceptance": [
      "prod 数据 backup（SSH sudo -u postgres pg_dump kolmatrix > /tmp/bl063-pre-migration.dump 或 SQL 内 backup table）",
      "prod redeploy（GitHub Actions UI dispatch HEAD = main，由用户手动触发）",
      "prod /api/health 返回 git_sha = main HEAD",
      "deploy script 自动跑 npx prisma migrate deploy 应用 F002 migration",
      "prod SQL audit: is_saved 列不存在 / kol 表行数无非预期变化 / engagement_rate non_null_pct 不退化",
      "prod /campaigns/[id] 实地：按钮 enabled + dialog 含全量池",
      "24h 监控：sentry / pm2 logs 无 isSaved 相关错误",
      "docs/test-reports/BL-063-signoff-2026-05-XX.md 写最终结论：staging + prod SQL output + UI 实物截图 + Phase 1 第一批完成确认",
      "Reviewer 复验全部 acceptance 后写 signoff，progress.json status verifying → done"
    ]
  }
]
