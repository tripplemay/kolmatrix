[
  {
    "id": "F001",
    "title": "爬虫: service_settings 表 + repo + admin crawler-state API (generator, guang-tech/apify)",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 2.5,
    "status": "done",
    "done_by": "Kimi_generator",
    "done_at": "2026-06-10T11:58+0800",
    "verify_note": "guang-tech/apify PR #12 commit cd6209d (branch feat/bl108-crawler-pause, 本地 clone ~/project/apify-kol). service_settings 单行表(integer pk CHECK id=1, scraping_enabled/refresh_enabled both default true, updated_at/updated_by) + migration 0005(幂等 CREATE+seed 默认行, journal 手动追加 idx5 与repo惯例一致). SettingsRepo getState(行缺失 fail-open both true)/setState(单行 upsert onConflictDoUpdate, set 只含显式传入开关, insert 兜底路径未指定开关落默认true). 路由 admin-crawler-state.ts 照搬 admin-schedules requireAdmin DI 模式: GET 返 {scrapingEnabled,refreshEnabled,updatedAt,updatedBy}; PATCH zod safeParse(至少一开关, 非法400 validation+issues 不触repo), updatedBy 可由 kolmatrix proxy 透传缺省 'admin-api'. server.ts 注册. 单测9个(settings-repo 4: 映射/缺行默认/partial upsert两向; admin-crawler-state 5: 401/403/GET装配/PATCH默认updatedBy/透传/400). L1: pnpm --filter @apify-kol/service test 131/131 绿(前置坑: workspace 包 @apify-kol/{apify,sdk} 需先 pnpm build 出 dist 否则 vitest/tsc 解析失败, 与 Dockerfile 顺序一致); tsc tsconfig.test.json --noEmit=0. lint script 引用的 eslint 该 repo 从未安装(预存, 非本批). ⏭️ F002 同 PR 追加 commit.",
    "acceptance": [
      "新建 service_settings 表(drizzle pgTable, 单行: scraping_enabled boolean default true / refresh_enabled boolean default true / updated_at / updated_by text); migration + seed 默认行(both true)",
      "SettingsRepo: getState() / setState({scraping_enabled?, refresh_enabled?, updatedBy})(单行 upsert)",
      "admin 路由(照搬 admin-schedules.ts requireAdmin 模式): GET /admin/crawler-state → {scrapingEnabled, refreshEnabled, updatedAt, updatedBy}; PATCH /admin/crawler-state {scrapingEnabled?, refreshEnabled?}(zod 校验, 写 repo + updatedBy)",
      "单测(repo upsert / API GET-PATCH / 默认值 true); 爬虫端 pnpm --filter @apify-kol/service test 绿",
      "交付: guang-tech/apify upstream patch/PR(走 BL-086-F001 流程), 不积累本地分叉; 用户 merge + 部署 /opt/apify-kol-service"
    ]
  },
  {
    "id": "F002",
    "title": "爬虫: 各入队点 gate 两层开关 (generator, guang-tech/apify)",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 2.5,
    "status": "done",
    "done_by": "Kimi_generator",
    "done_at": "2026-06-10T12:40+0800",
    "verify_note": "PR #12 commit 8cd71d7(gates) + 564d9a0(Dockerfile 部署债). gate 在入队源(D3): 自动调度静默跳过 — refresh-scheduler tick(主&&子, settingsRepo 进 deps) / schedule-syncer runScheduledJob cron(主, 不落 scrape_jobs) / yt-email+aggregator+cross-platform(index.ts 三闭包抽到 jobs/enqueues.ts 工厂加主开关 gate, 行为逐项保持: cache-window 去重/cross-platform findByUsername 去重/isSeed/payload 形状). 手动入口 409 crawler_paused: admin-seeds POST + admin-jobs create/retry(rejectIfPaused helper, GET 不受影响). 刻意无 worker backstop(与 drain 语义一致, 审查 3 lens 均认同). 对抗审查(7 agent)3 confirmed should-fix 全采纳: ①bl091-yt-email-backfill.ts ops 脚本漏 gate(唯一未拦成本路径)→暂停时大声中止 exit 1; ②Dockerfile 自 BL-091 起两 stage 都缺 COPY packages/apify(clean checkout docker build 必败→旧容器静默跑, runbook awk 热补丁的根因)→上游修掉, BL-108 migration 0005 部署依赖此修; ③admin-gates-409 retry 断言空洞(boss.send 被前两个 POST 满足)→显式断言 retry 重入队 jobDbId 22+状态重置. 单测15个(每 gate 点 OFF/ON 双向). L1: 146/146 绿 + tsc=0. ⚠️ 该 repo 无 CI, 测试只本地跑(Codex F005 L1 会复跑). ⏭️ 用户 merge PR #12 + 部署 /opt(runbook sync, awk 步骤可删); F003/F004 kolmatrix 端可先按契约开发.",
    "acceptance": [
      "主开关 scraping_enabled=false → 跳过/拒绝所有入队: 自动调度(refresh-scheduler tick / cron scrape index.ts:84 / yt-email enqueue index.ts:51 / aggregator enqueue index.ts:61) tick 内读标志 false 则 return(不入队不报错)",
      "手动 admin 入口(admin-seeds.ts manual_seed / admin-jobs.ts 手动 job) scraping_enabled=false → reply 409 {error:'crawler_paused'} 明确消息; manual_seed 受主开关管(用户决策)",
      "子开关 refresh_enabled=false → 仅 refresh-scheduler tick return(其它抓取不受影响); refresh-scheduler 实际 gate = scraping_enabled && refresh_enabled",
      "标志每 tick 读一次(SettingsRepo.getState, ≤5min 生效); 已入队 job 自然 drain 不取消; 可选 worker backstop(scrape/yt-email/aggregator 处理前再查主开关)",
      "单测(主开关 off→各入队点跳过/拒绝 / 子开关 off→仅 refresh 跳过 / 都 on→正常); 前置 F001; 同一 PR 后续 commit"
    ]
  },
  {
    "id": "F003",
    "title": "kolmatrix: 监控页 proxy + 状态装配 (generator, kolmatrix)",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 2.0,
    "status": "done",
    "done_by": "Kimi_generator",
    "done_at": "2026-06-10T16:30+0800",
    "verify_note": "commit bd097ef(CI 绿)+审查修复 2c86347. crawler-state-client(GET/PATCH /admin/crawler-state 代理, 复用 CrawlerMonitorError 分类, updatedBy 透传); crawler-control-state(assembleCrawlerControlState: availability ok/unknown + 暂停时长仅暂停中计 now-updatedAt NaN防御 + 积压dueNow + lastRefreshAt, stats/state 任一失败独立降级; loadCrawlerControlState allSettled 绝不抛); setCrawlerStateAction(use server: admin 鉴权→PATCH→event_log crawler.state_toggled 审计→revalidate, 错误{ok:false,error:kind}; updatedBy=email||id||fallback slice200). 配套: 爬虫 /admin/stats +lastRefreshAt 只读字段(PR#12 e6f590c, 71763a4 改 to_char ISO — 审查发现 pg ::text 非 ISO 老 Safari 拒解); kolmatrix CrawlerStatsSchema +lastRefreshAt default(null) 旧 API 优雅降级. 单测16个+审查后加固. 审查确认: 契约字段/可空性/序列化两端一致; 爬虫未部署新路由时 404→unknown 态不 500(实测路径).",
    "acceptance": [
      "server action/route 代理爬虫 /admin/crawler-state(GET 读 / PATCH 写, 用 APIFY_KOL_BASE_URL + admin key, 复用现有 client 模式); 错误优雅处理(爬虫不可达→状态未知态不 500)",
      "状态装配供 UI: 当前主/子开关态 + updatedAt/updatedBy + 暂停时长(now-updatedAt, 暂停中才算) + 积压过期 refresh 数(爬虫 kols where next_refresh_at<=now() count, 经 /admin/stats 或新增只读字段) + 最近一次 refresh 时间(scrape_jobs kind=refresh max createdAt)",
      "单测(proxy 读写 / 状态装配 / 爬虫不可达降级); L1 全绿",
      "可与爬虫 F001/F002 并行开发(按 /admin/crawler-state 接口契约 mock)"
    ]
  },
  {
    "id": "F004",
    "title": "kolmatrix: 监控页两个 toggle UI + 确认 + audit + i18n (generator, kolmatrix)",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 2.5,
    "status": "done",
    "done_by": "Kimi_generator",
    "done_at": "2026-06-10T16:30+0800",
    "verify_note": "commit 59e5c87(CI 绿)+审查修复 2c86347. CrawlerPauseControls: 两 switch(checked=已暂停语义), 主暂停→子置灰强制 checked+已覆盖提示(D1), unknown 双禁用; 确认弹窗讲清代价(DialogHeader 排版对齐全库惯例 — 审查修复); 乐观翻转+失败回滚+内联瞬态错误条(库无全局 toast, 文档化偏离)+成功采用爬虫确认态+router.refresh; props 同步用 render 期间调整模式(过 react-hooks lint). 状态行: Paused/Running/Unknown badge+暂停时长+积压+最近 refresh+操作人. audit=F003 action event_log. page 装配复用已拉 stats(单次请求, 测试钉死), 控制面渲染独立于 stats 失败; BL-096 READ-ONLY 文案随控制面落地更新(title+banner, 审查确认无测试/e2e/视觉基线断言旧文案). i18n pause.* 26 keys×5 locale(占位符一致性审查实证). 单测: 组件8(含审查补的子开关 payload 显式断言[blocking 修复]+deferred 乐观属性)+page 2+action 4. 8-agent 对抗审查 1 blocking+4 should-fix 全采纳. ⚠️ 'toast'验收实现为内联瞬态错误条(库惯例, 无 toast 基建).",
    "acceptance": [
      "监控页加两个 toggle: 主「暂停所有爬虫抓取」+ 子「暂停 refresh」; 主 OFF 时子置灰(已被覆盖); 显示 F003 装配状态(暂停中 + 已暂停 X 天 + 积压数 + 最近 refresh)",
      "翻转前确认弹窗(讲清代价: 主=全停抓取 / 子=存量指标冻结); 乐观 UI + 失败回滚 + toast",
      "audit log(kolmatrix 侧记录谁翻的 + 目标态); i18n 5 locale(zh/en/ja/ko/es)",
      "单测(toggle 调 action / 置灰逻辑 / 确认流 / admin-gated 非 admin 不显); L1 全绿"
    ]
  },
  {
    "id": "F005",
    "title": "Codex L1+L2 + signoff (executor:codex)",
    "executor": "codex",
    "priority": "high",
    "estimated_hours": 1.5,
    "status": "pending",
    "acceptance": [
      "L1: kolmatrix lint 0err warn≤3 / tsc=0 / npm test; 爬虫 pnpm --filter @apify-kol/service test 绿",
      "L2(爬虫+kolmatrix 都部署后): ① 翻主开关 OFF→各类 scrape 入队停(scrape_jobs 不再新增/成本停涨), 手动 manual_seed 投喂被拒(409) ② 翻 refresh 子开关 OFF(主 ON)→仅 refresh 停 hashtag 发现等仍跑 ③ 状态显示准确(暂停时长/积压/最近 refresh) ④ 恢复主开关→抓取恢复无 catch-up 尖峰 ⑤ admin-only",
      "signoff docs/test-reports/BL-108-signoff-2026-06-XX.md"
    ]
  }
]
