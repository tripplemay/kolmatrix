[
  {
    "id": "F001",
    "title": "mapper 接 fork location 字段 + i18n-iso-countries normalize + 单测",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 2.5,
    "status": "done",
    "done_by": "Kimi_generator",
    "done_at": "2026-06-01T10:00+0800",
    "acceptance": [
      "npm install i18n-iso-countries (350+ countries, ISO 3166-1, multi-locale aliases)",
      "src/lib/kol-sync/adapters/apify-kol.ts:438 country: null → country: normalizeCountryName(item.location)",
      "新建 src/lib/kol-sync/normalize-country.ts: input=fork location string (英文国名如 'United States' / 'Türkiye'), output=ISO 3166-1 alpha-2 (US / TR) or null",
      "normalize 实现: i18n-iso-countries.getAlpha2Code(name, 'en') 主路径; 遇 unknown name (Mars / 空字符串 / null) fallback null 不报错",
      "src/lib/kol-sync/adapters/apify-kol.ts:361 注释 'fork doesn\\'t expose these' 删除/修正为 'country derived from item.location (fork field) when present; null for TT/IG which fork does not scrape'",
      "单测 ≥6 case 覆盖: (1) 'United States' → 'US' (2) 'Türkiye' → 'TR' (3) 'India' → 'IN' (4) 'Mars' → null (5) item.location undefined → null (6) item.location '' → null",
      "L1 PASS: npm run lint 0 error warning≤3 / npx tsc --noEmit / npm test (新增 normalize-country.test.ts + apify-kol mapper test 扩充)"
    ]
  },
  {
    "id": "F002",
    "title": "Schema migration: kol 表加 country_enrichment_attempted_at + index",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 1.0,
    "status": "done",
    "done_by": "Kimi_generator",
    "done_at": "2026-06-01T10:46+0800",
    "done_note": "migrate dev 因 4 个旧 migration checksum 漂移 (pre-existing, 非本批) 要求 destructive reset，改为手写 migration.sql (Prisma 风格 + ROLLBACK header) + migrate deploy 非破坏性 apply。L1 全绿: validate / migrate status up-to-date / tsc=0 / npm test 1393 passed。",
    "acceptance": [
      "prisma/schema.prisma Kol model 加 countryEnrichmentAttemptedAt DateTime? @map('country_enrichment_attempted_at') @db.Timestamptz",
      "复合 index @@index([tenantId, countryEnrichmentAttemptedAt], name: 'kol_tenant_country_attempted_idx')",
      "npx prisma migrate dev --name bl_081_add_country_enrichment_attempted_at",
      "Migration 文件含 ROLLBACK SQL: DROP INDEX kol_tenant_country_attempted_idx; ALTER TABLE kol DROP COLUMN country_enrichment_attempted_at;",
      "Migration BACKFILL: 不需要 (新字段允许 NULL, 现有 2146 KOL 全 NULL 即 'never attempted')",
      "RLS policy 不变 (新字段在已有 kol 表上, 沿用 tenant_id 隔离)",
      "L1 PASS: npx prisma validate / npx prisma migrate status / npx tsc --noEmit"
    ]
  },
  {
    "id": "F003",
    "title": "enrichment-stage WHERE 改造 + attempted_at 更新逻辑",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 2.5,
    "status": "done",
    "done_by": "Kimi_generator",
    "done_at": "2026-06-01T11:13+0800",
    "done_note": "enrichment-stage.ts WHERE 加 attempted_at gate (field-ref lastSyncedAt>attemptedAt, ?. 容错 unit fake 无 kol.fields); processOne 删 updateData-空-早退 bug, country 候选行无条件写 countryEnrichmentAttemptedAt=new Date() (与 kol.update+auditLog 同 $transaction 原子); audit payload after 加 enrichment_attempted_at; import.ts upsert 当 payload.countryCode 真值时设 countryEnrichmentAttemptedAt=nowDate (与 lastSyncedAt 共享 nowDate 确保 gate 排除). 单测扩充 4 new (gate-skip/gate-retry/null-still-stamps/atomic-rollback) + 修 2 existing after 断言. L1: tsc=0 / lint 0err3warn(pre-existing) / npm test 1397 passed.",
    "acceptance": [
      "src/lib/kol-sync/enrichment-stage.ts:283-307 WHERE 改造: 在原 OR clause 外加 AND (countryEnrichmentAttemptedAt IS NULL OR last_synced_at > countryEnrichmentAttemptedAt) — 已 attempted 但 last_synced 后有新数据则重试",
      "enrichKol 写入路径: 无论 LLM 返 null 还是真值, 都更新 countryEnrichmentAttemptedAt = NOW() (写在与 country_code 同一事务)",
      "事务原子性: 任一字段写入失败整体 rollback (避免 attempted_at 写了但 country 没写, 下次跳过反而错失)",
      "audit_log payload 加 enrichment_attempted_at: 记录 attempt 历史供后续排查",
      "fork mapper 提供 country 的 KOL 在 sync 时同步设置 countryEnrichmentAttemptedAt = NOW() (避免 daily cron 再扫已知 country 的 KOL)",
      "单测扩充 ≥4 case: (1) 已 attempted 且 last_synced=attempted 跳过 (2) 已 attempted 但 last_synced 新 重试 (3) LLM 返 null 仍写 attempted_at (4) 事务 rollback 测试",
      "L1 PASS"
    ]
  },
  {
    "id": "F004",
    "title": "refresh-selector=0 audit report (仅查不修)",
    "executor": "generator",
    "priority": "medium",
    "estimated_hours": 2.0,
    "status": "done",
    "done_by": "Kimi_generator",
    "done_at": "2026-06-01T11:20+0800",
    "done_note": "产 docs/audits/BL-081-refresh-selector-audit.md (6 节). 代码侧根因已确定: refresh=0 非 selector/cursor/fork bug, 而是 BL-059 单源重构删了 refresh phase — scripts/kol-sync-daily.ts:199 refreshCount:0 硬编码, runDaily 无 refresh 调用, fetchTieredRefreshIds 0 production caller (孤儿死代码, 无 test). 能力仍在 (dispatcher.runRefresh + adapter.refresh GET /kol/:platform/:userId 都在). 3 候选修复 (A 重接 selector 推荐 / B 轻量 lastSyncedAt-asc / C 仅扩 discover). SSH 验证 O1-O3 (日志确认 / prod DB staleness 量化 / fork endpoint live-check) 已给精确命令但未执行 (read-only prod 诊断需用户授权/runbook). docs only L1 无需 lint.",
    "acceptance": [
      "SSH prod 跑 grep '\"refreshCount\"' /var/log/kolmatrix-kol-sync.log 7-30 天历史确认 refresh=0 是否一直为 0 / 是否曾有 N>0 时期",
      "看 src/lib/kol-sync/refresh-selector.ts 实现 + 调用点 (scripts/kol-sync-daily.ts 或 dispatcher.ts), 定位 cursor 逻辑 / 选择规则",
      "看 fork apify-kol-service 端是否支持 refresh endpoint (GET /kol 是否有 cursor / since 参数 / refresh path)",
      "产 docs/audits/BL-081-refresh-selector-audit.md 含: (1) 现状 (refresh=0 持续期 + 影响 KOL 数据 stale 程度) (2) 根因 (selector bug / cursor bug / fork 不支持 / 配置漏) (3) 影响评估 (老 KOL follower / engagement_rate 多久没更新 + 商业影响) (4) 修复建议 (3 候选方向)",
      "Report 入 git, 不实装 fix (留独立批次)",
      "L1 PASS: 文档 lint 无 (docs only)"
    ]
  },
  {
    "id": "F005",
    "title": "Staging 量化 + prod backfill 写 attempted_at",
    "executor": "generator",
    "priority": "high",
    "estimated_hours": 2.0,
    "status": "pending",
    "acceptance": [
      "新建 scripts/kol-country-attempted-backfill.ts: 一次性给所有 country_code IS NULL 的 KOL 写 countryEnrichmentAttemptedAt = NOW() (不调 LLM, 仅 DB UPDATE)",
      "Staging 验证: 部署 F001-F003 + F005 backfill 跑一次, next 24h 监控 daily-sync log + aigcgateway calls 量化降幅",
      "Staging acceptance: daily LLM call <200/天 (从 ~500/天 降低 60%+)",
      "Prod backfill: SSH 跑 backfill script, 写所有 2081 attempted=NULL KOL 的 attempted_at = NOW()",
      "Prod 验证 24h: aigcgateway daily call <200/天 (mapper YT 直填 + attempted_at 阻止 silent retry)",
      "Backfill script idempotent (重复跑只更新 NULL attempted_at, 不动已有值)",
      "L1 PASS (script 单测 ≥2 case dry-run + apply)"
    ]
  },
  {
    "id": "F006",
    "title": "Codex Reviewer L1+L2 + signoff (executor:codex)",
    "executor": "codex",
    "priority": "high",
    "estimated_hours": 1.5,
    "status": "pending",
    "acceptance": [
      "L1 自动化 5 项 PASS: (1) npm run lint 0 error warning≤3 (2) npx tsc --noEmit (3) npm test 含 normalize-country + enrichment-stage 单测 (4) npx prisma validate (5) migration ROLLBACK SQL 文法 valid",
      "L2 staging+prod 实测 6 项 PASS: (1) mapper YT 抽样 ≥5 KOL location → country_code 正确 normalize (2) attempted_at 写入抽样 ≥5 KOL 验事务原子性 (3) daily cron 跑后 LLM call 真降到 <200/天 (4) 90 个现有 country_code 0 影响 (抽样查现有 country 仍正确) (5) 下游 discovery/match country filter 不破 (6) refresh-selector audit report 可读且 actionable",
      "成本量化: 5/27-6/01 baseline daily $0.44 vs F005 后 24h daily LLM cost 对比, 期望降幅 >60%",
      "signoff doc docs/test-reports/BL-081-signoff-2026-06-XX.md 含 L1/L2 结果 + 量化降幅表 + 终签",
      "F004 audit report 抽样审 ≥1 候选方向技术可行性 (Reviewer 不写代码, 仅判断 report 质量)"
    ]
  }
]
