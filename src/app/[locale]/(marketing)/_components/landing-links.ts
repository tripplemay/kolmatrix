/**
 * BL-115-F001 — external CTA targets for the marketing landing page.
 *
 * ⚠️ PRD_DOC_URL must be confirmed reachable before prod (spec §1: the link
 * is unverified at authoring time). Swap the value here if the path differs.
 */
export const PRD_DOC_URL = "https://kol.saga1001.com/prd";
