# BL-115 落地页折中加装 — 转化机制 + 邮件协作定位（对齐投放规范文档）

> **Type：** 落地页加装（投放规范文档对齐）。spec 硬性。
> **来源：** 用户飞书投放规范文档 + 缺漏分析 docs/reviews/landing-vs-prd-doc-gap-2026-06-13.md。
> **取向（用户 2026-06-13）：** **折中加装**到 BL-114 现有落地页（不整页重做）。**用文档口径,缺失能力以后再建；游戏IP 按 PRD（本批用真实框架默认）**。
> **基线：** BL-114 Neural Velocity 落地页（staging@50f0180）。

## §1 背景 + ⚠️ 产品现状对照（已核实,落地须知情）

文档定位「游戏KOL营销全流程**邮件协作中心**」+ 强转化。当前是宽泛产品页。本批**加装文档刚需(转化机制)+ 补关键内容块 + 邮件侧重**,不整页推翻。

**⚠️ 文档若干宣称超出产品现状（用户决：用文档口径,缺失以后建。此处留痕,落地时这些 = "待建真功能"跟进项）：**
| 文档宣称 | 现状 | 本批处理 |
|---|---|---|
| 1000+ 游戏邮件模板 | 真实 27(10系统+16AI+1用户) | 用文档口径(用户决), 标记**待建/扩充模板**跟进 |
| 实时追踪开信率**回复率** | 开信率/送达率✅; **回复率从不写(BL-101)** | ⚠️ 文案按文档, 但**强烈建议仅"开信率/送达率"**(回复率空数据试用即穿帮); 回复率真实化 = BL-101 跟进 |
| DKIM/SPF/DMARC一键合规配置 | DomainHealthCard **静态展示**(发件域 Resend 配置, 非用户配置工具) | 演示用真实 DomainHealthCard; 文案避免"一键配置工具"暗示 |
| 98% 信誉分 | 硬编码静态 98% | 用真实 DomainHealthCard 的 98% 展示(措辞诚实非"保障") |
| 游戏IP(PUBG/原神/王者) | 模板主题, **非客户** | **默认"游戏垂类覆盖/模板主题"框架(非客户背书)**; 客户证言**留占位待真实来源**(避免虚构+商标/虚假背书风险) |
| PRD 链接 kol.saga1001.com/prd | 外部, 未验证存在 | 副CTA 跳此链接, **部署前须确认可访问** |

> **铁律(落地页真实性):** 第三方商标(游戏IP)不作虚假客户背书;证言须真实来源或占位;回复率穿帮风险已书面提示。产品自有数字口径按用户决策执行并留痕。

## §2 Features

> 全 generator 含单测 + i18n 5 locale + L1 全绿 + npm run build。加装到 BL-114 结构,守 perf99/a11y/reduced-motion 不回退;视觉沿用 Neural Velocity。

### F001 — 转化机制（最高价值, 投放刚需, 真实无 overclaim）(generator)
- **3 字段试用表单**(姓名 / 公司邮箱 / 所属游戏工作室)→ server action 落 leads(新表或复用 request-access)+ 校验 + 成功态。主 CTA "立即申请试用" 触发表单(模态或 Hero 内嵌)。
- **UTM 捕获**:从 URL query(utm_source/medium/campaign 等)存入 lead + cookie,分渠道归因。
- **埋点**:CTA 点击 / 表单提交 / 滚动深度 / 关键模块停留 → event_log 或前端分析 hook(投放测试要的全链路)。
- **副 CTA "查看完整 PRD 文档"** → kol.saga1001.com/prd(⚠️部署前确认链接);**底部"预约 1v1 产品演示"**(表单或日历链接)。
- 单测(表单校验/提交落库/UTM 捕获/埋点触发)。

### F002 — Hero 邮件协作侧重 + 数据栏 + 痛点副标（generator）
- Hero 标题/副标向「邮件协作中心」侧重(按文档口径: 送达率/合规/开信率痛点)。
- **Hero 核心数据栏**(4 条, 按文档口径,见 §1 处理: 模板库/合规配置/开信率送达率/信誉分)。
- 视觉沿用 Neural Velocity(渐变/光晕/色阶)。i18n + 单测 + build。

### F003 — 痛点模块 + 邮件中心演示模块（重新加回, BL-114 删过）(generator)
- **痛点模块**(文档 4 条邮件痛点: 跨平台邮件乱/送达率低/无法追踪状态/无合规校验)—— 重新加 section。
- **邮件中心演示模块**:复用**真实 reach UI**(DomainHealthCard DKIM/SPF/DMARC + 信誉分 / 最近发送列表 / 游戏垂类模板库)截图/静态展示,标注功能点。
- i18n + 单测 + build。

### F004 — 信任背书(真实框架) + 邮件 FAQ + SEO（generator）
- 游戏IP **"游戏垂类覆盖/模板主题"展示**(非"Trusted by 客户");安全认证(企业级加密等,**仅写真实具备的**);**客户证言留占位**(待用户给真实来源)。
- **FAQ 邮件化**(文档 3 问: 自定义游戏IP模板 / DKIM·SPF技术成本 / 数据更新频率)替换现通用 FAQ。
- **SEO 关键词**自然植入(游戏KOL营销/出海邮件管理/送达率优化/DKIM·SPF合规/游戏合作模板/开信率追踪)。
- i18n + 单测 + build。

### F005 — Codex L1+L2 + signoff（codex）
- L1：lint 0err warn≤3 / tsc=0 / npm test + npm run build。
- L2 staging:① 3字段表单提交落库 + UTM 捕获 + 埋点触发(投放转化链路通)② PRD/演示链接正确 ③ 邮件协作定位 + 痛点 + 邮件中心演示 + 邮件FAQ 体现 ④ Lighthouse perf99/WCAG AA/reduced-motion 不回退 ⑤ visual baseline 重拍 ⑥ 5 locale + 响应式 PC/移动。
- signoff `docs/test-reports/BL-115-signoff-2026-06-XX.md`。

## §3 风险与跟进

- **产品现状 gap(§1)**:落地页含按文档口径但超现状的宣称 —— **跟进真功能 backlog**: 回复率追踪(BL-101)/ 模板扩充 / 真实信誉分。建议尽快对齐避免长期虚标。
- **第三方/真实性**:游戏IP 用垂类框架(非客户背书);证言占位待真实来源;PRD 链接部署前确认。
- **加装 vs BL-114**:本批重新加回 BL-114 删的痛点/邮件演示 + 调 Hero 定位 → 页面更重/更邮件侧重(用户决折中)。守 perf99(新增 section/表单注意 LCP/JS)。
- visual baseline 必 Linux runner 重拍。⚠️ 部署 OOM NODE_OPTIONS=4096。
