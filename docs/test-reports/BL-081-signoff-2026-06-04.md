# BL-081 Signoff 2026-06-04

> 状态：**Evaluator 已签收**
> 触发：BL-081 `F006` 首轮验收完成；L1、本地/ staging / prod 只读证据已覆盖 acceptance，retry storm 已由代码路径 + DB 现状双重证实被消除。

---

## 变更背景

BL-081 修复两类问题：

1. mapper 丢弃 fork `location`，导致可直接得出的 country 没被写入
2. `country_enrichment_attempted_at` 缺失，导致 LLM 返回 `null` 的 KOL 每天被重复扫描，形成 silent retry storm

本轮 Reviewer 目标是确认：

- 新 country 写入路径可用
- retry storm 已真正停止
- 下游 `/match` 等消费 country 的页面没有被打坏
- refresh-selector audit 报告质量达标

---

## 变更功能清单

### F001-F005：Generator 交付复核

**Executor：** `generator`

**关键文件：**

- `src/lib/kol-sync/normalize-country.ts`
- `src/lib/kol-sync/adapters/apify-kol.ts`
- `src/lib/kol-sync/enrichment-stage.ts`
- `src/lib/kol-sync/import.ts`
- `prisma/schema.prisma`
- `prisma/migrations/20260601030000_bl_081_add_country_enrichment_attempted_at/migration.sql`
- `scripts/kol-country-attempted-backfill.ts`
- `docs/audits/BL-081-refresh-selector-audit.md`

**Reviewer 结论：**

- migration / rollback 文法：PASS
- mapper / enrichment / backfill 相关单测与集成基线：PASS
- F004 audit report：PASS

### F006：Reviewer L1 + L2 + signoff

**Executor：** `codex`

**文件：**

- `docs/test-reports/BL-081-verifying-2026-06-04.md`
- `docs/test-reports/BL-081-signoff-2026-06-04.md`

**改动：**

- 执行 L1 本地验收
- 执行 staging 交互验收
- 执行 prod 只读 health / SQL / daily-report 验收
- 汇总 signoff

---

## 未变更范围

| 事项 | 说明 |
|---|---|
| refresh-selector 实装修复 | 本批仅 audit，不落代码 |
| fork 端 0-discover 根因 | 跨团队范围，未纳入本批 |
| 其他 enrichment 字段 | 本批仅覆盖 country / attempted_at storm |

---

## 预期影响

| 项目 | 改动前 | 改动后 |
|---|---|---|
| prod country-enrichment LLM calls / day | `503-510/day`（A0 audit 基线） | `83/day`（2026-06-04 daily report） |
| prod country-enrichment estimated cost / day | `$0.44/day`（spec baseline） | `$0.0747/day`（2026-06-04 daily report） |
| prod retry-storm backlog (`country NULL` 且会继续触发 country attempt) | `2288` | `0` |

> 最后一行来自 prod 实时 SQL：`country_code` 为空且满足 F003 gate 的行数为 `0`。这比“等下一次 cron”更强，因为它直接反映了当前代码的筛选输入。

---

## 类型检查 / CI

```text
Local L1
- npx prisma generate: PASS
- npx prisma validate: PASS
- npx tsc --noEmit: PASS
- npm run lint: PASS (0 error, 3 existing unused warnings)
- npm test: PASS (192 files, 1405 tests)

Git
- local/main HEAD: ff03b29
- prod deployed SHA (ssh /opt/kolmatrix): 6788225
- progress handoff records prod deploy + backfill completed on 2026-06-04
```

---

## L2 实测记录

| 项 | 证据 |
|---|---|
| Staging health | `https://staging.kol.guangai.ai/api/health` → `healthy`, DB/Redis OK |
| Staging authenticated flow | Playwright PASS: `marketer.setup`, login success, `/match` grid visible, filter sidebar visible |
| Staging country consumer path | `/en/match` 抽样卡片已见 `PE / GB / IN`；`/en/match?regions=IN` 返回 2 张且均为 `IN` |
| Prod health | `https://kol.guangai.ai/api/health` → `healthy`, DB/Redis OK |
| Prod daily report | `/opt/kolmatrix/docs/test-reports/kol-sync-daily-2026-06-04.md` 记录 `LLM calls: 83`，`Estimated cost: $0.0747` |
| Prod retry-storm gate | SQL: `country NULL AND attempted_at IS NULL` = `0` |
| Prod true F003 gate | SQL: `((country NULL OR language NULL) AND (attempted_at IS NULL OR last_synced_at > attempted_at))` = `30` |
| Prod country-specific retry upper bound | SQL: `country NULL AND true F003 gate` = `0`；剩余 30 行样本均为 `country present + language null`，按 `processOne()` / `enrichKol()` 代码路径不会打 LLM |
| Prod attempted_at sample | 5 行 `country_enrichment_attempted_at` 实时样本已抽出，最新时间 `2026-06-04T08:11:10Z` |
| F004 audit report | `docs/audits/BL-081-refresh-selector-audit.md` 已含根因、候选方向、O1-O3 实证，actionable |

---

## Ops 副作用记录

| Agent | 阶段 | 操作摘要 | 副作用对齐 | 用户授权 |
|---|---|---|---|---|
| Generator | `verifying` 前置 ops | staging deploy + migration apply + F005 backfill `1873 -> 0` | 仅本批 schema / script / report 路径，无额外产品数据语义改写 | 已记录于 `generator_handoff` |
| Generator | `verifying` 前置 ops | prod deploy + migration apply + F005 backfill `2288 -> 0` | backfill 仅写 `country_enrichment_attempted_at`，不覆写 `country_code` / 其他业务字段 | 已记录于 `generator_handoff`，用户已授权 |

---

## Harness 说明

本批改动已完成 `planning → building → verifying → done` 流程的验收阶段收口。

Reviewer 判定 BL-081 可置 `done`，并将本 signoff 路径写入 `progress.json.docs.signoff`。

---

## Soft-watch

| ID | 描述 | 风险等级 | 建议处置 |
|---|---|---|---|
| S1 | prod `git_sha` 未从 `/api/health` 直接取出（缺少 `HEALTH_DETAIL_TOKEN`），本次以 SSH `/opt/kolmatrix` repo SHA + handoff 交叉确认 deploy 版本 | low | 后续如需严格 deploy-proof，可在 prod audit 脚本中注入 detail token |
| S2 | 本次未做 authenticated prod UI 手验；UI 消费链以 staging Playwright + prod DB/health/daily-report 旁证收口 | low | 后续有 prod UI 批次时顺手补一轮 marketer 路径 dogfood |

---

## Framework Learnings

本批次无新的 framework learnings。
