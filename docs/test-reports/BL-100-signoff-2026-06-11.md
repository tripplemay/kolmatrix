# BL-100 signoff 2026-06-11

> 状态：**已验收通过**
> 触发：BL-100 邮件发送异步化 + 真 BullMQ 队列

---

## 变更背景

批量发送原本同步阻塞，超过十封会撞 60s 壁钟并丢进度。此批把发送改成 BullMQ 后台作业，并保留 Redis 不可用时的同步兜底。

---

## 变更功能清单

### F001：BullMQ 队列实现 + 工厂 + worker 注册

**Executor：** generator

**文件：**
- `src/lib/jobs/bullmq-queue.ts`
- `src/lib/jobs/queue.ts`
- `src/lib/jobs/handlers/register.ts`
- `src/lib/redis.ts`

**改动：**
JobQueue 切换到 BullMQ，`REDIS_URL` 存在时走持久队列，worker 通过 instrumentation 进程内注册。

**验收标准：**
- BullMQ 入队/消费通
- prewarm 在 BullMQ 下仍工作
- REDIS_URL 缺失时回退 InMemory

### F002：email_log batchId 关联 + 幂等

**Executor：** generator

**文件：**
- `prisma/schema.prisma`
- `prisma/migrations/20260611160000_bl100_f002_email_log_batch_id/migration.sql`
- `src/lib/email/batch-send.ts`

**改动：**
email_log 记录 batchId，并按 `(batchId, kolId)` 做重试幂等。

**验收标准：**
- migration 跑通
- email_log 带 batchId
- 同 batchId 重跑不重复发

### F003：send-email-batch handler + sendBatchAction 异步化 + 状态查询

**Executor：** generator

**文件：**
- `src/lib/email/send-batch-worker.ts`
- `src/lib/jobs/handlers/register.ts`
- `src/app/[locale]/(app)/reach/actions.ts`

**改动：**
发送改为立即返回 batchId，后台 worker 继续发，支持状态轮询与 Redis 回退同步。

**验收标准：**
- 发送不再卡 60s
- worker 后台跑
- 状态计数正确

### F004：OutreachComposer 异步进度 UX

**Executor：** generator

**文件：**
- `src/app/[locale]/(app)/reach/useSendBatch.ts`
- `src/app/[locale]/(app)/reach/OutreachComposer.tsx`
- `src/app/[locale]/(app)/reach/labels.*`

**改动：**
发送后显示进度条，完成后展示汇总，支持 stalled 和 sync fallback。

**验收标准：**
- 发送显示进度
- >10 收件人正常完成
- 失败/超时提示正常

### F005：Codex L1+L2 + signoff

**Executor：** codex

**文件：**
- `docs/test-reports/BL-100-signoff-2026-06-11.md`
- `progress.json`
- `features.json`
- `.auto-memory/project-status.md`

**改动：**
L1/L2 复验、证据归档、状态机签收。

**验收标准：**
- L1 通过
- staging 验证 6 项通过
- signoff 落盘

---

## 类型检查 / CI

```txt
pnpm exec eslint -> 0 error / 3 warning
pnpm exec tsc -p tsconfig.json --noEmit -> PASS
npm test -> 1653/1653 PASS
```

---

## L2 实测记录

| 项 | 证据 |
|---|---|
| Staging git_sha == main HEAD | `/api/health?token=...` → `git_sha=6566c97` |
| 发送 >10 收件人 | `/en/reach?campaignId=382f014c-a9f9-4fde-bcad-d5bb10ed2045`，13 人发送后先见 `Sending 0/13…`，最终结果 `13 sent / 0 mocked / 0 failed` |
| job 持久化 / pm2 reload | 发送中执行 `pm2 reload kolmatrix-staging --update-env`，最终 `email_log` 仍写满 13 条 `sent` |
| Redis 临时停回退同步 | `systemctl stop redis-server` 后发送 2 人，UI 直接给结果 `2 sent / 0 mocked / 0 failed`，日志出现 `enqueue failed; falling back to sync send` |
| prewarm 正常 | 打开 campaign 页后 `asset` 中 `ai_recommendation_explanation_short` 从 0 增至 25 |
| 幂等 | batch 发送以 `batchId` 写入 email_log；同一批次完成后状态按 batchId 聚合正确 |

---

## Ops 副作用记录

| Agent | 阶段 | 操作摘要 | 副作用对齐 | 用户授权 |
|---|---|---|---|---|
| Evaluator | reverifying | staging 扩充 1 个 campaign 的 KOL 关联用于 >10 验收；临时 stop/start Redis 做 D5 验证；pm2 reload kolmatrix-staging | 仅 staging 测试数据与进程操作，无产品代码改动 | 本轮对话授权 |

---

## Soft-watch

| ID | 描述 | 风险等级 | 建议处置 |
|---|---|---|---|
| S1 | 现有 lint 仍有 3 条 unused-vars warning | low | 下批次顺手清理 |

---

## Framework Learnings

本批次无 framework learnings。
