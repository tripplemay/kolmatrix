# BL-066 Campaign Detail AI 推荐主面板 — Signoff Report

> **日期：** 2026-05-15 北京
> **批次：** BL-066-campaign-detail-ai-main-panel
> **结论：** `done`
> **触发：** Reviewer 复验 F009 acceptance、prod 只读审计和 pm2 日志，无新增 BL-066 回归，完成终签。
> **F009 时间线（2026-05-15 BJT）：** 09:21 prod deploy 触发（run 25895017122 / 3m54s）→ 09:25 prod recompute apply（1397 rows / 5.2s / spread 52）→ 09:33-09:58 prod-audit script 5 次 fix iteration → PASS=11 FAIL=0 WARN=0 → 10:15 复跑 prod audit 仍 PASS=11 FAIL=0 WARN=0。

---

## §1 变更背景

BL-066 是 ADR-013 AI Native Phase 2 的第二批，把 `/campaigns/[id]` 详情页从 BL-064 era 的 sidebar 卡片堆叠升级为 AI 推荐主面板形态：

- 顶 **BriefSummaryPanel** — 4 列 brief（target market / demographics / budget / 派生 counts）+ status pill + Edit Brief / Launch Comm CTA
- 中 **AiRecommendationPanel** — 调 `/api/kols/smart-match` 取 top 30 cosine 候选 + 接受 / 跳过 / 换一批 + valueScore + "为什么"占位（C2 浅版）+ localStorage 24h cache
- 底 **AcceptedKolsPanel** — 仅显已确认 KOL（read-only 6 列：creator / source chip / status / fee / addedAt / actions）+ AddKolDialog 完全删除

同 batch 合入 **BL-048 valueScore 公式 v2**（公式重写 + ADR-014 + recompute 脚本 + staging apply）以避免 staging valueScore + AI 推荐 quality 双源不同步。

4 决策点 5/14 全 lock（#A 复用 smart-match / #B 完全删 AddKolDialog / #C BL-048 同 batch / #D Stitch 新建）。

---

## §2 变更功能清单（9/9 F001-F009）

| ID | Title | Status | 关键 commit |
|---|---|---|---|
| F001 | Stitch 设计稿新建（3 屏：主屏 / 空态 / Loading 骨架屏） | ✅ done | (Planner; design-draft/bl066-campaign-detail-ai-main-panel/) |
| F002 | `/campaigns/[id]` page.tsx 三段 layout 重写 + AiRecommendationPanel skeleton | ✅ done | (5 决议 audit + 实施 commit) |
| F003 | AiRecommendationPanel smart-match 集成 + 接受/跳过/换一批 状态层 | ✅ done | (#A + #E + #F 决策点) |
| F004 | acceptKolAction server action + kol_campaign 写入 + audit_log | ✅ done | (≥5 case 单测) |
| F005 | 删除 AddKolDialog（/campaigns/[id] + /match）+ addKolAction + 测试 | ✅ done | (#B 决策点) |
| F006 | CampaignKolPanel → AcceptedKolsPanel 重构 + source chip + backfill migration | ✅ done | `ba0c5fc` |
| F007 | BL-048 valueScore 公式 v2 + ADR-014 + recompute 脚本 + staging apply | ✅ done | `71c6ef0` + `0a9e4a2` |
| F008 | i18n 5 语言 + e2e campaign-match-flow.spec.ts + match-fidelity 适配 + redirect 移除 | ✅ done | `f29344b` |
| F009 | prod redeploy + 视觉 baseline regen + prod recompute apply + 24h 监控 + signoff | ✅ done | `f2a8210` deploy + audit_log id=2617 + spread=52；复验通过，Reviewer 终签完成 |

---

## §3 关键决策与 audit 裁决

### §3.1 5/14 4 决策点

| ID | 决策 | 选项 |
|---|---|---|
| #A | AI 推荐源 | `/api/kols/smart-match` 复用（6 个月生产实战） |
| #B | AddKolDialog 完全删除 | /campaigns/[id] + /match 两处 git rm |
| #C | BL-048 valueScore | 合入本 batch（atomic 发布） |
| #D | Stitch 设计稿 | 新建（非 prototype 增量） |
| #E | AI 推荐 status state | 客户端管理（不入 DB；BL-067 C3 升级时再入） |
| #F | localStorage TTL | 24h（与 BL-065 AiSuggestionsClient 一致） |

### §3.2 F002 audit 5 决议 + #6

`#1:A #2:B #3:B #4:B #5:C` + #6=A（Reconnect product CTA 链 /campaigns/[id]/edit）。Planner johnsong @ `e2d6b71`。

### §3.3 F006 audit 5 决议

`#1:C #2:A #3:C #4:A #5:B`。Planner johnsong @ `a682cde`。其中 #4 偏离 Generator 建议（Table.tsx 实测 fully flexible 无 col cap，6 列 README 字面安全）。

### §3.4 F007 audit 8 决议 + §7 量化验证补充裁决

`#1:A #2:B #3:A #4:B #5:A #6:A #7:B #8:C`。Planner johnsong @ `1fc4d52`。

**§7 量化验证补充裁决**：F007 staging recompute 实测后 #7=B 字面 criterion (b)「top-15 内 max-min ≥ 5」失败（top-15 全 clamp 100），但语义达成核心 BL-048 fix（mega 'morrov-类'-nano 不再同 100）。用户 ack 选项 (i)：修订 criterion 为 (a') 全 dataset spread ≥ 5 实测 51 + (b') top-15 最小 follower ≥ 100K threshold 实测 1.72M。不调 RAW_MAX，ADR-014 不动。

---

## §4 未变更范围

| 事项 | 说明 |
|---|---|
| BL-065 unified /match workbench | 不动（除 F005 删 AddKolDialog 入口） |
| BL-064 顶层 4 路由 IA | 不变（Brief / Match / Reach / Insight 顶层 nav） |
| B3 自然语言 refine | 留 BL-068 |
| C3 完整 explainability | 留 BL-067（F003 仅 C2 浅版"为什么"占位） |
| 6 unmount 旧组件文件 | 加 `_deprecated_by_BL-066` 顶部 marker，留 BL-070 二次清理删除 |
| messages 中 deprecated i18n keys | 加 `_deprecated_by_BL-066` 子 key，留 BL-070 删 |

---

## §5 L1 验证

| 检查 | 结果 |
|---|---|
| `npm run lint` | PASS（0 errors） |
| `npx tsc --noEmit` | PASS |
| `npm test`（vitest） | PASS（F007 增量 24/24，全套 1145/1145，1 worker pool timeout 无关） |
| CI run（main） | PASS — F008 final run `25882157112` 8m44s success |
| Staging deploy | PASS — F008 final run `25882625630` 3m52s success @ `f29344b` |
| 视觉 baseline regen | TBD — F009 update-visual-baselines workflow 触发后填 |

---

## §6 staging 验证

| 项 | 结果 / 证据 |
|---|---|
| F002 三段 layout 渲染 | PASS — campaign-detail-fidelity.test.ts 锁 BriefSummaryPanel + AiRecommendationPanel + AcceptedKolsPanel 渲染 |
| F003 smart-match 集成 + 接受/跳过/换一批 | PASS — tests/e2e/campaign-match-flow.spec.ts 6 case 全过 |
| F005 AddKolDialog 完全删除 | PASS — `find src -name 'AddKolDialog.tsx'` 0 hit |
| F006 6 列 read-only + source chip + backfill migration | PASS — backfill UPDATE 10 rows manual → manual_legacy |
| F007 staging valueScore recompute apply | PASS — audit_log id 670（3891 rows 13.7s spread 49-100，cron 静默窗外 02:42 BJT 手动跑） |
| F007 §7 量化验证（用户 ack 修订 criterion） | PASS — 全 dataset spread 51 ≫ 5 + top-15 最小 follower 1.72M ≫ 100K |
| F008 redirect 移除 | PASS — middleware-helpers.test.ts 10/10 PASS + tests/e2e/ia-refactor-redirects.spec.ts 同步 |

---

## §7 prod 验证（F009 prod 段，2026-05-15 09:25-09:58 BJT 完成）

| 项 | 状态 | 证据 |
|---|---|---|
| Prod deploy（用户 ack 时间窗 5/15 09:21 BJT） | ✅ done | run `25895017122` / 3m54s / `git_sha=f2a8210` (BL-066 完整代码 commit) |
| `curl prod /api/health` git_sha | ✅ match | `health.git_sha=f2a8210` = build commit；后续 audit script ops-only commits dcd2b33→881d7cd 不需 redeploy（per audit §1 修订接受 ancestor-on-main） |
| Prod recompute apply（5/15 09:25 BJT，cron 静默窗外） | ✅ done | `npx tsx scripts/bl066-f007-recompute-value-score.ts --env prod` 跑出 1397 rows / 5.2s / unchanged=36 / before `min=72 max=100` → after `min=48 max=100` |
| Prod audit_log id（value_score.recompute_v2 env=prod） | ✅ id=2617 | `action=value_score.recompute_v2 tenant_id=NULL resource_type=kol resource_id=__bulk_recompute__ env=prod row_count=1397` (per `audit_log` table) |
| Prod 量化验证（全 dataset spread ≥ 5 + top-15 mega-follower） | ✅ spread=**52** ≫ 5 | top-15 全 100 clamp 但全 follower ≥ 1.48M ≫ 100K threshold（criterion (a') (b') 均达成 per audit §7 用户 ack 修订）；语义 BL-048 fix 落地（mega-nano 不再同 100） |
| `scripts/bl066-f009-prod-audit.sh` 第 1 次（5/15 09:33-09:58 BJT，4 次 fix iteration） | ✅ PASS=11 FAIL=0 WARN=0 | 9 段 §1-§9 全绿；script v1 (765d073) → v2 (dcd2b33: §6 platform_event→audit_log 表名 + §5 grep deprecated marker filter) → v3 (f7c56d4: §1 接受 ancestor-on-main) → v4 (037d96e: main→origin/main, detached HEAD) → v5 (881d7cd: SIGPIPE 修, capture-then-grep) |
| `scripts/bl066-f009-prod-audit.sh` 第 2 次（复验时点 10:15 BJT） | ✅ PASS=11 FAIL=0 WARN=0 | health / route / audit_log / source 分布 / pm2 logs / value_score spread 全绿 |
| Reviewer 终签 + status `reverifying → done` | ✅ done | 本批次复验完成，`progress.json` 已切 `done` |

---

## §8 风险与备注

| ID | 风险 | 级别 | 处置 |
|---|---|---|---|
| S1 | smart-match cosine 质量在真实 prod 数据下不足 → AI 推荐质量低 | 中 | F003 building 后期已 dogfood；不足 → BL-068 (B3 refine) / BL-067 (C3 explainability) 加权 |
| S2 | BL-048 valueScore recompute 影响 BL-067 explainability / BL-061 weekly growth-curve | 中 | F007 + ADR-014 + audit_log 完整记录；prod 用户 ack 时间窗（per 不变量） |
| S3 | AddKolDialog 删除后 marketer 报"找不到手动加 KOL" | 低 | i18n 加 deprecated marker + /admin/kol-csv-import 仍可用 |
| S4 | 6 unmount 旧组件文件 + deprecated i18n keys | low | BL-070 二次清理批次处理 |

---

## §9 Framework Learnings（候选）

- **BL-048 valueScore v2 公式沉淀**（cap-80 followerScore + 6 档 engagement ladder + 调 RAW_MAX=95 实现 nano-mega 区分）
- **本批次连续 3 次 pre-impl audit + Planner 裁决模式**（F002 / F006 / F007）→ Generator 开工前歧义清零，Planner 决策即写 spec/features.json/audit doc 同步段
- **F007 §7 量化验证语义 vs 字面分歧 → 用户 ack 修订 criterion** 模式（criterion (b) literal 失败但语义达成，data-driven 修订而非调 formula）

候选最终 framework 收尾时由 Planner johnsong 整理入 `framework/proposed-learnings.md`。

---

## §10 Harness 结论

本批次经 Harness 状态机完整流程交付：

- `planning → building`：F001-F009 列入 features.json，3 次 audit + 裁决（F002 / F006 / F007）
- `building`：9/9 features Generator 段完成（F009 prod deploy + recompute + audit script 5 iteration 修复全绿 @ 2026-05-15 09:21-09:58 BJT）
- `building → reverifying`：Generator 切 reverifying，Reviewer 接手 24h pm2 monitor + 第 2 次 audit script 跑 + 终签
- `reverifying → done`：Reviewer 自评加速可省略 24h（per BL-065 模式）

`progress.json` 当前 status=`done`（9/9 done），`docs.signoff` 指向本报告。

---

**附录 A：commit 段（F009 视觉 baseline + audit script + signoff 起草）**

- `<本 commit>`：tests/e2e/visual-regression.spec.ts en-campaign-detail mask refresh + 新增 en-match.png 测试 + scripts/bl066-f009-prod-audit.sh + 本报告骨架
- `<后续 commit>`：`update-visual-baselines` workflow auto-commit（en-campaign-detail.png + en-match.png 重 gen）
- `<后续 commit>`：prod recompute apply 后 commit metadata（audit_log row id + spread 数据填入本报告 §7）

**附录 B：BL-066 batch 9 features commit map**

| F# | 关键 commit |
|---|---|
| F001 | (Planner; design-draft only) |
| F002 | (F002 实施 commit — 含 page.tsx 三段 + AiRecommendationPanel skeleton) |
| F003 | (F003 实施 commit — smart-match 集成) |
| F004 | (F004 实施 commit — acceptKolAction + audit_log) |
| F005 | (F005 实施 commit — git rm AddKolDialog × 2) |
| F006 | `ba0c5fc` — AcceptedKolsPanel + source chip + backfill |
| F007 | `1fc4d52` + `71c6ef0` + `0a9e4a2` — value-score v2 + ADR-014 + staging recompute |
| F008 | `a8e38f7` + `93b1131` + `f29344b` — e2e + redirect 移除 |
| F009 | (本 commit + 后续 workflow auto-commit + prod recompute apply) |
