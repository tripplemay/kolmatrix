# BL-091 YT 邮箱解锁链路修复 Signoff 2026-06-07

> 状态：**PASS (fix_rounds=0)** — verifying→done
> 触发：F004 Codex L1+L2 验收
> Evaluator：Reviewer (Codex)
> 签名时间：2026-06-07

---

## 变更背景

"用 Apify 抓 YouTube KOL 商务邮箱"链路代码完整但从没跑过。Bug A：触发器只挂在 discovery 路径而信号由 refresh 写入→永不相交。Bug B：yt_email_check_records 双写失效→去重坏→重复烧 Apify。344 个 youtube(TikHub 标 hasBusinessEmail=true 但无邮箱)积压。走 Apify(SCALE 已付费)不依赖 TikHub 充值。

---

## 变更功能清单

### F001 — Bug A：触发器接入 refresh 路径
**Executor：** generator | **Status：** PASS
**改动：** `refresh-scrape.ts` YouTube 分支 upsert 前读旧 hasBusinessEmail，仅 false/null→true 跃迁触发 enqueueYtEmail 一次。PR #6 merge→master abd7a81。
**验证：** 单测 6 PASS + code review。⚠️ Runtime 触发实测 DEFERRED（TikHub 余额耗尽 refresh 不跑，用户同意 defer→BL-092）。

### F002 — Bug B：yt_email_check_records UPSERT 修复
**Executor：** generator | **Status：** PASS
**改动：** markRunning(kolId, channelId) 改 INSERT ON CONFLICT。PR #7 merge→master 4cc9421。单测 4 + 集成测 5(PG)全绿。
**L2 验证（Codex SSH）：** `SELECT status,count(*) FROM yt_email_check_records GROUP BY status` → **succeeded 339 / failed 1 / no_email 1 / queued 1** ✅

### F003 — 344 backfill 入队
**Executor：** generator | **Status：** PASS
**改动：** 脚本 `bl091-yt-email-backfill.ts` / `.mjs`，对 has_business_email=true 且无邮箱 youtube 入队 yt-email。PR #8 merge→master f1d1bb7。
**L2 验证（Codex SSH）：** YT 邮箱覆盖 **526 flag / 523 有邮箱** = 99.4% 覆盖率（原 184→523，+339 解锁）✅。成功率 339/342=99.1%。成本 ~$49。

### F005 — worker poll 超时 env 可配
**Executor：** generator | **Status：** PASS
**改动：** 加 YT_EMAIL_POLL_TIMEOUT_MS env(min 30s，默认 300s)。PR #9 merge → master 4d102f1。
**L2 验证（Codex SSH）：** worker 日志确认 `poll timeout 300000ms` ✅。300s 期实现 99.1% yield（vs 120s 期 ~62%）。

### F004 — Codex L1+L2 + signoff
**Executor：** codex | **Status：** PASS

**L1 验证：**
| 检查项 | 结果 |
|---|---|
| `npx prisma generate` | PASS |
| `npm run lint` | 0 errors, 3 pre-existing unused-import warnings |
| kolmatrix 产品代码变更 | 零（BL-091 全在上游 guang-tech/apify） |

**L2 验证（Codex SSH 独立实证）：**
| 检查项 | 结果 | 证据 |
|---|---|---|
| Deployed SHA | `4d102f1`（与 Generator 声称一致） | `git rev-parse HEAD` on /opt/apify-kol-service |
| yt_email_check_records 落库 | 339s/1f/1n/1q | `SELECT status,count() GROUP BY` |
| YT 邮箱覆盖率 | 526 flag, 523 有邮箱 = 99.4% | `SELECT COUNT() FILTER` |
| F005 poll timeout | 300000ms | `docker logs` grep "poll timeout" |
| Apify-kol 健康 | ok | `curl localhost:3004/health` |
| F001 runtime 触发 | DEFERRED（TikHub 余额耗尽→BL-092） | 用户已同意；单测 6 + code review 覆盖逻辑 |

---

## 未变更范围

| 事项 | 说明 |
|---|---|
| kolmatrix src/ | 零运行时改动 |
| kolmatrix Prisma schema | 无变更 |
| TikHub 依赖 | 不使用（走 Apify SCALE） |
| F001 端到端触发 | defer→BL-092（充值后 refresh 恢复才能验） |

---

## 预期影响

| 项目 | 改动前 | 改动后 |
|---|---|---|
| yt_email_check_records | 0 行（链路死亡） | 342 行(99.1% succeeded) |
| YT 真邮箱覆盖 | 184 | 523 (+339 = +184%) |
| worker poll timeout | 硬编码 120s | env 可配，默认 300s |
| 去重机制 | 失效（无记录→永真） | 生效（UPSERT + 闭包检查） |
| 静默空转风险 | Bug A 永不相交 | false→true 跃迁触发一次 |

---

## L2 实测记录

| 项 | 证据 |
|---|---|
| Codex SSH 直连 VPS | `ssh tripplezhou@34.180.93.185` → 成功 |
| DB 记录落库 | succeeded=339, failed=1, no_email=1, queued=1 ✅ |
| 邮箱覆盖 | `SELECT` 确认 526 flag / 523 email = 99.4% ✅ |
| 部署 SHA 对齐 | /opt/apify-kol-service HEAD = `4d102f1` ✅ |
| Service health | `curl localhost:3004/health` → `{"status":"ok"}` ✅ |
| 日志 poll timeout | `docker logs` 含 `poll timeout 300000ms` ✅ |

> **RSC server action 限制：** N/A — 纯 infra/bugfix，无 UI 改动。

---

## Soft-watch

| ID | 描述 | 风险 | 建议 |
|---|---|---|---|
| S1 | F001 runtime 触发未实测（TikHub 余额耗尽阻塞 refresh） | low | BL-092 充值后验证 refresh 跃迁→入队 |
| S2 | 3 pre-existing lint warnings（非本批引入） | low | 下批次顺手清理 |
| S3 | F003 1 failed(kol111304) + 1 queued(kol70929) straggler | low | 删 progress 文件后可回收重跑 |
| S4 | /opt/apify-kol-service 残留 .mjs 脚本 | low | 下次 fork-sync 时清理 |

---

## Deferred → BL-092

| 项 | 备注 |
|---|---|
| F001 runtime 触发实测 | refresh hasBusinessEmail 跃迁→enqueueYtEmail→records 落库 |
| TikHub 充值后 YT 邮箱增量 | 充值恢复 refresh，新跃迁频道持续解锁 |

---

## Harness 说明

本批经 Harness 状态机完整流程：`planning → building → verifying → done`（首轮 PASS, fix_rounds=0）。
Signoff 路径：`docs/test-reports/BL-091-signoff-2026-06-07.md`
