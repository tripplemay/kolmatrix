# BL-023 KOL 评分升级 验证报告 2026-05-07

> 状态：**Reviewer verification complete**
> 触发：BL-023 `KOL 评分体系升级`，含 F008 engagement_rate 单位修正与 backfill SQL
> Reviewer：Codex

## Summary

- Scope: 验证 BL-023 的 8 个 feature，覆盖 `computeKolValueScore` 新公式、`similarityToScore` 直接映射、`kol-sync-daily.ts` 的 top100 重算逻辑、`engagement_rate` 单位修正、以及 staging 端浏览器 / API 证据。
- Documents: [`docs/specs/BL-023-kol-scoring-upgrade-spec.md`](/Users/yixingzhou/project/joyce/docs/specs/BL-023-kol-scoring-upgrade-spec.md), [`src/lib/kol/value-score.ts`](/Users/yixingzhou/project/joyce/src/lib/kol/value-score.ts), [`src/lib/discovery/smart-match.ts`](/Users/yixingzhou/project/joyce/src/lib/discovery/smart-match.ts), [`scripts/kol-sync-daily.ts`](/Users/yixingzhou/project/joyce/scripts/kol-sync-daily.ts), [`scripts/backfill-engagement-rate-bl023-f008.sql`](/Users/yixingzhou/project/joyce/scripts/backfill-engagement-rate-bl023-f008.sql), [`progress.json`](/Users/yixingzhou/project/joyce/progress.json)
- Environment: 本地 L1 `npm run lint`, `npm run typecheck`, `npx vitest run ...`, `npm run test:integration -- tests/integration/kol-discovery.test.ts` + staging `https://staging.kol.guangai.ai`
- Result totals: PASS 4, FAIL 0, BLOCKED 1, NOT RUN 0

## Test Cases

- BL-023-TC-01 L1 pure-function and batch regression - PASS
- BL-023-TC-02 Staging health / login / discovery shell - PASS
- BL-023-TC-03 Staging Smart Match API - PASS
- BL-023-TC-04 Staging KOL detail percent rendering - PASS
- BL-023-TC-05 Prod deploy + backfill SQL handoff - BLOCKED

## Execution Results

### BL-023-TC-01 L1 pure-function and batch regression

Result: PASS
Evidence:
- `npm run lint` -> PASS with 3 pre-existing warnings only
- `npm run typecheck` -> PASS
- `npx vitest run tests/unit/value-score.test.ts tests/unit/smart-match-similarity.test.ts tests/unit/kol-sync-engagement-batch.test.ts` -> `33/33` tests PASS
- `npm run test:integration -- tests/integration/kol-discovery.test.ts` -> `12/12` tests PASS
Observed Behavior:
- New engagement ladder, authenticity modifier, direct similarity mapping, and engagement batch unit fix all stayed green under their dedicated tests.
Mismatch vs Spec:
- None in BL-023 scope.
Defect Link / Reference:
- None.

### BL-023-TC-02 Staging health / login / discovery shell

Result: PASS
Evidence:
- `https://staging.kol.guangai.ai/api/health` returned `status=healthy`, `database=ok`, `redis=ok`, `git_sha=b6c3668`
- Login with `marketer@kolmatrix.local / KOLMatrix@2026!` reached `/zh/discovery`
- Discovery shell rendered `AI 智能搜索` and the KOL grid; first page showed `匹配到 1,195 位 KOL`
Observed Behavior:
- Staging is healthy and on the expected commit.
Mismatch vs Spec:
- None.
Defect Link / Reference:
- None.

### BL-023-TC-03 Staging Smart Match API

Result: PASS
Evidence:
- Staging DB contains 6 products, including `BL040 Staging Test Game`
- POST `/api/kols/smart-match` for `productId=cmotrswwx0000lfbnt9e89jn6` returned `200`
- Response contained `embeddedJustInTime=true`
- Top result `similarity=0.6064436435699506` produced `matchScore=61`
- Subsequent results stayed in the `57-61` range, all inside the expected `0-100` scale
Observed Behavior:
- Smart Match scoring now exposes an honest 0-100 scale instead of the old midpoint-shifted range.
Mismatch vs Spec:
- None.
Defect Link / Reference:
- None.

### BL-023-TC-04 Staging KOL detail percent rendering

Result: PASS
Evidence:
- `/zh/database` loaded 10 saved KOL rows on staging
- `/zh/kols/eb344e66-3aeb-4022-bbb7-e485540514b3` rendered `互动率11.4%` on the overview card
- The page still shows `价值分 —` for that row because the live staging row has not yet been through the new top100 recompute path
Observed Behavior:
- `engagementRate` displays as a percent, matching the unit fix and the seed/filters/UI contract.
Mismatch vs Spec:
- The page-level value score is not yet materialized for every row on staging.
Defect Link / Reference:
- Not a code defect; it is the expected consequence of the pending cron/backfill ops.

### BL-023-TC-05 Prod deploy + backfill SQL handoff

Result: BLOCKED
Evidence:
- Current project status still lists the prod deploy + `sudo -u postgres psql -d kolmatrix -f scripts/backfill-engagement-rate-bl023-f008.sql` step as a manual handoff
- This step has not been executed from this evaluator session
Observed Behavior:
- I can verify the SQL file and the code path, but I cannot complete the prod deploy/backfill step from the evaluator role.
Mismatch vs Spec:
- The spec’s prod observation window is not yet closed.
Defect Link / Reference:
- None.

## Defects

- None in BL-023 implementation scope.

## Coverage Gaps

- Prod top100 `valueScore` recompute after the `engagement_rate` unit fix has not been observed yet.
- Staging still shows some KOL rows with old/null `valueScore` because the daily cron/backfill path has not been exercised in this session.

## Open Questions

- Should the prod deploy + backfill handoff be completed before the batch is eligible for final signoff?

## Final Decision

- Ready: Not yet
- Readiness: Partial
- Final: `BLOCKED` on the pending prod deploy/backfill handoff
