# BL-070 Reach + Insight + 二次清理 — Signoff Report

> **日期：** 2026-05-19 北京
> **批次：** BL-070-reach-insight-cleanup（Phase 4 第二批 / 项目近期最后一批）
> **结论：** _TBD（Evaluator 填）_ — 待 prod deploy + 24h 监控 + 12 项 checklist 全 PASS
> **触发：** Generator (johnsong) 完成 F001-F007 + F008 generator 域内任务（audit script + signoff skeleton）后切 status=verifying；Evaluator (Reviewer) 接管 prod deploy ack + 24h 监控 + 12 项 checklist 验证 + 此 signoff doc 终签。
> **F008 时间线（待填）：**
>   - YYYY-MM-DD HH:MM prod deploy 触发（run XXXXX / Xm Xs）
>   - YYYY-MM-DD HH:MM bl070-prod-audit.sh 首跑 PASS=X FAIL=X WARN=X
>   - YYYY-MM-DD HH:MM bl070-prod-audit.sh 24h 复跑 PASS=X FAIL=X WARN=X
>   - YYYY-MM-DD HH:MM Reviewer 终签

---

## §1 变更背景

BL-070 是 ADR-013 AI Native Phase 4 第二批，也是项目近期对外上线前最后一批：

- **Reach 段（F001-F002）**: /reach 路由实装 — git mv /outreach → /reach 全量迁移 + Match accept→Reach 衔接 toast；customize.ts + topic-cloud.ts 迁移到 runAigcAction SDK（v0.9.22 #6 沉淀触发条件落地）
- **Insight 段（F003）**: /insight 路由实装 — Dashboard + Reports 合并到 3-tab IA layout（dashboard/reports/analytics）；git mv /weekly-report → /insight/weekly-report；4 条 301 redirect
- **二次清理段（F004-F005）**: 3 老路由目录 git rm（/dashboard + /knowledge-base + /campaigns/new）+ IA_REDIRECT_RULES 全清 10 条 + safe-link/nav-config 同步 + 6 BL-066 unmount 组件 + 9 i18n deprecated markers + 6 整 ns + 7 nav.* 老 keys
- **E2E 重写段（F006）**: 4 路由 IA 全量 e2e suite 重写 — match-flow 合并 22 case + reach-flow 新建 6 case + insight-flow 新建 6 case + 4 已合并 specs git rm
- **视觉 baseline 段（F007）**: update-visual-baselines workflow regen 7 个 baselines + git rm 6 老 baseline + 5 locale reach.*/insight.* 占位 ns + visual-regression unskip
- **F008**: staging + prod deploy + 24h 监控 + 12 项对外上线 checklist signoff（本 doc）

8 决策点 5/18-19 全 lock：
1. ready-to-build
2. Reach 迁移 + Match衔接 + customize 迁
3. Insight 仅合并（AI 学到偏好留 Phase 5）
4. 二次清理全清
5. BL-070 同批即停 redirect（老路由 404，项目近期未 prod 上线 SEO 风险可控）
6. e2e 完整重写 + 老 e2e 清理
7. 嵌入 spec §10 12 项 checklist + signoff doc 验证
8. 基础设施全 ack 复用 v0.9.22

---

## §2 变更功能清单（8/8 F001-F008）

| ID | Title | Status | 关键 commit |
|---|---|---|---|
| F001 | /reach 路由 layout + Outreach 功能迁移 + 老 /outreach 301 + Match→Reach 衔接 | ✅ done | `2b24fc1` + CI 自修 `191eca6` |
| F002 | customize.ts + topic-cloud.ts 迁移到 runAigcAction SDK（v0.9.22 #6 沉淀落地） | ✅ done | `f6b8e09` |
| F003 | /insight 路由 layout + Dashboard + Reports 合并 + 4 条 301 redirect | ✅ done | `d10e646` |
| F004 | 5 老路由目录 git rm + middleware redirect 删除 + UUID guard | ✅ done | `8c15543` + `05b9a14` + `cacd0b5` |
| F005 | 删 6 BL-066 unmount 旧组件 + 5 locale deprecated i18n keys 全删 | ✅ done | `3e40600` + `31a7cfd` |
| F006 | 4 路由 IA 全量 e2e suite 重写 + 老 e2e spec 清理 | ✅ done | `ec39157` (CI 一轮过 0 自修) |
| F007 | 视觉 baseline 全量 regen + 5 locale i18n 完整 cover (reach + insight) | ✅ done | `3f3d9b1` + `8c633ab` (baseline workflow) + `ed7f6a4` + `cb35e51` |
| F008 | staging + prod deploy + 24h 监控 + 对外上线 ready signoff | _TBD_ | _Generator 域内: audit script `scripts/bl070-prod-audit.sh` + 本 signoff skeleton; Evaluator 接管 prod deploy + 监控 + 终签_ |

---

## §3 关键决策与 audit 裁决

### §3.1 8 决策点（5/18-19 全 lock）

| ID | 决策 | 选项 |
|---|---|---|
| #1 | spec ready-to-build | 直接 building（无 audit） |
| #2 | Reach 实施路径 | 路径迁移 + Match 衔接 + customize 迁 SDK |
| #3 | Insight 重构深度 | 仅合并（AI 学到偏好留 Phase 5） |
| #4 | 二次清理范围 | 全清 5 老路由目录 + 6 BL-066 unmount 组件 + 9 deprecated i18n marker + nav 老 keys |
| #5 | BL-070 redirect 生命周期 | 同批即停（老路由 404，项目近期未 prod 上线 SEO 风险可控） |
| #6 | e2e 重写策略 | 完整重写 4 IA suite + 4 已合并 specs git rm + 不破坏 BL-066/067/068 现 case |
| #7 | F008 验收策略 | spec §10 嵌入 12 项 checklist + Reviewer signoff doc 逐项验 |
| #8 | 基础设施复用 | 全 ack 复用 v0.9.22（runAigcAction SDK + IaRedirectRule status field + Turbopack --webpack 防御） |

---

## §4 §10 12 项对外上线 ready checklist 验证

Evaluator 跑 `bash scripts/bl070-prod-audit.sh` 自动验大部分项；手动验 §5-§9。

| # | Checklist 项 | 自动化 / 手动 | 验证方法 | Status |
|---|---|---|---|---|
| 1 | 4 路由全 active | 自动化 (§2) | `bl070-prod-audit.sh` §2 curl /en/{brief,match,reach,insight} → 307→/login | _TBD_ |
| 2 | 7 老路由全 404 | 自动化 (§3) | `bl070-prod-audit.sh` §3 curl /en/{dashboard,discovery,database,outreach,reports,weekly-report,knowledge-base} → 404 | _TBD_ |
| 3 | 5 locale UI 全 PASS | 自动化 (§4) | `bl070-prod-audit.sh` §4 5 locale × 4 路由 mount sanity | _TBD_ |
| 4 | cost cap < $5/day/tenant | 自动化 (§12) | `bl070-prod-audit.sh` §12 audit_log 24h sum cost_usd; BL-070 自身 0 incremental cost (复用 BL-067 SDK actionId) | _TBD_ |
| 5 | 4 路由 e2e suite 全 PASS | 手动 | CI run for prod deploy commit → E2E job 全绿（match-flow 22 case + reach-flow 6 + insight-flow 6 + brief-flow 6 = 40+ case） | _TBD_ |
| 6 | 视觉 baseline 全 PASS | 手动 | CI E2E visual project 全绿（25 baselines per F007 final state） | _TBD_ |
| 7 | a11y ≥90 | 手动 | Lighthouse a11y audit 在主 4 IA 路由 ≥90 | _TBD_ |
| 8 | Lighthouse 性能 ≥80 | 手动 | Lighthouse performance ≥80 在主 4 IA 路由 | _TBD_ |
| 9 | ≥5 marketer dogfood spot check | 手动 | 邀 5 marketer 在 staging 跑完 brief → match → reach → insight 流程,反馈 0 P0/P1 bug | _TBD_ |
| 10 | 24h 监控数据正常 | 自动化 (§11) | `bl070-prod-audit.sh` §11 pm2 logs grep BL-070-related errors; 24h 后复跑 | _TBD_ |
| 11 | prod /api/health git_sha 验 | 自动化 (§1) | `bl070-prod-audit.sh` §1 health.git_sha == origin/main HEAD (或 last 10 commits ancestor) | _TBD_ |
| 12 | i18n-locale-coverage parity 8/8 PASS | 自动化 (§7) | `bl070-prod-audit.sh` §7 5 locale 0 `_deprecated_by_*` marker residue; CI `tests/unit/i18n-locale-coverage.test.ts` 8/8 PASS | _TBD_ |

---

## §5 v0.9.23 候选框架沉淀（3 条，BL-070 done 阶段 Planner 集中沉淀）

| # | 来源 | 类型 | 建议写入 |
|---|---|---|---|
| 1 | BL-070-F004 | 新坑 | `framework/harness/generator.md` — 删显式子路由前必须先加上游 [id] UUID guard（扩展 v0.9.21 BL-064-F006 沉淀范围）。删 `/campaigns/new/page.tsx` 后 next.js fallback 到动态 `/campaigns/[id]/page.tsx` → Prisma 抛 invalid uuid 500。grep 自查：`find src/app -name 'page.tsx' -path '*[*]*'` 列动态路由后逐个查是否已有 UUID guard。 |
| 2 | BL-070-F004 | 新坑 | `framework/harness/generator.md` — `notFound()` 在 next-intl 包装下 HTTP status 不可预测（200 或 404）。Next.js 15 App Router server component notFound() 标准是 404，但 next-intl middleware 包装响应后实际 status 可能 surface 为 200 + not-found body。e2e 验路由废弃时不能严格 `expect(status).toBe(404)`，应 `expect(status).toBeOneOf([200, 404])` + 验 Location header 不含错误目的地（或验 page body 含 "not found" 文案 + URL 未变化）。 |
| 3 | BL-070-F005 | 新坑 | `framework/harness/generator.md` — 删 i18n deprecated ns 前必须 grep 实际 callers，ns 可能跨 batch git mv 后仍 in use。BL-070-F004 git mv 把 KB CRUD 组件搬到 brief/ 但组件内部仍 `useTranslations("knowledgeBase")`。盲信 marker "will delete this namespace" 整 ns 删会破 production。Python 批处理脚本应内嵌该自检：`grep -rln 'useTranslations|getTranslations.*"<ns>"' src/` 0 caller 才能整 ns 删。 |

## §6 CI 自修轮统计（BL-070 累积 5 轮，符合 v0.9.21 "删除文件类批次 CI 多轮自修属预期"）

| Round | Commit | 根因 |
|---|---|---|
| F001#1 | `191eca6` | edge-states-coverage outreach→reach |
| F004#1 | `05b9a14` | integration middleware test + woff2 subset + /campaigns/[id] UUID guard |
| F004#2 | `cacd0b5` | /campaigns/new e2e 改测 "不再 redirect to /brief"（notFound + i18n 包装 HTTP status 不可预测） |
| F005#1 | `31a7cfd` | woff2 subset 再 regen（5 文件删除后 icon set 缩小） |
| F006 | _0 自修轮 ✓_ | 合并 + 抽 helpers 模式良好 |
| F007#1 | `cb35e51` | visual-baselines-shape EXPECTED_BASELINES 同步（baseline rename） |

## §7 最终签字

- **Generator (johnsong)**: F001-F007 + F008 generator 域内任务全完成（audit script + 本 signoff skeleton），切 status=verifying 让 Evaluator 接管 — _2026-05-19 YY:YY BJT_
- **Evaluator (Reviewer)**: _12 项 checklist 全 PASS 后填字, 切 status=done_ — _TBD_

---

> **BL-070 done = Phase 4 完整 done = 4 路由 IA 完整闭环 = 对外上线 ready（距 ~2 周）**
