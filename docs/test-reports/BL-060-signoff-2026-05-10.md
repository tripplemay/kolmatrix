# BL-060 soft-delete-ui-filter-hotfix Signoff 2026-05-10

> 状态：**Reviewer signoff PASS**
> 触发：BL-060 `reverifying` 完成 fix-round 2 后的最终复验
> Reviewer：Codex

## 总体结论

- BL-060 已完成签收，结论为 `Ready`。
- fix-round 2 通过共享 `storageState` 把 `database-fidelity` 的 7 次重复登录收敛成 1 次 setup 登录，彻底消除了 staging 上的 login waitForURL 抖动。
- soft-delete 相关 query 修复、历史污染清理、以及 staging 可见性验证都已闭环。

## 验收结果

- L1 通过：
  - `npm run lint` 0 errors / 2 warnings
  - `npx tsc --noEmit` 通过
  - `npm run test` 156 files / 1101 tests PASS
- Playwright staging 通过：
  - `tests/e2e/marketer.setup.ts` -> PASS
  - `tests/e2e/database-fidelity.spec.ts` -> 7/7 PASS
  - `tests/e2e/login-cinematic.spec.ts` -> PASS
  - `tests/e2e/marketer-dashboard.spec.ts` -> PASS
- staging 通过：
  - `https://staging.kol.guangai.ai/api/health` -> healthy

## 关键证据

### 1. database-fidelity 彻底稳定

- `tests/e2e/marketer.setup.ts` 先登录一次并写入 `playwright/.auth/marketer.json`。
- `tests/e2e/database-fidelity.spec.ts` 改为 `test.use({ storageState: "playwright/.auth/marketer.json" })`。
- 7 条用例全部 PASS：
  - Quick Stats KPI strip
  - filter bar dimensions
  - Tier and Game filters
  - Insights Panel
  - no ghost CTA
  - Bulk Action Bar mount
  - header CTAs

### 2. BL-060 的 query / SQL 修复仍保持有效

- `dashboard/data.ts`、`database/stats.ts`、`kpi-snapshot.ts`、`campaigns/list-kpis.ts`、`campaigns/detail.ts`、`crm/overview.ts` 继续保持 `deletedAt: null`。
- `BL-060-F003` 与 `BL-060-F005` SQL ops 文件仍在仓库中。

### 3. 运行时核对

- `api/health` healthy，staging 可用。
- 登录、仪表盘、数据库页面都能稳定通过 Playwright 复验。

## 说明

- fix-round 2 的核心是把“每条用例各自登录”改成“setup 一次登录 + 复用 session”。
- 这次没有新增数据库 ops。

## 最终结论

- Final grade: `A-`
- Readiness: `Ready`
- `progress.json.docs.signoff` 已填入本报告路径。
