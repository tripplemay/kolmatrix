# BL-035 L2 Verifying Notes — 2026-05-05

## 范围
- 批次：BL-035
- 阶段：verifying
- 目标：staging 证据核对 + 识别已闭环项 / 受限项

## 已实证通过
- `F009`：`kol_campaign` 索引在 staging PostgreSQL 中存在，`kol_campaign_kol_id_idx` 与 `kol_campaign_tenant_id_idx` 均可见。
- `F012`：staging `/zh/discovery` 首屏按 `valueScore` 排序时，首屏为高分真实 KOL，未见 mock KOL 顶首页。
- `F004`：staging `/zh/weekly-report` 的分享 toast 复制出 `https://staging.kol.guangai.ai/shared/weekly-report/...`，域名正确，没有回落到外部 host。
- `F001`：staging 新密码 `KOLMatrix@2026!` 可登录；旧密码 `KOLM@2026!` 被拒，页面提示“密码至少需 12 个字符。”
- `F003`：staging `/api/kols/smart-match` 连续第 11 次请求返回 `429`，`error=rate_limit_exceeded`，`Retry-After=56`。
- `F006`：staging `/api/webhooks/resend` 在未配置 secret 的情况下返回 `500`，未静默接受签名。

## 受限项
- `F005`：staging 当前只有一个 tenant，无法构造真实跨租户反例，因此不能在 staging 数据层直接复验 `not_found` 反例。
- `F008`：当前 staging 数据下可见的 campaign 仅有 3/4/3 个 KOL，无法自然把邮件中心推进到 `>8` 的批量边界；只能保留单测证据。
- `F013`：KOL 详情页在 YouTube 样本上确实渲染了 topic cloud，但 staging 侧未暴露可直接抓取的 `actions/run` 请求体日志，因此无法在不改环境的前提下逐字核对 `USER_VIDEO_TITLE` 包裹的网络证据。

## 备注
- 这轮验证未写 signoff；`docs.signoff` 仍应保持空，直到所有必须项都能闭环。
- `F006` 仍需用户完成 staging/prod secret 配置后再做最终复验。
