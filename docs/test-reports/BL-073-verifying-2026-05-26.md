# BL-073 Verifying Report — 2026-05-26

## 结论

- 批次：`BL-073-prod-hotfix`
- 结果：`FAIL`
- Reviewer：`codex: Reviewer`
- 阶段：`verifying`

这轮 BL-073 首轮验收不能 signoff。L1 自动化整体通过，但 L2 抽样暴露了两个真实 blocker：

1. `F006` 的 staging 行为未完全满足验收口径。`/zh/match` 默认页会显示 20 张 KOL 卡片，但 `language` filter 没有灰显，也没有 `"(暂无数据)"` 提示；当前只看到 `region` 维度被灰显。
2. `F007` 的 `STRICT_MS_ICONS` 拦截能力未真正成立。临时副本里把 `MatchSummaryBar` 的 `grid_view` 改成 `unknown_icon` 后，`tests/unit/material-symbols-coverage-unit.test.ts` 仍然 PASS，因为 `scripts/regenerate-material-symbols-subset.sh` 会把 `unknown_icon` 自行 discover 进输出列表。

按 spec / features 口径，这两个点都阻断 `F008`。

## L1 结果

### 1. `npm run lint`

结果：

```text
0 errors / 3 warnings
```

warnings：

- `src/app/api/health/__tests__/route.test.ts:18:20`
- `src/lib/queue/explain-recommendations-worker.ts:81:3`
- `tests/integration/db-platform-admin-nullif.test.ts:13:31`

判定：`PASS`

### 2. `npx tsc --noEmit`

结果：PASS

判定：`PASS`

### 3. `npm test`

结果：

- `185 files`
- `1322 tests`
- 全部通过

备注：

- 运行期间出现 Node `--localstorage-file` warning，但未导致测试失败
- 低于 Generator handoff 里写的 `186 / 1329`，但本地实测结果本身是全绿

判定：`PASS`

### 4. manifest 8 漏 ligature

检查：

- `forward_to_inbox`
- `refresh`
- `article`
- `attach_money`
- `error_outline`
- `hourglass_empty`
- `mark_email_unread`
- `verified_user`

结果：8 项全命中 `scripts/material-symbols-icons-manifest.txt`

判定：`PASS`

### 5. woff2 size

结果：

- 当前 `src/app/fonts/material-symbols-outlined.woff2` = `11628` bytes
- 上一版（`10fbb79^`）= `11080` bytes
- 增量 = `+548` bytes

判定：`PASS`

备注：增量不是 handoff 里写的 `~800B`，但确实存在正向增长。

### 6. `messages/zh.json`

结果：

- `match.emptyState.body` 存在
- 值为：`未找到符合当前筛选的 KOL — 请尝试调整或清除部分筛选条件。`

判定：`PASS`

## L2 staging 抽样

环境：

- `https://staging.kol.guangai.ai`
- authenticated Playwright probe using `playwright/.auth/marketer.json`

### 1. `/zh/brief` form 与 tab nav 同宽

结果：

- form 宽度：`976`
- 页内 tab nav 宽度：`976`

判定：`PASS`

### 2. `/zh/match` 默认进入显示 20 个 KOL 卡片

结果：

- `[data-testid="match-kol-card"]` count = `20`
- `match-row` count = `0`
- 非 empty-state

判定：`PASS`

### 3. `/zh/match` emptyState 中文文案非字面 key

抽样方式：

- 强制 URL：`/zh/match?languages=zzzz`

结果：

- 页面进入 empty-state
- 文案为：`未找到符合当前筛选的 KOL — 请尝试调整或清除部分筛选条件。`
- 未出现字面 key `match.emptyState.body`

判定：`PASS`

### 4. `/zh/match` filter sidebar country/language 灰显

结果：

- `region` 维度出现 `"(暂无数据)"` 提示
- 但 `[data-testid="match-filter-languages"]`：
  - `disabled = false`
  - `aria-disabled = "false"`
  - 无 `match-filter-languages-no-data`

判定：`FAIL`

说明：

- 实现代码里没有 `country` 维度，实际对应的是 `region`
- 当前 staging 只满足了 `region` 灰显，不满足 spec / F008 写明的 `country/language` 灰显口径

### 5. Material Symbols strict 拦截能力

抽样方式：

1. 复制最小工作区到 `/tmp/bl073-ms-strict`
2. 把 `src/app/[locale]/(app)/match/MatchSummaryBar.tsx` 中 `grid_view` 改成 `unknown_icon`
3. 运行：

```bash
STRICT_MS_ICONS=true npx vitest run tests/unit/material-symbols-coverage-unit.test.ts
```

结果：

- 测试仍然 `PASS`
- 同时在 temp copy 中运行 `bash scripts/regenerate-material-symbols-subset.sh`，输出包含：
  - `grid_view`
  - `unknown_icon`

判定：`FAIL`

说明：

- 当前 strict 守卫的判定是“src 中提到的 icon 是否在 manifest 或 regenerate script discover 列表里”
- 但 regenerate script 会把 `unknown_icon` 自己扫出来，导致未知 ligature 被“自证合法”
- 这意味着 acceptance 中要求的“临时 PR 加 unknown icon → CI 红”目前不成立

### 6. `/zh/campaigns/[id]` icon 抽样

结果：

- 抽样页中已确认渲染：
  - `forward_to_inbox`
  - `refresh`
  - `attach_money`
- 其余目标 ligature 在该样本页未露出；对应代码位于 toast / accept-toast / conditional branch 中

判定：`PASS (sample-only)`

备注：这项不是当前阻断项；当前 FAIL 已由 F006/F007 两个 blocker 充分成立。

## 最终判定

### PASS

- `F001`
- `F002`
- `F003`
- `F004`
- `F005`

### FAIL

- `F006`
  - 原因：staging 只看到 `region` 灰显，`language` 未灰显，未满足 `country/language` 灰显验收口径
- `F007`
  - 原因：`STRICT_MS_ICONS` 无法拦截 `unknown_icon`；临时副本验证仍 PASS

### 未完成

- `F008`
  - 原因：signoff 被 `F006` / `F007` 阻断

## 建议修复

1. 修正 `F006` 的 staging 行为或验收口径一致性。
   - 如果真实目标是 `region + language`，那就需要让 `language` 也在 coverage=0 时灰显并显示 `"(暂无数据)"`
   - 如果产品只要求 `region`，则必须同步修 spec / features / handoff，避免 Reviewer 验收口径和实现不一致

2. 修正 `F007` 的 strict 守卫设计。
   - 不能再用“未知 ligature 被 discover 出来就算合法”的路径
   - 至少要保证 `unknown_icon` 这类未入 manifest / 未在受控白名单中的 token 能让测试 fail

修完后应进入 `reverifying`，复验重点只需重跑：

- `tests/unit/material-symbols-coverage-unit.test.ts`
- staging `/zh/match` filter disabled 行为
- 必要的 `lint` / `tsc` / targeted UI 抽样
