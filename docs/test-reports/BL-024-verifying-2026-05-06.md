# BL-024 Ghost-controls Cleanup Verifying 2026-05-06

> 状态：**Verifying**
> 触发：复核 BL-024 5 个 ghost-control 相关 feature 的 L1/L2 验收

## 结论

- 功能层面通过。
- 当前未见阻塞性功能缺陷。
- 有一条 soft-watch：spec 文案要求 `tracking-list.png` / `suppression-list.png` 视觉基线，但仓库现有 visual-regression contract 仅覆盖 19 张既有 baseline，没有这两页的专门 screenshot test。

## 变更功能清单

### F001 /database 头 3 按钮

**Executor：** generator

**验收结果：** 通过

**证据：**
- 浏览器走查确认 `导出` 链接、`导入 CSV` 按钮、`新增 KOL` 按钮都可点。
- `/api/database/export-csv` 已有集成测试覆盖。
- `/api/database/import-csv` 已有集成测试覆盖。

### F002 /roi 时间范围 toggle

**Executor：** generator

**验收结果：** 通过

**证据：**
- 浏览器走查确认 `7天 / 30天 / 90天 / 全部` 四个范围均为活链接。
- 页面上的 ROI KPI、趋势图、AI 洞察区均渲染正常。

### F003 /weekly-report Last Week / Last Month toggle

**Executor：** generator

**验收结果：** 通过

**证据：**
- staging 浏览器确认 `上周`、`上月` 控件均可见且 `上月` 提示仍保持当前规范说明。
- 周报页面的生成、下载、分享、复制链接等主路径正常。

### F004 /outreach/tracking 列表

**Executor：** generator

**验收结果：** 通过

**证据：**
- 浏览器走查确认 `追踪` tab 为活链接，status filter 全部可见。
- 列表加载出真实 `EmailLog` 行，分页按钮存在。
- 集成测试覆盖 tenant scoping 与 status filter。

### F005 /outreach/suppression 列表

**Executor：** generator

**验收结果：** 通过

**证据：**
- 浏览器走查确认 `黑名单` tab 为活链接。
- 页面展示 empty state 文案 `暂无被屏蔽的 KOL。`
- 集成测试覆盖空会话重定向与 tenant 过滤。

## L1 结果

```text
npm run test:integration -- tests/integration/database-export-csv.test.ts tests/integration/database-import-csv.test.ts tests/integration/roi.test.ts tests/integration/weekly-report.test.ts tests/integration/outreach-tracking.test.ts tests/integration/outreach-suppression.test.ts
Test Files  6 passed (6)
Tests       27 passed (27)

npx vitest run tests/unit/visual-baselines-shape.test.ts
Test Files  1 passed (1)
Tests       3 passed (3)
```

## L2 结果

| 项 | 结果 |
|---|---|
| Staging health | `healthy`，`db=ok`，`redis=ok` |
| 浏览器走查 | `/zh/weekly-report`、`/zh/database`、`/zh/roi`、`/zh/outreach/tracking`、`/zh/outreach/suppression` 均确认活控件与真实数据渲染 |
| 关键 invariant | tracking 列表真实显示 `EmailLog` 行；suppression 页面 empty state 正常；database 页面 export/import/add KOL 均可操作 |

## Soft-watch

| ID | 描述 | 风险等级 | 建议处置 |
|---|---|---|---|
| SW-1 | BL-024 spec 文案要求 `tracking-list.png` / `suppression-list.png` 视觉基线，但当前 visual-regression contract 未包含这两页的专门 screenshot test，且 `tests/screenshots/baseline/` 中没有对应 PNG。 | medium | 若后续要求严格对齐 spec 文案，建议补充这两页的 screenshot test + baseline；若以现有回归门禁为准，可保持现状。 |

