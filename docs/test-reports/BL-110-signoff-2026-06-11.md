# BL-110 signoff 2026-06-11

> 状态：**已验收通过**
> 触发：BL-110 split-brain quickwins wave 1 复验完成

---

## 变更背景

合并 BL-101/102/103/104 的四个低风险用户可见止血项，避免死链、脏卡、误计已接受 KOL、以及回复链误导性空态。

---

## 变更功能清单

### F001：KOL 详情面包屑死链修复 → `/match`

**Executor：** generator

**文件：**
- `src/app/[locale]/(app)/kols/[id]/page.tsx`
- `src/app/[locale]/(app)/kols/[id]/__tests__/kols-detail-fidelity.test.tsx`

**改动：**
面包屑从死链 `/kols` 改为 `/match`。

**验收标准：**
- 点击 KOL 详情面包屑直达 `/match`
- 不再 404

### F002：`/assets` type 白名单收口

**Executor：** generator

**文件：**
- `src/lib/assets/types.ts`
- `src/lib/assets/queries.ts`
- `src/app/[locale]/(app)/assets/page.tsx`

**改动：**
默认列表和 welcome 统计只看 `LISTABLE_ASSET_TYPES`。

**验收标准：**
- explanation cache 行不出现在 `/assets` grid
- explanation-only tenant 正确进入 welcome

### F003：`kol_campaign` accept 读口径统一

**Executor：** generator

**文件：**
- `src/app/[locale]/(app)/campaigns/[id]/accepted-filter.ts`
- `src/app/[locale]/(app)/campaigns/[id]/AcceptedKolsPanel.tsx`
- `src/app/[locale]/(app)/campaigns/[id]/page.tsx`

**改动：**
accepted 视图只显示 `source` 白名单且 `suggestionStatus` 为 `accepted`/`NULL` 的行。

**验收标准：**
- `skip/swap` 不再进入已接受列表
- accepted count 不再误计

### F004：Reply 链诚实空态止血

**Executor：** generator

**文件：**
- `src/lib/email/analytics.ts`
- `src/features/dashboard/EmailPerformanceCard.tsx`
- `src/app/[locale]/(app)/reach/tracking/page.tsx`
- `src/app/[locale]/(app)/reach/tracking/TrackingTable.tsx`

**改动：**
回复 pending 改为按全时段 `repliedAt` 是否存在判断，不再按窗口/当前页误判。

**验收标准：**
- 无真实 reply 时显示 honest empty state
- 有历史 reply 的 tenant 不再显示 B4 pending note

---

## 未变更范围

| 事项 | 说明 |
|---|---|
| BL-105 Edit Brief | 不在本批 |

---

## 预期影响

| 项目 | 改动前 | 改动后 |
|---|---|---|
| KOL 详情面包屑 | 404 | 正常进入 `/match` |
| `/assets` grid | 混入 explanation cache | 仅可见资产 |
| accepted KOL 列表 | 误含 skip/swap | 只含真实 accepted |
| reply 面板 | 误导性 pending | 真实数据驱动 |

---

## 类型检查 / CI

```txt
pnpm exec eslint -> 0 error / 3 warning
pnpm exec tsc -p tsconfig.json --noEmit -> PASS
npm test -> 1627/1627 PASS
```

---

## L2 实测记录

| 项 | 证据 |
|---|---|
| Staging git_sha == main HEAD | `95f348d` deployed, health aligned |
| 端到端流验证 | staging `/en/kols/...` -> `/en/match`; `/en/assets`; `/en/campaigns/2d11dd71...`; `/en/reach`; `/en/reach/tracking`; `/en/insight` |
| 关键 invariant | `skip` 行不在 accepted panel；有历史 reply 的 tenant 不再显示 pending note |
| 浏览器手动验 | staging/prod 登录态页面均通过，`dashboard-email-perf-reply-note` 不再出现于有 reply tenant |

---

## Ops 副作用记录

本批次无数据库写入型 ops。

---

## Soft-watch

| ID | 描述 | 风险等级 | 建议处置 |
|---|---|---|---|
| S1 | 现有 lint 仍有 3 条 unused-vars warning | low | 下批次顺手清理 |

---

## Framework Learnings

本批次无 framework learnings。
