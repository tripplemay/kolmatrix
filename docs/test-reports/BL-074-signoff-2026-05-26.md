# BL-074 Signoff Report — 2026-05-26

## 结论

- 批次：`BL-074-ia-v2`
- 结果：`PASS`
- Reviewer：`codex: Reviewer`
- 阶段：`verifying`

BL-074 已完成首轮验收并终签。未发现新的产品 blocker，当前批次可推进到 `done`。

## L1 结果

### 1. `npm run lint`

结果：

```text
0 errors / 3 warnings
```

warnings：

- `src/app/api/health/__tests__/route.test.ts:18:20`
- `src/lib/queue/explain-recommendations-worker.ts:81:3`
- `tests/integration/db-platform-admin-nullif.test.ts:13:31`

判定：`PASS`

### 2. `npx tsc --noEmit`

结果：PASS

判定：`PASS`

### 3. local Playwright

命令：

```bash
bash scripts/test/codex-e2e.sh tests/e2e/sidebar-nav-5-routes.spec.ts
```

结果：

- `5 passed`
- `29 skipped`

覆盖到：

- 5-nav 顺序（`en` / `zh`）
- `/en/campaigns` 高亮 Campaigns
- `/campaigns` 行 Match KOL CTA 的 href 口径

判定：`PASS`

### 4. `NAV_ITEMS` 静态检查

结果：

- `campaigns` item 存在
- 顺序为：
  - `/brief`
  - `/campaigns`
  - `/match`
  - `/reach`
  - `/insight`

判定：`PASS`

### 5. `messages/zh.json`

结果：

- `nav.campaigns = 活动`
- `nav.campaignsDescription = 进行中的触达活动与管线`
- `campaigns.matchKolAction` 3 个 key 完整存在

判定：`PASS`

### 6. ADR-015 / ADR-013 / README

结果：

- `docs/adr/ADR-015-5-route-ia-add-campaigns-nav.md` 存在，`167 LOC`
- ADR-013 顶部存在 superseded marker
- `docs/adr/README.md` 索引包含 ADR-015，并标明 supersedes ADR-013 §IA

判定：`PASS`

## L2 staging 抽样

环境：

- `https://staging.kol.guangai.ai`
- 本轮重新登录 `marketer@kolmatrix.local / KOLMatrix@2026!`

备注：

- 现有 `playwright/.auth/marketer.json` 已过期，会落回登录页；本轮已用真实登录重建会话完成抽样

### 1. `/zh` sidebar 5-nav 顺序

结果：

前 5 个一级 nav 为：

- `概要`
- `活动`
- `匹配`
- `触达`
- `洞察`

对应 href：

- `/zh/brief`
- `/zh/campaigns`
- `/zh/match`
- `/zh/reach`
- `/zh/insight`

判定：`PASS`

### 2. `/en/campaigns` 高亮 Campaigns

结果：

- `nav a[aria-current='page']` = `Campaigns`
- href = `/en/campaigns`

判定：`PASS`

### 3. `/en/campaigns/[id]` 高亮 Campaigns

结果：

- 进入某活动详情后，active nav 仍是 `Campaigns`
- href = `/en/campaigns`

判定：`PASS`

### 4. `/campaigns` 行 Match KOL CTA

结果：

- `campaign-row-match-cta` count = `4`
- 首个 href = `/en/match?campaignId=2d11dd71-1a98-4ee0-b15d-314dae9fcd3c`

判定：`PASS`

### 5. `/en/insight` QuickActions

结果：

- 仅 3 个按钮
- href 分别为：
  - `/en/brief?tab=products`
  - `/en/match`
  - `/en/match?view=table`

判定：`PASS`

## 最终判定

### PASS

- `F001`
- `F002`
- `F003`
- `F004`
- `F005`
- `F006`

### 批次状态

- `progress.json.status = done`
- `completed_features = 6`
- `docs.signoff = docs/test-reports/BL-074-signoff-2026-05-26.md`

## 备注

- 当前仍有 3 个历史 lint warning，维持 soft-watch，不阻断 BL-074 `done`
- staging health endpoint 在本地 `x-health-token: staging-health-check` 探针下只返回 `healthy`；本轮以页面级 staging 抽样为主证据
