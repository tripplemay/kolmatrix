# BL-108 signoff

- L1: PASS
  - `npm run lint` 0 errors / 3 warnings
  - `pnpm exec tsc -p tsconfig.json --noEmit` PASS
  - `npm test` 1592/1592 PASS
  - targeted `CrawlerPauseControls` spec 10/10 PASS

- L2: PASS
  - Admin UI: `data-ready` gate works; initial state renders `Initializing controls…`, then switch becomes clickable and confirm dialog opens
  - Main OFF: `PATCH /admin/crawler-state` -> `scrapingEnabled=false`; `POST /admin/seeds` -> `409 crawler_paused`; `POST /admin/scrape-jobs` -> `409 crawler_paused`
  - Main ON + refresh OFF: `POST /admin/scrape-jobs` -> `201`
  - Restore: UI returns to `Running`; `updatedBy=codex-l2`
  - Admin-only: `marketer@kolmatrix.local` is redirected to `/en/insight`
  - Staging state restored to `scrapingEnabled=true`, `refreshEnabled=true`

