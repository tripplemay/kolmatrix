# BL-076 signoff — PASS

> Sprint: `BL-076-apify-numeric-overflow`
> Reviewer: `codex: Reviewer`
> Date: `2026-05-27`
> HEAD: `5e1e05f`

## Verdict

`PASS`

BL-076 is ready to close. The hotfix is effective on prod, and the final fix-round 2 acceptance wording now matches the real steady-state semantics of staging.

## L1

All required local checks passed:

- `npm run lint` → `0 errors / 3 warnings`
- `npx tsc --noEmit` → PASS
- `npm test` → PASS
- `npx prisma migrate status` → `Database schema is up to date!`
- local schema confirms `engagementRate @db.Decimal(7, 2)`

## L2

### Staging steady-state validation

Command:

```bash
AI_DAILY_COST_USD_PER_TENANT_MAX=500 npx tsx scripts/kol-sync-daily.ts --enrichment-limit=10
```

Observed result:

```text
discover=2567 inserted=0 updated=1859 failed=0 errors=0 level=INFO
```

Staging report agrees:

```text
Imported: inserted=0 updated=1859 skipped=708 failed=0
Embedding hook: Embedded=0 Failed=0
Enrichment stage: Scanned=10 language+=2 country+=0 failed=0
```

This satisfies the fix-round 2 lock:

- `(inserted + updated) > 0`
- `failed = 0`
- no `numeric field overflow`

### Prod instantaneous backfill evidence

Current structured prod log line confirms the outage-recovery run:

```json
{"discoverCount":2567,"inserted":474,"updated":1385,"failed":0,"errors":[]}
```

### Prod SQL evidence

Current superuser SQL checks:

```text
engagement_rate > 999.99 = 15
engagement_outlier = 157
audit_log kol.import_failed = 0
created_at > 2026-05-26T16:37:00Z = 474
max(engagement_rate) = 9137.06
```

Sample persisted rows above the old `Decimal(5,2)` ceiling:

```text
tiktok  beyond2known  9137.06  true
tiktok  zyph209       8730.80  true
tiktok  allengngy     5870.02  true
```

### Health

`/api/health` remains healthy and currently reports:

- `total_active_kols=1871`
- `country_fill_rate=0.056654195617316945`
- `language_fill_rate=0.12560128273650453`

## Conclusion

BL-076 closes with the following acceptance interpretation:

- staging validates steady-state sync health via `inserted + updated > 0`
- prod log + prod SQL provide the one-time outage-recovery evidence that new rows were successfully backfilled
- the original numeric overflow failure mode is no longer present
