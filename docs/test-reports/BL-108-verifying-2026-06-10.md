# BL-108 verifying report

- L1: PASS
  - `joyce`: `lint` 0 errors / 3 warnings, `typecheck` PASS, `npm test` 1588/1588 PASS
  - `apify-kol`: `pnpm --filter @apify-kol/service test` PASS, `tsc --noEmit` PASS, build PASS
- L2: PARTIAL PASS / BLOCKED
  - Main OFF: `PATCH /admin/crawler-state` -> `scrapingEnabled=false`; `POST /admin/seeds` -> `409 crawler_paused`; `POST /admin/scrape-jobs` -> `409 crawler_paused`
  - Main OFF wait window: `scrape_jobs.kind='refresh'` stayed flat until restore; no catch-up spike
  - Refresh OFF with main ON: `PATCH /admin/crawler-state` -> `refreshEnabled=false`; `POST /admin/scrape-jobs` -> `201`
  - Restore ON: refresh count resumed by +3 only (`342 -> 345`)
  - Blocker: `/en/admin/crawler-monitor` renders state, but switch clicks do not trigger confirmation or state change in headless Playwright; console logs React minified error `#418`

