# BL-071 Verifying Report — 2026-05-25

## 结论

- 结果：`FAIL`
- 批次：`BL-071-harness-cleanup`
- Reviewer：`codex: Reviewer`
- 范围：F010 首轮 L1 + L2 抽样验收

BL-071 的结构重组大部分已通过，但首轮验收仍有 2 个真实 blocker，均属于文档/状态重组未完全收口：

1. `F002` cowork 清理口径未达成
2. `F003` planner 拆分后的活规则 cross-reference 未完全迁移

这两项都违反了本批次自己定义的严格 grep 验收条件，因此本轮不能 signoff。

## 已执行

### L1

1. `find framework/ -name "*.md" -exec ...` 抽查 frontmatter / header
2. `grep -rln "framework/harness/planner.md" framework/ .auto-memory/ docs/specs/ docs/dev/ --include="*.md"`
3. `grep -rln -i "cowork" framework/ .auto-memory/ --include="*.md" --include="*.sh"`
4. `python3 -c 'import json; json.load(open("backlog.json"))'`
5. `python3 -c 'import json; json.load(open("progress.json"))'`
6. `python3 -c 'import json; json.load(open("features.json"))'`
7. `bash framework/bootstrap.sh` in temp dir `/tmp/bl071-bootstrap-test`
8. `find framework/harness -maxdepth 1 ...` + `find framework/harness/checklists ...`

### L2

1. 抽读 `planner-workflow.md` / `planner-arbitration.md` / `planner-checklists.md`
2. 抽读 `evaluator.md` §13
3. 抽读 `generator.md` Suspense fallback 合并段
4. 抽读 `framework/proposed-learnings.md` header §「写入流程」
5. 抽读 `framework/CHANGELOG.md` v0.9.23 与 `framework/archive/proposed-learnings-archive-v0.9.23.md`

## 通过项

- JSON 校验通过：`backlog.json` / `progress.json` / `features.json`
- `framework/bootstrap.sh` 在临时目录实跑通过
- `framework/harness/checklists/` 子目录迁移正确
- `planner-workflow.md` / `planner-arbitration.md` / `planner-checklists.md` 不是机械切割，topic 拆分成立
- `evaluator.md` §13 已把量化 criterion、mock infeasible、RSC server-action mock 不可用按 topic 合并
- `generator.md` Suspense fallback 已把 BL-070 `#29 + #30` 合并为单段并保留双 source
- `framework/proposed-learnings.md` header 已正式化 D7 + D8
- `framework/CHANGELOG.md` v0.9.23 与 archive v0.9.23 对应关系成立

## 阻塞项

### 1. F003 FAIL — 活规则仍残留旧 `planner.md` 路径

**证据：**

- `.auto-memory/role-context/planner.md`
- `framework/harness/planner-workflow.md`
- `framework/harness/planner-arbitration.md`
- `framework/harness/planner-checklists.md`

这些文件仍直接出现 `framework/harness/planner.md` 字面路径；其中前者属于活 role-context，后三者属于当前生效的 framework 规则文件。

**为什么是 blocker：**

F003/F010 的 acceptance 都要求：

- 活规则 cross-reference 应迁到新 3 子文件
- `grep "framework/harness/planner.md"` 仅命中 archive / CHANGELOG / 历史保留

当前实际不满足这个口径，因此 F003 不能判 PASS。

### 2. F002 FAIL — cowork 清理仍残留活状态文件命中

**证据：**

- `.auto-memory/project-status.md`

当前 `grep -rln -i "cowork" framework/ .auto-memory/ --include="*.md" --include="*.sh"` 仍命中该活状态文件。

**为什么是 blocker：**

F002/F010 的验收口径要求 cowork grep 只剩历史保留命中；`.auto-memory/project-status.md` 是当前运行态记忆文件，不属于历史保留，因此 F002 不能判 PASS。

## 判定

### Feature 结果

- `F001` PASS
- `F002` FAIL
- `F003` FAIL
- `F004` PASS
- `F005` PASS
- `F006` PASS
- `F007` PASS
- `F008` PASS
- `F009` PASS
- `F010` 未完成（因 blocker 中止 signoff）

### 建议修复

1. 清理 `.auto-memory/project-status.md` 中的 `cowork` 活引用，保留历史信息到 CHANGELOG / archive 即可。
2. 把活规则里所有旧 `framework/harness/planner.md` 字面路径改成：
   - `planner-workflow.md`
   - `planner-arbitration.md`
   - `planner-checklists.md`
   或改成不含旧路径字面的说明文本。
3. 修完后重跑：
   - `grep -rln "framework/harness/planner.md" framework/ .auto-memory/ docs/specs/ docs/dev/ --include="*.md"`
   - `grep -rln -i "cowork" framework/ .auto-memory/ --include="*.md" --include="*.sh"`
   - `bash framework/bootstrap.sh` temp bootstrap

