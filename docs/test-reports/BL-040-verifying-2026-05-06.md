# BL-040 Product targetAudience Required Verifying 2026-05-06

> 状态：**Verifying**
> 触发：复核 BL-040 `targetAudience` 必填收紧

## 结论

- L1 通过。
- 本地浏览器 smoke 通过。
- 当前 staging 仍受已知 app-role password gap 影响，未能做实时 L2 线上交互，只能用本地 3099 + 源码/测试证据补足验收。

## 验收结果

### F001 / DB + 全栈类型清理

**结果：** 通过

**证据：**
- `generateAiAssets.ts` 的 prompt 已直接使用 `input.targetAudience`，不再拼接 `Not specified`。
- server action 已直接传递 `data.targetAudience`。
- DTO 类型已为 `string`。

### DB contract

**结果：** 通过

**证据：**
- `tests/integration/product-targetaudience-required.test.ts` 通过，覆盖 raw NULL insert 被 DB 拒和 Zod 拒空字符串。

### 表单 smoke

**结果：** 通过

**证据：**
- 本地 `/zh/knowledge-base` 打开“录入新产品”弹窗后，空提交会把 `targetAudience` 标红并显示“请填写目标受众。”

## L1

```text
npm run test:integration -- tests/integration/product-targetaudience-required.test.ts tests/integration/product-flow.test.ts
Test Files  2 passed (2)
Tests       11 passed (11)

npx vitest run src/lib/products/__tests__/generateAiAssets.test.ts
Test Files  1 passed (1)
Tests       14 passed (14)

npm run lint
0 errors / 3 warnings

npm run typecheck
pass
```

## L2 / 线上约束

| 项 | 结果 |
|---|---|
| staging health | 仍受已知 app-role password gap 影响，当前不可用 |
| 替代验证 | 本地 3099 页面 smoke + 源码行级核对 |

## 软约束

- staging 未能执行真实浏览器交互，不作为本次功能失败处理。

