# BL-080 Landing 插画集成 Signoff 2026-06-09

> 状态：**PASS (fix_rounds=0)** — verifying→done
> Evaluator：Reviewer (Codex)

---

## 变更背景

Landing page v2：用 AI 插画替代真实产品截图（Hero + 6 Features + EmailCenter + BeforeAfter）。2026-06-01 暂停（等用户 AI gen），2026-06-08 恢复。Planner 代生成 8 PNG（aigc-gateway gemini），Generator 完成 F002-F005 集成+优化+验证。

---

## Feature 评估

### F001 — 8 AI prompts
**Executor：** generator (planner preempt) | **Assessment：PASS**
- `docs/specs/BL-080-illustration-prompts.md`，含 brand colors / composition / mood / avoid spec

### F002 — 8 PNG 验收
**Executor：** generator | **Assessment：PASS**
- 8/8 PNG 就绪 `public/landing/illustrations/`，规范命名，品牌一致（navy+cyan+purple）

### F003 — 集成 + fallback
**Executor：** generator | **Assessment：PASS**
- Hero：插画替换 video 背景（`landing-hero-illustration` testid）
- Features：5 cards 映射 feature-{brief,match,reach,insight,crm}
- EmailCenter：`email-center-illustration.png`
- BeforeAfter：插画替换动画表格（转 server component）
- Fallback：`illustration-asset.ts` server `existsSync` 守门
- i18n：3 `illustrationAlt` × 5 locale 真译

### F004 — next/image 优化
**Executor：** generator | **Assessment：PASS**
- Hero quality=85+sizes, Features/Demo/BeforeAfter quality=80+lazy
- next.config `images.qualities=[75,80,85]`
- Generator 实测 LCP 870ms（perf 99）

### F005 — baseline + a11y
**Executor：** generator | **Assessment：PASS**
- Lighthouse 6 门全过（perf≥85, LCP<2.5s, CLS<0.05, TBT<200ms, SEO≥90, a11y≥90）
- a11y 100：alt/contrast/focus 全过
- reduced-motion：静态插画天然适配
- Baseline：4 整页（en/zh desktop/mobile），CI 绿

### F006 — Codex L1+L2 + signoff
**Executor：** codex | **Assessment：PASS**

---

## L1 自动化验证

| 检查项 | 结果 |
|---|---|
| `npm run lint` | **0 errors, 3 pre-existing warnings** |
| `npx tsc --noEmit` | **PASS** |
| `npm run test` | **207 files, 1542 tests PASS** |
| `npm run build`（NODE_OPTIONS=4096） | **SUCCESS** |
| PNG ≥6/8 | **8/8** ✅ |
| illustrationAlt × 5 locale | **3 keys × 5 locale 完整** ✅ |
| e2e hero assertion | 更新为 `landing-hero-illustration` ✅ |
| fallback guard | `illustration-asset.ts` existsSync ✅ |

## L2 staging 验证

| 检查项 | 结果 | 证据 |
|---|---|---|
| staging health | **healthy** ✅ | `curl /api/health` |
| landing page (/en) | **200** ✅ | |
| 8 illustrations in HTML | **all present** ✅ | hero(18)+features(10-12)+email(10)+before-after(12) |
| CTA → /request-access | **confirmed** ✅ | multiple instances in HTML |
| CTA → /login | **confirmed** ✅ | |
| e2e test IDs | `landing-hero`, `landing-hero-illustration`, `landing-cta-{primary,secondary}` 全在 ✅ |
| Lighthouse（Generator 实测） | perf 99, LCP 870ms, CLS 0, TBT 0, SEO 92, a11y 100 ✅ |
| reduced-motion | 静态插画天然适配（video 已移除） ✅ |

> PageSpeed API 无法访问 staging（非公开），接受 Generator 独立浏览器 Lighthouse 实测数据。

---

## 未变更范围

| 事项 | 说明 |
|---|---|
| CTA 链路 | /request-access, /login 保持 |
| sitemap / canonical | 不变 |
| BL-078 视觉 token | 不变 |
| 业务路径 | 零破坏 |

---

## Soft-watch

| ID | 描述 | 风险 |
|---|---|---|
| S1 | 3 pre-existing lint warnings | low |
| S2 | beforeAfter dead i18n keys（`colTask,colBefore,colAfter,rows.*`）可后续清 | low |
| S3 | illustrations source 8.8MB（next/image 运行时压缩至 ~105KB） | low |
| S4 | Safari/Firefox 跨浏览器渲染未独立验证（Generator Chrome实测 + 静态插画天然一致） | low |

---

## Harness 说明

首轮 PASS (fix_rounds=0)。Signoff：`docs/test-reports/BL-080-signoff-2026-06-09.md`
