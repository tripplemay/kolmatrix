# BL-081 Verifying 2026-06-04

> 状态：`verifying` 进行中
> 范围：BL-081 `F006` Reviewer 首轮验收（L1 本地 + L2 staging + F004 audit 复核）

---

## 结论

- 当前结论：**PARTIAL（主要阻塞收敛为 24h 时间窗证据）**
- 已确认：
  - L1 自动化基线通过
  - staging 健康、登录、`/match` 主工作台、region filter 均正常
  - F004 audit report 可读且具备可执行建议
- 未完成：
  - 24h `daily LLM call < 200/day` 时间窗证据尚未形成
  - staging / prod DB 级 `country_enrichment_attempted_at` 抽样无只读查询通道，暂以自动化 + handoff 旁证

---

## L1 结果

环境前置：

- `.nvmrc` = `20`
- 本机默认 `node -v` = `v25.7.0`
- 按 evaluator 规则切换到 `node v20.20.2`

命令结果：

- `npx prisma generate` PASS
- `npx prisma validate` PASS
- `npx tsc --noEmit` PASS
- `npm run lint` PASS（`0 error, 3 warnings`）
- `npm test` PASS（`192 files, 1405 tests`）

Lint warnings（soft-watch，不阻断本轮）：

1. `src/app/api/health/__tests__/route.test.ts:18` `afterEach` unused
2. `src/lib/queue/explain-recommendations-worker.ts:81` `_ctx` unused
3. `tests/integration/db-platform-admin-nullif.test.ts:13` `beforeEach` unused

判定依据：均为既有 `@typescript-eslint/no-unused-vars`，符合 evaluator §10.3 `0 errors + <=3 unused-import-style warning` 的 soft-watch 条件。

---

## F002 Migration 复核

文件：

- `prisma/schema.prisma`
- `prisma/migrations/20260601030000_bl_081_add_country_enrichment_attempted_at/migration.sql`

复核结果：

- schema 已包含 `countryEnrichmentAttemptedAt DateTime? @map("country_enrichment_attempted_at") @db.Timestamptz`
- migration 已包含：
  - `ADD COLUMN "country_enrichment_attempted_at" TIMESTAMPTZ`
  - `CREATE INDEX "kol_tenant_country_attempted_idx"`
  - ROLLBACK 注释块：
    - `DROP INDEX IF EXISTS "kol_tenant_country_attempted_idx";`
    - `ALTER TABLE "kol" DROP COLUMN IF EXISTS "country_enrichment_attempted_at";`

判定：PASS

---

## L2 结果（staging）

环境：

- `https://staging.kol.guangai.ai`

健康检查：

- `GET /api/health` → HTTP 200
- body:
  - `status = "healthy"`
  - `database.status = "ok"`
  - `redis.status = "ok"`
  - `kol_coverage.total_active_kols = 4519`
  - `kol_coverage.country_fill_rate = 0.5855`

Playwright staging 手验：

1. `tests/e2e/marketer.setup.ts` PASS
   - staging marketer 登录态可建立
2. `tests/e2e/login-cinematic.spec.ts`
   - `logs in via /en/login ...` PASS
3. `tests/e2e/match-flow.spec.ts`
   - `card view mounts the grid + at least one KOL card` PASS
   - `filter sidebar exposes status pills` PASS

补充浏览器脚本证据：

1. `/en/match` 首屏抽样 8 张卡片，已看到真实 country badge：
   - `PE`
   - `GB`
   - `IN`
2. `/en/match?regions=IN` 抽样结果：
   - 返回 `2` 张卡片
   - 抽样卡片均显示 `IN`

判定：

- `country_code` 已在 staging UI 消费并可见：PASS
- `/match` region filter 基于 country 维度工作：PASS
- 下游 match 页面未因 BL-081 回归：PASS

---

## L2 结果（prod，只读）

环境：

- `https://kol.guangai.ai`

健康检查：

- `GET /api/health` → HTTP 200
- body:
  - `status = "healthy"`
  - `database.status = "ok"`
  - `redis.status = "ok"`
  - `kol_coverage.total_active_kols = 2383`
  - `kol_coverage.country_fill_rate = 0.0399`

来自最新 `generator_handoff` 的 prod 交接事实（已写入 `progress.json`）：

- prod 已部署到包含 BL-081 的版本
- migration 已应用
- prod F005 backfill 已执行：
  - dry-run `2288`
  - apply stamped `2288`
  - re-run `0`
- 交接声称 prod `country-null` 通过 F003 gate 已从 `2288 -> 0`

判定：

- prod deploy 已完成：PASS
- prod 健康态正常：PASS
- prod backfill 已完成：以状态机 handoff 旁证记 PASS
- prod 24h 降幅：PENDING（需时间窗）

---

## F004 Audit Report 复核

文件：

- `docs/audits/BL-081-refresh-selector-audit.md`

复核点：

- 明确区分“audit only / no fix implemented”：PASS
- 根因是否可定位到代码而非猜测：PASS
  - `scripts/kol-sync-daily.ts` 中 `refreshCount: 0` 硬编码
  - `runDaily()` 未接 refresh path
  - `refresh-selector.ts` 生产调用断开
- 是否给出可执行候选方向：PASS
  - Dir A / B / C 均给出范围与代价
- 是否有 live verification 补充：PASS
  - §5.5 已记录 O1-O3 读数与修正后的结论

判定：PASS

---

## 尚未完成的验收项

1. `country_enrichment_attempted_at` staging / prod DB 抽样 >=5
   - 当前阻塞：无 staging 只读 SQL 通道 / 无现成 API 暴露该字段
   - 现有旁证：
     - `src/lib/kol-sync/__tests__/enrichment-stage.test.ts` 已在 L1 通过
     - Generator handoff 记录 staging backfill `eligible 1873 -> 0`
     - Generator handoff 记录 prod backfill `2288 -> 0`

2. `daily LLM call < 200/day` 的 prod 24h 实测
   - 当前阻塞：需要下一次 daily cron / 24h 观察窗

3. 基于 prod 的最终成本量化
   - 依赖 24h 观察窗

4. signoff
   - `docs.signoff` 仍为空
   - 当前不能置 `done`

---

## 当前建议

下一步不是修代码，而是补验收前置：

1. 观察下一次 daily cron / 24h `aigateway` 与 daily sync 指标
2. 如可取得只读 SQL/日志证据，补 `attempted_at` 抽样与 `<200/day` 实数
3. 复做 F006 收口，产 `BL-081-signoff-YYYY-MM-DD.md`

在以上前置完成前，本批次应保持 `verifying`，不应切 `done`。
