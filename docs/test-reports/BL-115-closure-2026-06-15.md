# BL-115 结案记录 — 落地页折中加装(投放规范对齐)

> **状态：** DONE（用户分批逐个确认 + 手工免验收，Codex F005 免除）
> **日期：** 2026-06-15
> **性质：** 非 Codex signoff。用户全程**分批检查点逐个确认每个 feature**（提交史为证: 每 feature→staging→⏸️待确认→下一个），最终手工免验置 done（对自有 harness 行使权限）。

## 用户指令
"BL-115 手动免验 done + 开 BL-117 再平衡"（2026-06-15）。

## 交付（F001-F004 generator 全 done，用户逐个 staging 确认）
- **F002** Hero 邮件协作定位 + 4 数据栏（9196ba7）
- **F001** 转化机制：Lead 表 migration + TrialLeadCta 模态 + UTM cookie/捕获 + 埋点 LandingAnalytics + /api/landing-event→event_log + PRD 占位/演示链接（9b84d5f）
- **F003** 痛点 4 卡 + EmailCenterDemo 3 面板（9954338）
- **F004** 信任真实框架（genre 垂类 + 安全 badge + 证言占位，去假 wordmark）+ 邮件 FAQ + SEO（de2cfe4）
- baseline 经 update-visual-baselines workflow 重拍（9ea2c73）。

## 已验证
- ✅ **用户分批逐个确认**每个 feature staging 效果（检查点节奏全程执行）。
- ✅ **main CI 全绿**（run 27488383379: lint 0e/3w · tsc=0 · npm test 1705 · build · E2E · visual baseline）。
- ✅ **转化数据路径 Planner sanity check 为真**（非 stub）: Lead 表 migration `20260614020000_bl115_f001_lead_table` + 完整 UTM 字段 + `/api/landing-event` 端点 + 表单 action/attribution + 单测。
- ✅ staging @9ea2c73 healthy。

## 未验证（F005 免除，soft-watch）
- ⚠️ **转化机制端到端**（真实提交→lead 落库 + UTM + event_log）未跑实测 —— 投放数据命脉。**建议正式投放前用真实表单提交跑一次端到端确认**(数据路径已 sanity check 为真，风险低但投放依赖它)。
- ⚠️ **PRD 链接 `kol.saga1001.com/prd`** 仍为占位常量(landing-links.ts)，**部署前须确认可访问/给真实地址**。
- ⚠️ Lighthouse perf99 / WCAG AA / reduced-motion(BL-080 硬约束)未显式再验，建议 prod 前后抽查。
- ⚠️ prod migration(lead 表)随部署 `prisma migrate deploy` 落地，部署后确认。

## 真实性留痕(spec §1，用户决，跟进 BL-116)
1000+ 模板用文档口径(真 27) · 回复率全程不提(repliedAt 从不写) · 游戏 IP 用 genre 垂类非客户背书 · 证言占位待真实来源。

## 后续
- **BL-117 定位再平衡**(主站回调"全球 KOL 智能营销"广义定位) —— 本批的邮件侧重将被再平衡。
- **待 prod 部署**(先定 PRD 链接 + 端到端表单确认)。
