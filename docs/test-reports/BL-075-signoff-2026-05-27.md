# BL-075 Signoff Report — 2026-05-27

## 结论

- 批次：`BL-075-kol-data-coverage`
- 结果：`PASS`
- Reviewer：`codex: Reviewer`
- 阶段：`reverifying`
- fix rounds：`1`

BL-075 已完成终签。上轮唯一 blocker 是 staging `daily sync` upstream 不健康；fix-round 1 已恢复链路并通过复验。

## L1 结果

### 1. `npm run lint`

结果：

```text
0 errors / 3 warnings
```

warnings：

- `src/app/api/health/__tests__/route.test.ts:18:20`
- `src/lib/queue/explain-recommendations-worker.ts:81:3`
- `tests/integration/db-platform-admin-nullif.test.ts:13:31`

判定：`PASS`

### 2. `npx tsc --noEmit`

结果：PASS

判定：`PASS`

### 3. `npm test`

结果：

- `188 files`
- `1368 tests`
- 全部通过

判定：`PASS`

备注：比首轮多 `3` 个 parseArgs / `--enrichment-limit` 相关用例，和 fix-round 1 handoff 一致。

### 4. backfill dry-run

命令：

```bash
npx tsx scripts/kol-enrichment-backfill.ts --dry-run
```

结果：

```text
[kol-enrichment-backfill] starting (dryRun=true tenant=ALL concurrency=5 limitPerTenant=ALL llmGapMs=default)
[kol-enrichment-backfill] WARN — AIGCGATEWAY_KOL_COUNTRY_ACTION_ID is not set. Country LLM fallback will be skipped; only audience-geo top-1 + language will run.
[kol-enrichment-backfill] BEFORE: total=12 country_filled=12 (100.0%) language_filled=12 (100.0%)
[kol-enrichment-backfill] no tenants have NULL country/language KOLs — exiting clean
```

判定：`PASS`

### 5. `.auto-memory/environment.md`

结果：

- action 清单仍含 `kol-country-enrichment`
- action id 仍记录为 `cmpm3pr2e0011bno3f1vd4v9r`

判定：`PASS`

## L2 结果

### 1. prod `/api/health`

结果：

```json
{
  "kol_coverage": {
    "total_active_kols": 1397,
    "country_fill_rate": 0.2040085898353615,
    "language_fill_rate": 0.45454545454545453
  }
}
```

判定：`PASS`

说明：

- `fill_rate` 仍低于 spec 里的 best-effort 记录线（language 60% / country 30%），但 spec 已明确“不设硬阈”
- 当前数字与 [BL-075-backfill-2026-05-26.md](/Users/yixingzhou/project/joyce/docs/test-reports/BL-075-backfill-2026-05-26.md) 的 after-numbers 一致

### 2. staging `/zh/match` coverage hint

结果：

- 页面显示：
  - `地区 已覆盖 68%`
  - `语言 已覆盖 1%`
- country / language 没有 disable
- 页面继续返回 KOL 结果列表

判定：`PASS`

### 3. staging `/zh/match?regions=US`

结果：

- 已选筛选中出现 `美国`
- 结果总数：`1,594`
- 首屏结果中可见明确 `US` KOL
  - `Soul Fox Gaming`
  - `Streamer Showdown`
  - `USU Esports`

判定：`PASS`

### 4. backfill accuracy 样本

证据来源：

- [BL-075-backfill-2026-05-26.md](/Users/yixingzhou/project/joyce/docs/test-reports/BL-075-backfill-2026-05-26.md) §3

报告记录：

- `30` 行人工抽样
- `0 obvious false-positive country mis-tags`
- language fills 全部准确
- country fills：
  - `6` 行明确正确
  - `2` 行 defensible
  - `3` 行 borderline
  - `0` 行 outright wrong

判定：`PASS`

### 5. staging `daily sync` enrichment stage

fix-round 1 复验命令：

```bash
ssh tripplezhou@34.180.93.185 \
  'cd /opt/kolmatrix-staging && set -a && source .env.staging && set +a && \
   AI_DAILY_COST_USD_PER_TENANT_MAX=500 \
   npx tsx scripts/kol-sync-daily.ts --enrichment-limit=10'
```

本轮实测结果：

```text
[kol-sync-daily] starting (dryRun=false)
[enrichment-stage] tenant=753ee82c-0a5b-4795-859a-b56d97be56e5 scanning 10 KOLs (concurrency=5, llm_gap_ms=2100, dryRun=false)
[enrichment-stage] tenant=753ee82c-0a5b-4795-859a-b56d97be56e5 DONE scanned=10 lang+=2 country+=2 llm_calls=3 cost_est=$0.0027 failed=0
[kol-sync-daily] DONE — report: /opt/kolmatrix-staging/docs/test-reports/kol-sync-daily-2026-05-26.md
[kol-sync-daily] level=WARN summary: discover=2567 inserted=0 updated=0 errors=1 quota_est=1
```

同时，最新 daily report 末尾包含：

- `Adapter health`
  - `apify-kol: healthy`
- `Discover`
  - `Total raw rows: 2567`
  - `Failed adapters: 0`
- `Enrichment stage`
  - `Scanned: 10`
  - `failed=0`

判定：`PASS`

说明：

- 这已经足够关闭上轮 blocker：staging 不再在 discover 前 bail，enrichment stage 确实被触发
- fix-round 1 报告还额外记录了 `5 audit_log rows` 的 SQL 证据；本轮未独立重跑这条 SQL，但结合真实复跑输出和 `failed=0`，可接受放行

### 6. residual warning: discover-import numeric overflow

本轮 daily-sync 仍有：

```text
discover-import[apify-kol]:
Invalid prisma.kol.upsert() invocation:
Value out of range for the type: numeric field overflow
```

判定：`soft-watch`

原因：

- 这条发生在 discover-import 路径
- 与 BL-075 需要验收的 enrichment-stage / coverage hint / fill-rate 链路解耦
- 已在 Generator handoff 和 fix-round report 中明确标成独立 hotfix 候选

## 最终判定

### PASS

- `F001`
- `F002`
- `F003`
- `F004`
- `F005`
- `F006`
- `F007`

### 批次状态

- `progress.json.status = done`
- `completed_features = 7`
- `docs.signoff = docs/test-reports/BL-075-signoff-2026-05-27.md`

## 备注

- 当前仍有 3 个历史 lint warning，维持 soft-watch，不阻断 `done`
- `discover-import[apify-kol] numeric field overflow` 建议单独起后续 hotfix，不应回流阻断 BL-075 终签
