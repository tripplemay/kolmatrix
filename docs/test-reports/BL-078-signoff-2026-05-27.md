# BL-078 signoff — PASS

> Sprint: `BL-078-landing-visual-polish`
> Reviewer: `codex: Reviewer`
> Date: `2026-05-27`
> HEAD: `fb34b09`

## Verdict

`PASS`

BL-078 can close as `done`. The only prior blocker was landing-page text contrast on staging; fix-round 1 removes that regression without breaking CTA flow, locale rendering, reduced-motion fallback, or Lighthouse performance.

## L1

All required local checks passed:

- `npm run lint` → `0 errors / 3 warnings` (same baseline warnings as before)
- `npx tsc --noEmit` → PASS
- `npm test` → `189 files / 1375 tests PASS`
- 11 landing component files remain present and exported
- `messages/zh|en|ja|ko|es.json` add no new i18n keys in this batch

## L2

### Staging landing path

Command:

```bash
E2E_BASE_URL=https://staging.kol.guangai.ai npx playwright test tests/e2e/landing.spec.ts --project=chromium --no-deps
```

Observed result:

```text
7 passed
```

This covers:

- root locale redirect
- `/zh` and `/en` landing render
- hero video attributes
- poster fetch
- primary CTA → `/request-access`
- secondary CTA → `/request-access?demo=1` with demo intent preserved

### Lighthouse

Command:

```bash
npx lighthouse https://staging.kol.guangai.ai/zh --chrome-flags='--headless=new --no-sandbox' --only-categories=performance,accessibility,seo,best-practices --preset=desktop
```

Observed summary:

```json
{
  "performance": 0.99,
  "accessibility": 1,
  "seo": 0.58,
  "bestPractices": 0.96,
  "lcp": 530,
  "cls": 0,
  "tbt": 0,
  "contrastScore": 1,
  "contrastCount": 0
}
```

Acceptance notes:

- perf gate passes comfortably
- the prior contrast blocker is resolved: `contrastCount 13 -> 0`
- staging SEO remains low because of intentional staging-only `noindex` / robots / canonical posture; this is not a product blocker for BL-078

### Browser / locale / reduced-motion matrix

Spot checks passed in `chromium`, `webkit`, and `firefox` with `prefers-reduced-motion=reduce` for `zh / en / ja / ko / es`:

- hero remains visible
- primary CTA href stays `/<locale>/request-access`
- secondary CTA href stays `/<locale>/request-access?demo=1`
- no horizontal overflow observed
- navigation remains intact in fallback browsers

### Accessibility sampling

Keyboard and focus-visible sampling on staging `/zh` passed:

- `Tab` reaches top-nav actions and hero CTAs in expected order
- visible outlines/glow remain present on primary interactive targets
- no broken focus trap or hidden CTA was observed

## Design assessment

The landing now matches the locked visual direction well enough to sign off:

- Linear-style whitespace and restrained chrome are preserved
- Plausible-like clarity and card simplicity land without changing structure or copy
- motion remains subtle and degrades correctly under reduced-motion and fallback browsers

## Conclusion

BL-078 is accepted:

- contrast regression fixed
- no CTA or request-access path break
- no locale overflow regression found in sampled locales
- no Lighthouse performance regression
- no additional reviewer blocker remains
