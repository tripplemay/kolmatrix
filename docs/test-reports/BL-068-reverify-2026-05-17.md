# BL-068 Reverify 2026-05-17

> 状态：**FAIL**
> 执行者：Codex Reviewer
> 环境：`https://staging.kol.guangai.ai`
> 批次：`BL-068-conversational-refine`
> 轮次：`fix-round 1`

## 测试范围

- staging 部署 sha 与健康状态
- `/campaigns/[id]` refine 主链复验
- `/match?campaignId=` 与 `zh` locale 挂载复验
- refine request body 是否发送完整当前池
- `scripts/bl068-cost-audit.ts --hours=24` 与 SQL 一致性
- parse success rate gate（目标 `>= 80%`）

## 使用的源文档

- `docs/specs/BL-068-conversational-refine-spec.md`
- `progress.json`
- `scripts/bl068-cost-audit.ts`
- `docs/test-reports/BL-068-staging-spot-check.md`

## 覆盖摘要

- PASS: staging 当前部署 sha 已对齐 `main` `9c90f9b`
- PASS: staging `/api/health` 返回 healthy
- PASS: `/campaigns/[id]`、`/match?campaignId=`、`zh` locale 仍能挂载 RefineInputBar
- PASS: 5s timeout 覆盖真实结果的问题已消失；en 实测返回的是 server action 的真实 `permutation_invalid`
- PASS: request body 已发送完整当前池，而不是仅 visible 5；本次 payload 长度为 29，和页面当前池一致
- PASS: `scripts/bl068-cost-audit.ts` 已能与 SQL 对齐，不再出现假阴性 `0 calls`
- FAIL: staging 真实 parse success rate 只有 `25.00% (1/4)`，未达到 signoff gate `>= 80%`
- FAIL: en 真实 refine 仍出现 `permutation_invalid`，最新审计记录显示 `expected_count=29`、`returned_count=31`

## 结构化测试用例

| ID | 用例 | 结果 | 证据 |
|---|---|---|---|
| T1 | staging 健康检查 | PASS | `curl https://staging.kol.guangai.ai/api/health` 返回 `healthy` |
| T2 | staging 部署 sha 与当前 `main` HEAD 一致 | PASS | SSH `/opt/kolmatrix-staging` `git rev-parse HEAD` = `9c90f9b91479a7acb306ac766568c78909fa3f73` |
| T3 | `/campaigns/[id]` 挂载 refine input | PASS | `/en/campaigns/382f...` 可见 `textbox "Refine with AI: ..."` |
| T4 | `/match?campaignId=` 复用挂载 | PASS | `/en/match?campaignId=382f...` 可见同一 refine input |
| T5 | `zh` locale 文案挂载 | PASS | `/zh/campaigns/382f...` 可见 `textbox "用 AI 微调：例如减少 micro tier、多加女性受众"` |
| T6 | refine 主路径不再被 timeout 覆盖 | PASS | network `reqid=49` 返回 200，UI 显示 `Rerank result was invalid. Current pool unchanged.`，未再出现 timeout toast |
| T7 | refine 请求使用完整当前池 | PASS | network `reqid=49` request body `currentPoolIds` 长度为 29；页面显示 `Visible: 5 / 29` |
| T8 | 24h audit script 与 SQL 一致 | PASS | 脚本输出 `calls=4 / applied=1`；SQL 直查 `total_calls=4 / applied_calls=1` |
| T9 | parse success rate 达到 `>=80%` | FAIL | `npx tsx scripts/bl068-cost-audit.ts --hours=24` 输出 `25.00% — FAIL` |
| T10 | en refine 常见 query 能稳定成功 | FAIL | `fewer micro creators, more female audience in Japan` → `ai_recommendation.refine_permutation_invalid`，`expected_count=29 returned_count=31` |

## L2 实测记录

| 项 | 证据 |
|---|---|
| Staging health | `{"status":"healthy","uptime_seconds":681,"checks":{"database":{"status":"ok"},"redis":{"status":"ok"}}}` |
| Staging git sha | `9c90f9b91479a7acb306ac766568c78909fa3f73` |
| en refine request body | `currentPoolIds` 含 29 个 id，不再是 5 个 visible id |
| en refine response | network `reqid=49`：HTTP 200，body=`{ ok: true, data: { unparsable: true, capExhausted: false, errorKind: "permutation_invalid" } }` |
| en UI result | 页面状态文本为 `Rerank result was invalid. Current pool unchanged.` |
| permutation audit payload | latest `ai_recommendation.refine_permutation_invalid`：`raw_query="fewer micro creators, more female audience in Japan"`, `expected_count=29`, `returned_count=31` |
| zh sample | latest `ai_recommendation.refine_applied`：`raw_query="减少 micro 类创作者，多一些日本女性受众"`，`parsed_filters.locale="ja"`，`audience_gender="female"` |
| Audit script | `TOTAL calls: 4`, `refine_applied / total: 1 / 4`, `rate: 25.00% — FAIL` |
| SQL truth | `SELECT COUNT(*) total_calls, SUM(...) applied_calls ...` → `4 / 1` |

## 缺陷列表

### B5. Parse success rate 仍显著低于验收门槛，当前 staging 只有 25%

- 严重级别：High
- 复现步骤：
  1. 在 `/en/campaigns/382f014c-a9f9-4fde-bcad-d5bb10ed2045` 输入 `fewer micro creators, more female audience in Japan`
  2. 点击 `Refine`
  3. 运行 `npx tsx scripts/bl068-cost-audit.ts --hours=24`
- 实际结果：
  - 最新 24h 统计 `refine_applied / total = 1 / 4`
  - parse success rate `25.00%`
- 预期结果：
  - roadmap / F007 gate 要求 `>= 80%`

### B6. en refine 仍会触发 permutation invalid，返回 31 个结果给 29 个当前池

- 严重级别：High
- 复现步骤：
  1. 打开 `/en/campaigns/382f014c-a9f9-4fde-bcad-d5bb10ed2045`
  2. 输入 `fewer micro creators, more female audience in Japan`
  3. 点击 `Refine`
- 实际结果：
  - server action 返回 `errorKind="permutation_invalid"`
  - audit_log 记录 `expected_count=29`、`returned_count=31`
  - UI 提示 `Rerank result was invalid. Current pool unchanged.`
- 预期结果：
  - LLM 输出应通过 strict permutation 验证，返回与当前池等长且同集合的重排结果

## 结论

- 本轮 `reverifying` 结论：**FAIL**
- fix-round 1 已验证修复了部署 sha、timeout 竞争、full-pool request body、audit script 一致性
- 但当前仍不满足 signoff 条件，核心阻塞是 **真实 parse gate 未达标**
- 建议状态切换：`reverifying -> fixing`
- `docs.signoff` 继续保持 `null`

## 修复建议

1. 继续追 `permutation_invalid` 根因，重点检查 29 条当前池场景下 prompt / server 解析 / 结果裁剪逻辑。
2. 用 staging 重新跑一组不少于 10 条的 dogfood query，覆盖 en/zh、tier、audience、locale，确认成功率能稳定拉到 `>=80%`。
3. 修复后重新部署 staging，再由 Reviewer 做第二轮复验。
