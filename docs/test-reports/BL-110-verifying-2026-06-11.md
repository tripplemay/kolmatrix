# BL-110 verifying report

- Scope
  - BL-110 split-brain quickwins wave 1
  - F001 breadcrumb `/kols/[id] -> /match`
  - F002 `/assets` listable type whitelist + welcome gating
  - F003 accepted-KOL read path excludes `skipped/swapped`
  - F004 reply-tracking honest empty state

- L1
  - `pnpm exec eslint` -> 0 error / 3 warning
  - `pnpm exec tsc -p tsconfig.json --noEmit` -> PASS
  - `npm test` -> 1624 / 1624 PASS

- Local evidence
  - Seeded local DB and added evaluator-only fixture data.
  - F001 PASS: `/en/kols/f8e0ce25-d256-4df4-a036-72e5fef468d1` breadcrumb href is `/en/match`; click lands on `/en/match`.
  - F002 PASS:
    - main tenant `/en/assets` shows 21 visible cards, injected explanation asset `BL110 hidden explanation asset` stays hidden
    - explanation-only tenant shows `assets-welcome-banner`, and injected explanation asset stays hidden
  - F003 PASS: local campaign `53c923c7-f887-4307-8dfb-6b7250ccd110` renders accepted count `2`; only `GamerXia` + `SakuraYT` appear; `NeonHaze` / `KaiBytes` do not.
  - F004 pending-path PASS on local reply-pending tenant:
    - `/en/reach` reply KPI shows `—` + `Reply tracking coming soon (B4)`
    - `/en/reach` recent replies card shows pending copy
    - `/en/reach/tracking` shows reply-tracking footnote
    - `/en/insight` dashboard shows reply pending note

- Staging / prod read-only evidence
  - Staging F001 PASS: `/en/kols/662501e6-42eb-4f71-946f-2f1acdbc1c41` breadcrumb href is `/en/match`; click lands on `/en/match`.
  - Prod F001 PASS: `/en/kols/1de5eafe-d86b-4e24-a1f4-f55398432be7` breadcrumb href is `/en/match`; click lands on `/en/match`.
  - Staging F002 PASS: `/en/assets` shows 24 cards; `explain-detailed/...` cache assets do not surface in the grid.
  - Prod F002 PASS: `/en/assets` shows 24 cards; `explain-detailed/...` cache assets do not surface in the grid.
  - Staging F003 PASS on real data: campaign `2d11dd71-1a98-4ee0-b15d-314dae9fcd3c` (`Genshin Impact — SEA`) has one accepted and one skipped DB row; UI accepted count is `1`, accepted panel shows only `제미니의 게임채널`, and excludes skipped row `왕군의 게임 실험`.

- Blocking finding
  - F004 FAIL on staging dashboard.
  - Repro:
    1. Login to `https://staging.kol.guangai.ai/en/login` as `admin@kolmatrix.local`
    2. Visit `/en/reach`
    3. Observe reply KPI = `18.5%`, recent replies pending note absent
    4. Visit `/en/reach/tracking`
    5. Observe 50 tracking rows, reply footnote absent
    6. Visit `/en/insight`
    7. Observe `dashboard-email-perf-reply-note` visible
  - DB cross-check on staging tenant `753ee82c-0a5b-4795-859a-b56d97be56e5`:
    - `email_log.replied_at IS NOT NULL` rows exist (`56` total)
    - reply logs exist historically, so dashboard copy `Reply data is pending inbound email integration (B4).` is false
    - dashboard note is being driven by 14-day chart rows only, not by presence of any real reply data

- Conclusion
  - F001-F003 pass.
  - F004 is not ready for signoff because dashboard can still show the B4 pending note for a tenant that already has real reply data.
  - Batch returns to `fixing`.
