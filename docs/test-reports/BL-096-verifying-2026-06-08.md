# BL-096 爬虫监控页面 Signoff 2026-06-08

> 状态：**PARTIAL (F002 i18n needs fix)** — verifying→fixing
> 触发：F003 Codex L1+L2 验收
> Evaluator：Reviewer (Codex)

---

## 变更背景

两次爬虫静默故障（TikHub 余额耗尽 / aigcgateway 额度拒）靠人肉发现 → 常驻只读监控面板。ADR-017 瘦客户端架构：数据归爬虫（/admin/stats 扩展），KOLMatrix 只调 API 渲染。

---

## Feature 评估

### F001 — /admin/stats 扩展
**Executor：** generator | **Assessment：** PASS
- PR #11 merged→392f154，/opt fork-sync+rebuild confirmed
- /admin/stats 返回 14 字段：drain, igToday, ingestRateByDay, kolsByPlatform, kolsByTier, refreshBacklog, scrapeCompositionToday, tikhubBalanceUsd, ytEmailByStatus, costTodayUsd, apifyCostThisMonthUsd + 3 meta
- 实测返回真实数据（manual_seed inserted 2534 等）

### F002 — /admin/crawler-monitor 页面
**Executor：** generator | **Assessment：** **PARTIAL**

| 检查项 | 结果 | 详情 |
|---|---|---|
| lint | 0e/3w ✅ | pre-existing |
| tsc | PASS ✅ | 0 errors |
| client tests | 6/6 PASS ✅ | fetch/parse/degrade/errors/abort/kind |
| page tests | 4/4 PASS ✅ | banner/gate/redirect/degrade |
| admin gate | 307 ✅ | unauthenticated → login redirect |
| staging health | healthy ✅ | db+redis ok |
| **i18n 5 locale** | **4 FAIL** ❌ | see below |

### F003 — Codex L1+L2
**Executor：** codex | **Assessment：** **VERIFIED (F002 i18n → fixing)**

---

## i18n 缺陷明细

`npm run test` 207 files, **1 failed** (i18n-locale-coverage):

| Locale | 未翻译 leaves | 具体 key |
|---|---|---|
| **zh** | 1 | `admin.crawlerMonitor.health.instagram: "Instagram"` |
| **ja** | 1 | `admin.crawlerMonitor.health.instagram: "Instagram"` |
| **ko** | 1 | `admin.crawlerMonitor.health.instagram: "Instagram"` |
| **es** | 3 | `admin.crawlerMonitor.health.instagram: "Instagram"`, `healthStatus.ok: "OK"`, `total: "Total"` |

**根因：** KEEP_AS_EN_PATHS 已有 `discovery.platforms.instagram` 和 `match.platforms.instagram`，但缺少 `admin.crawlerMonitor.*` 路径。

**修复方案（任一）：**
- A：在 `tests/unit/i18n-locale-coverage.test.ts` KEEP_AS_EN_PATHS 加 `admin.crawlerMonitor.health.instagram`, `admin.crawlerMonitor.healthStatus.ok`, `admin.crawlerMonitor.total`
- B：在 zh/ja/ko/es 的 `messages/*.json` 中本地化这些值

**严重程度：** low — 全为合理保留英文的 proper noun / universal term。功能测试全绿。

---

## 所有其他检查 PASS

### L1
| 检查项 | 结果 |
|---|---|
| `npx prisma generate` | PASS |
| `npm run lint` | 0 errors, 3 pre-existing warnings |
| `npx tsc --noEmit` | PASS |
| client tests (9) | PASS |
| page tests (4) | PASS |
| 其他 193 files / 1537 tests | PASS |

### L2
| 检查项 | 结果 | 证据 |
|---|---|---|
| /opt SHA | **392f154** ✅ | SSH `git rev-parse HEAD` |
| /admin/stats fields | **14 fields** ✅ | drain/igToday/ingestRate/refreshBacklog/scrapeComposition/ytEmail/balance/cost |
| staging health | healthy ✅ | `curl /api/health` |
| admin gate | **307** ✅ | unauthenticated → login redirect |
| stale /admin/stats degrade | PASS ✅ | client test confirms graceful fallback |

---

## Soft-watch（额外，不阻塞）

| ID | 描述 | 风险 |
|---|---|---|
| S1 | i18n 4 locale 3 leaves → fixing round | low |
| S2 | 3 pre-existing lint warnings | low |

---

## Harness 说明

status → **fixing**：F002 i18n 4 leaves 未翻译（KEEP_AS_EN_PATHS 需更新）。
预期 fix round 1 即可 reverifying→done。
