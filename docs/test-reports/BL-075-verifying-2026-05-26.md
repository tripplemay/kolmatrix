# BL-075 Verifying Report — 2026-05-26

## 结论

- 批次：`BL-075-kol-data-coverage`
- 结果：`FAIL`
- Reviewer：`codex: Reviewer`
- 阶段：`verifying`

BL-075 首轮验收不能 signoff。L1 全部通过，L2 里 prod health 数字、staging `/zh/match` coverage hint、`regions=US` 结果页，以及 backfill accuracy 报告都成立；当前唯一 blocker 是 staging `daily sync` 无法健康跑完，因此 `F003` 的“enrichment stage 触发 + audit_log 写入”链路还不能验收放行。

## L1 结果

### 1. `npm run lint`

结果：

```text
0 errors / 3 warnings
```

warnings：

- `src/app/api/health/__tests__/route.test.ts:18:20`
- `src/lib/queue/explain-recommendations-worker.ts:81:3`
- `tests/integration/db-platform-admin-nullif.test.ts:13:31`

判定：`PASS`

### 2. `npx tsc --noEmit`

结果：PASS

判定：`PASS`

### 3. `npm test`

结果：

- `188 files`
- `1365 tests`
- 全部通过

判定：`PASS`

### 4. backfill dry-run

命令：

```bash
npx tsx scripts/kol-enrichment-backfill.ts --dry-run
```

结果：

```text
[kol-enrichment-backfill] starting (dryRun=true tenant=ALL concurrency=5 limitPerTenant=ALL llmGapMs=default)
[kol-enrichment-backfill] WARN — AIGCGATEWAY_KOL_COUNTRY_ACTION_ID is not set. Country LLM fallback will be skipped; only audience-geo top-1 + language will run.
[kol-enrichment-backfill] BEFORE: total=12 country_filled=12 (100.0%) language_filled=12 (100.0%)
[kol-enrichment-backfill] no tenants have NULL country/language KOLs — exiting clean
```

判定：`PASS`

备注：本地 seed 数据已是 12/12 filled，所以 dry-run 走的是“无待补全行”的正常退出路径。

### 5. `.auto-memory/environment.md`

结果：

- action 清单中存在 `kol-country-enrichment`
- 记录了 action id `cmpm3pr2e0011bno3f1vd4v9r`

判定：`PASS`

## L2 结果

### 1. prod `/api/health`

命令：

```bash
curl -sS -H 'x-health-token: staging-health-check' https://kol.guangai.ai/api/health
```

结果：

```json
{
  "status": "healthy",
  "kol_coverage": {
    "total_active_kols": 1397,
    "country_fill_rate": 0.2040085898353615,
    "language_fill_rate": 0.45454545454545453
  }
}
```

判定：`PASS`

备注：

- 这与 Generator handoff 和 `docs/test-reports/BL-075-backfill-2026-05-26.md` 中的 after-numbers 一致
- 语言 `<60%`、国家 `<30%` 只做记录，不按 spec 当硬失败

### 2. staging `/zh/match` coverage hint

结果：

- 页面显示：
  - `地区 已覆盖 68%`
  - `语言 已覆盖 1%`
- country / language 没有 disable
- 页面仍返回 KOL 结果列表

判定：`PASS`

### 3. staging `/zh/match?regions=US`

结果：

- 已选筛选中出现 `美国`
- 结果总数：`1,594`
- 首屏多张卡片明确显示 `US`
  - 例如 `Soul Fox Gaming`, `Streamer Showdown`, `USU Esports`

判定：`PASS`

### 4. backfill accuracy 样本

证据来源：

- [BL-075-backfill-2026-05-26.md](/Users/yixingzhou/project/joyce/docs/test-reports/BL-075-backfill-2026-05-26.md) §3

报告记录：

- `30` 行人工抽样
- `0 obvious false-positive country mis-tags`
- language fills 全部准确
- country fills 中：
  - `6` 行明确正确
  - `2` 行 defensible
  - `3` 行 borderline
  - `0` 行 outright wrong

判定：`PASS`

### 5. staging `daily sync` enrichment stage + audit_log

命令：

```bash
ssh tripplezhou@34.180.93.185 "cd /opt/kolmatrix && npx tsx scripts/kol-sync-daily.ts --dry-run"
```

结果：

```text
[kol-sync-daily] starting (dryRun=true)
[kol-sync-daily] DONE — report: /opt/kolmatrix/docs/test-reports/kol-sync-daily-2026-05-26.md
[kol-sync-daily] level=WARN summary: discover=0 inserted=0 updated=0 errors=1 quota_est=1
[kol-sync-daily] alerts: discover_count=0 (streak=1/3) | errors=1
```

报告内容：

```text
## Adapter health
- apify-kol: unhealthy {"upstream":"apify-kol-service","error":"apify-kol: response was not valid JSON: Unexpected token '<', \"<!DOCTYPE \"... is not valid JSON"}

## Errors
- all adapters unhealthy — bailing before discover
```

判定：`FAIL`

说明：

- 这不是 lint / unit / backfill 逻辑失败
- 但它确实阻断了 spec 要求的 L2 验收项：
  - 无法证明 staging `daily sync` 真的进入 enrichment stage
  - 无法在 staging 上完成 `kol.enriched` audit_log 写入链路验收

## 最终判定

### PASS

- `F001`
- `F002`
- `F004`
- `F005`
- `F006`

### FAIL

- `F003`
  - 原因：staging `daily sync` upstream 不健康，导致 enrichment stage / audit_log 链路无法在 staging 验证通过

### 未完成

- `F007`
  - 原因：signoff 被 `F003` 阻断

## 建议修复

1. 先恢复 staging `apify-kol` upstream 的健康状态，至少让 `kol-sync-daily.ts --dry-run` 不再在 discover 前 bail。
2. 恢复后重跑 staging `daily sync`，补齐：
   - enrichment stage 实际触发
   - `kol.enriched` audit_log 写入
3. 复验时可复用本轮其余证据：
   - prod `/api/health` 数字
   - `/zh/match` coverage hint
   - `regions=US` 非空结果
   - backfill accuracy 报告
