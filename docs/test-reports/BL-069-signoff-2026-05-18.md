# BL-069 Brief Page Merge Signoff 2026-05-18

> 状态：**PASS**
> 批次：`BL-069-brief-page-merge`
> Evaluator：Codex Reviewer
> 触发：fix-round 1 完成后执行 staging 复验，B1/B2 修复通过且 24h parse gate 仍达标

## 结论

BL-069 本轮复验通过，可以签收。

- B1 已修复：3 条 legacy route 在登录态真实 document 请求中均返回 `301`
- B2 已修复：`BRIEF_FORCE_CAP_EXHAUSTED=true` staging-only 模拟入口可用，toast / 空表单 / 审计 `forced=true` / 清理恢复全通过
- `/brief` 主链路保持可用：恢复后正常 parse 仍成功
- 24h parse success rate：`17 / 21 = 80.95%`，高于 gate `>=80%`

## L1 复验

| 项 | 结果 | 证据 |
|---|---|---|
| Typecheck | PASS | `npm run typecheck` |
| 定向单测 | PASS | `6 files, 51 tests passed` |

定向测试覆盖：

- `src/__tests__/middleware-helpers.test.ts`
- `src/app/[locale]/(app)/brief/__tests__/brief-actions.test.ts`
- `src/app/[locale]/(app)/brief/__tests__/BriefPageClient.test.tsx`
- `src/app/[locale]/(app)/brief/__tests__/ProductsClient-deep-link.test.tsx`
- `src/app/[locale]/(app)/brief/__tests__/brief-create-campaign.test.ts`
- `tests/unit/i18n-locale-coverage.test.ts`

## L2 复验记录

| 项 | 结果 | 证据 |
|---|---|---|
| staging repo sha | PASS | `/opt/kolmatrix-staging` = `fb0160592fbd17155e8ae81dcf288ea549aa8d1c` |
| staging health | PASS | `healthy`，DB / Redis `ok` |
| `/knowledge-base` redirect | PASS | 登录态真实请求 `reqid=126`：`301 -> /en/brief?tab=products` |
| `/knowledge-base/[productId]` deep link | PASS | 登录态 curl with session cookie：`301 -> /en/brief?tab=products&productId=cprod1111111111111111` |
| `/campaigns/new` redirect | PASS | 登录态真实请求 `reqid=89`：`301 -> /en/brief?action=new` |
| 正常 parse（修复前置） | PASS | `Launch Genshin Impact in Japan...` 成功填入 product/budget/date/market/audience/category |
| cap 模拟 toast | PASS | UI 出现 `Daily AI quota reached. Please fill the form manually.` |
| cap 模拟空表单 | PASS | cap 模拟命中后 product/budget/date/market/audience/category 保持空 |
| cap 模拟审计 | PASS | `ai_brief.parse_cap_exhausted|true|2026-05-18T15:23:32.423Z` |
| 清理恢复 | PASS | 恢复 `.env.staging` 备份后再次 parse `Promote PUBG Mobile in Korea...` 成功填表 |
| 24h parse gate | PASS | `17 / 21 = 80.95%` |

## Cap 模拟操作记录

- runbook：`docs/dev/bl069-cap-exhausted-simulation-runbook.md`
- staging env backup：
  - `/opt/kolmatrix-backups/.env.staging.bl069-cap-sim.20260518-152125`
- 启用：
  - 追加 `BRIEF_FORCE_CAP_EXHAUSTED=true`
  - `pm2 reload kolmatrix-staging --update-env`
- 清理：
  - 备份文件恢复 `.env.staging`
  - 再次 `pm2 reload kolmatrix-staging --update-env`
  - `grep` 结果：`(no flag found — clean)`

## 24h Audit

命令：

```bash
ssh tripplezhou@34.180.93.185 'cd /opt/kolmatrix-staging && npx tsx scripts/bl069-cost-audit.ts --hours=24'
```

结果摘录：

- `calls: 21`
- `parse_applied / total: 17 / 21`
- `rate: 80.95% — PASS`
- `ai_brief.parse_cap_exhausted — calls=1`

## 风险备注

- cap 模拟依赖 staging-only env flag，操作期间会让所有 brief parse 走 cap fallback；后续继续沿用时必须严格按 runbook 清理。
- 当前 24h gate 只高出阈值 `0.95%`，仍建议继续观察 `unparsable` 和 `cap_exhausted` 比例，但这不阻塞本批签收。

## Harness 说明

本批次满足 signoff 硬性要求。

- `docs.signoff` 已填写本报告路径
- `progress.json.status` 应推进到 `done`
