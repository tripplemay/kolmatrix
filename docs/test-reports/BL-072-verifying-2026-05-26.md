# BL-072 Verifying Report — 2026-05-26

## 结论

- 批次：`BL-072-prod-hotfix`
- 结果：`FAIL`
- Reviewer：`codex: Reviewer`
- 阶段：`verifying`

BL-072 首轮验收的功能面基本通过：L2 staging 6 项抽样全部过线，L1 的 `tsc`、`vitest`、旧 IA/stale path grep、页面宽度、Material Symbols、i18n key 也都通过。当前唯一 blocker 是 `lint` warning 阈值超标。

按当前 `framework/harness/evaluator.md` §10.3：

- `0 errors + ≤3 unused-import-style warning` 可 soft-watch
- `0 errors + ≥4 warning` 需要切回 `fixing`

本轮 `npm run lint` 是 `0 error / 4 warnings`，因此不能 signoff。

## L1 结果

### 1. `npm run lint`

结果：

```text
4 warnings, 0 errors
```

命中：

- `src/app/[locale]/(app)/insight/page.tsx:120:41`
- `src/app/api/health/__tests__/route.test.ts:18:20`
- `src/lib/queue/explain-recommendations-worker.ts:81:3`
- `tests/integration/db-platform-admin-nullif.test.ts:13:31`

其中与本批次直接相关的是：

- `src/app/[locale]/(app)/insight/page.tsx:120:41`
  - `_locale` defined but never used
  - `git blame` 显示该行来自本批次 `7278af0`（F001+F002）

判定：`FAIL`

### 2. `npx tsc --noEmit`

结果：PASS

判定：`PASS`

### 3. `npm test`

结果：

- `185 files`
- `1322 tests`
- 全部通过

备注：

- 运行期间出现大量 Node `--localstorage-file` warning，但未导致测试失败
- F007 新增的 3 个 advisory test 已被包含在全套里

判定：`PASS`

### 4. 旧 IA / stale path grep

命令：

```bash
grep -rEn "['\"\\\`]/(knowledge-base|discovery|database|outreach|emails|analytics|dashboard|reports|weekly-report|campaigns/new)" src/ --include="*.tsx" --include="*.ts" | grep -v "__tests__\\|\\.test\\.\\|/api/"
```

结果：0 命中

判定：`PASS`

### 5. 4 路由宽度

`brief` / `match` / `reach` / `insight` 的 page 容器都使用 `max-w-[1600px]`。

staging 实测：

- `/en/brief` 容器宽度：`1136`
- `/en/insight` 容器宽度：`1136`

判定：`PASS`

### 6. Material Symbols + zh 文案键

在 temp copy 中运行 `scripts/regenerate-material-symbols-subset.sh`，输出包含：

- `discovered 100 unique icons`
- `table_rows`

同时 `messages/zh.json` 已含：

- `insight.reports.title`
- `insight.reports.body`
- `insight.analytics.title`
- `insight.analytics.body`

判定：`PASS`

## L2 staging 抽样

环境：

- `https://staging.kol.guangai.ai`
- `marketer@kolmatrix.local / KOLMatrix@2026!`
- Playwright authenticated probe

### 1. `/zh/insight`

结果：

- heading = `Insight`
- subtitle 为中文描述：`Dashboard、Reports、(即将) Analytics — 全局 KOL 营销脉搏。`
- tabs = `仪表盘 / 报告 / 分析`
- reports panel body 为中文：`为你的租户 AI 生成的周度营销表现报告。`

判定：`PASS`

### 2. `/en/match` view toggle

结果：

- card icon = `grid_view`
- table icon = `table_rows`

判定：`PASS`

### 3. `/en/brief` 宽度对齐

结果：

- `/en/brief` 宽度 = `1136`
- `/en/insight` 宽度 = `1136`

判定：`PASS`

### 4. `/en/insight` QuickActions 4 按钮

结果：

- `knowledgeBase` → `/en/brief?tab=products`
- `discovery` → `/en/match`
- `database` → `/en/match?view=table`
- `campaigns` → `/en/campaigns`

均未落到 404。

判定：`PASS`

### 5. `/en/crm` CrmPipelineBars

结果：

- 点击第一个 bar（`prospect`）后到达：
  - `/en/crm?status=prospect`

判定：`PASS`

### 6. `/en/kols/<id>` Back

结果：

- 从 `/en/match?view=table` 进入首个 KOL 详情：
  - `/en/kols/fddf7f1f-3bb8-4523-ab0b-1702982f213f`
- Back link href = `/en/kols`

判定：`PASS`

## 最终判定

### PASS

- `F001`
- `F003`
- `F004`
- `F005`
- `F006`
- `F007`

### FAIL

- `F002`
  - 原因：本批次改动引入 `src/app/[locale]/(app)/insight/page.tsx:120:41` unused warning，导致整体 `lint` warning 数达到 `4`

### 未完成

- `F008`
  - 原因：signoff 被 `lint` warning 阈值阻断

## 建议修复

最小修复路径：

1. 清掉 `src/app/[locale]/(app)/insight/page.tsx:120:41` 的 `_locale` unused warning
2. 重跑 `npm run lint`

如果总 warning 回到 `≤3`，则可按当前 evaluator 规则视为 soft-watch，其余 L1/L2 证据已足够支撑复验。
