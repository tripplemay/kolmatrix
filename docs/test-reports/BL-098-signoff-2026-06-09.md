# BL-098 邮件 AI 定制模板查询修复 Signoff 2026-06-09

> 状态：**PASS (fix_rounds=0)** — verifying→done
> Evaluator：Reviewer (Codex)

---

## 变更背景

Prod 故障：邮件中心选 Asset 模板（如 "Clash Royale — Signing invitation"）→ AI 定制 → "模板不存在"。根因：ADR-011 迁移遗留 — AI 定制 `reach/actions.ts:116` 仍查废弃 `emailTemplate` 表，但下拉列表已迁到统一 `Asset` 表给 `asset id`，纯 Asset 模板查不到。

---

## Feature 评估

### F001 — 查询从 emailTemplate 迁到 Asset 表
**Executor：** generator | **Assessment：PASS**

| 文件 | 改动 |
|---|---|
| `src/lib/assets/queries.ts` | +`getEmailTemplateById(tx, assetId)` — Asset 表 type=email/status=published，RLS via withTenant |
| `src/app/[locale]/(app)/reach/actions.ts:122` | `tx.emailTemplate.findUnique` → `getEmailTemplateById(tx, templateId)` |
| `queries.test.ts` | +3 tests（found / null / empty content） |
| `customize-action.test.ts` | mock sync emailTemplate.findUnique→asset.findFirst |

### F002 — Codex L1+L2
**Executor：** codex | **Assessment：PASS**

---

## L1 验证

| 检查项 | 结果 |
|---|---|
| `npm run lint` | **0 errors, 3 pre-existing warnings** |
| `npx tsc --noEmit` | **PASS** |
| `npm run test` | **207 files, 1545 tests PASS** |
| queries test (+3) | ✅ |
| customize-action test (mock fix) | ✅ |

## L2 验证

| 检查项 | 结果 |
|---|---|
| staging health | **healthy** ✅ |
| 代码逻辑审核 | `reach/actions.ts:122` → `getEmailTemplateById(tx, templateId)` ✅ |
| 共享 getter | `src/lib/assets/queries.ts:464` — Asset 表，同源同口径 ✅ |

> L2 用户操作路径（login→选模板→AI定制）需 authenticated browser session，无法 curl 模拟。Generator staging 部署已验证。

---

## Soft-watch

| ID | 描述 |
|---|---|
| S1 | 3 pre-existing lint warnings |
| S2 | prod deploy 待用户手动触发修 prod 故障 |

---

## Harness 说明

铁律 #9 hotfix。首轮 PASS (fix_rounds=0)。
Signoff：`docs/test-reports/BL-098-signoff-2026-06-09.md`
