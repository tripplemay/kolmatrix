# BL-052 dashboard-trend-edge-polish 验证报告 2026-05-07

## 总体结论
- L1 本地验证大体通过：`lint` 0 errors / 3 warnings，`npx prisma generate` 通过，`npx tsc --noEmit` 通过，`npm run test` 1084/1084 通过。
- L2 staging smoke 通过：`/api/health`、`/zh/dashboard`、`/zh/knowledge-base`、`/zh/database` 页面均正常渲染，没有看到错误页或空白页。
- 但默认 `npm run test:integration` 没有全绿，失败点落在 `tests/integration/pre-commit-hook.test.ts` 的 `icon staged + woff2 matches script output → hook PASS`，因此当前批次不能签收。

## 问题清单
- [Medium] `pre-commit` hook 的 Material Symbols 再生路径在默认集成套件里不稳定：`tests/integration/pre-commit-hook.test.ts` 里 “icon staged + woff2 matches script output” 用例在完整 `npm run test:integration` 中失败，hook stdout 显示 `scripts/regenerate-material-symbols-subset.sh failed (network? quota? typo in manifest?)`，导致套件退出码为 1。该脚本直接依赖 Google Fonts 网络拉取 subset woff2，完整套件并发运行时会暴露出稳定性问题。文件证据见 [`framework/templates/pre-commit-hook.sh`](/Users/yixingzhou/project/joyce/framework/templates/pre-commit-hook.sh) 和 [`scripts/regenerate-material-symbols-subset.sh`](/Users/yixingzhou/project/joyce/scripts/regenerate-material-symbols-subset.sh)。

## 评分卡
- Correctness（正确性）: 4/5 - 业务侧趋势卡、空状态、网络状态与 staging 页面渲染都正常；未发现 BL-052 功能逻辑错误。
- Regression Risk（回归风险）: 3/5 - 新增 KPI snapshot / edge-state 代码面较稳，但默认集成套件存在可复现的 hook 稳定性失败，说明发布验证链路有回归风险。
- Security（安全性）: 4/5 - 未见新的权限绕过、敏感数据泄漏或未验证输入边界。
- Reliability（可靠性）: 2/5 - `pre-commit` hook 依赖外部网络再生 woff2，在完整集成运行中出现失败，说明验证链路存在明显抖动。
- Performance（性能）: 4/5 - Dashboard 读取趋势与快照逻辑未见明显热路径退化；未观察到 staging 性能异常。
- Maintainability（可维护性）: 4/5 - 结构清晰，`kpi-trends` / `kpi-snapshot` / `StatCard` 拆分合理；hook 脚本的网络依赖是唯一明显的维护风险点。
- Test Readiness（测试完备度）: 2/5 - 单文件复验可通过，但默认 `npm run test:integration` 不能稳定全绿，当前不满足签收门槛。

## 待确认事项
- 需要确认 `scripts/regenerate-material-symbols-subset.sh` 的网络拉取是否必须保留在默认集成路径中，还是应改成可离线缓存/预生成，以消除 `pre-commit` hook 的不稳定性。
- 需要确认 CI/本地验收是否要求默认并发 `test:integration` 必须全绿，还是允许对这类网络型测试单独串行/隔离。

## 最终结论
- Weighted result（加权结果）: 业务实现本身基本通过，但验证链路未稳定全绿。
- Final grade（最终等级）: C
- Readiness（可推进性）: Not ready

---

## Planner 裁决（2026-05-08, johnsong）

**裁决结论：BL-052 不被该 flaky test 阻塞，可签收。flaky test 单独入 backlog `BL-054-flaky-network-test-isolate` 治理。**

**裁决依据（铁律 P5 — 理由可复用）：**

1. **范围正交**：失败的 `tests/integration/pre-commit-hook.test.ts` 来自 **BL-027-F004**（commit `2c8af8a`），`scripts/regenerate-material-symbols-subset.sh` 来自 **BL-025-F009 / BIx-mvp-polish-pass**。BL-052 13 commits 内（c4afd5a..3ba3fe2）零修改这两文件，与 BL-052 范围（KPI 真趋势化 + Edge State Polish）完全正交。

2. **acceptance 边界**：BL-052 验收标准是 `docs/specs/BL-052-dashboard-trend-edge-polish-spec.md` §3.10（Part A 5 features）+ §4.5（Part B 6 features）各自 acceptance 列表，不含"全套 `npm run test:integration` 必须全绿"的普遍约束。把外部网络依赖的 flaky test 计入 BL-052 评分超出 scope。

3. **失败本质 = 既有 framework reliability 缺陷**：单文件跑 PASS / 全套跑 fail = **并发抖动 + 外部网络依赖**（Google Fonts subset woff2 拉取）。这是 framework / CI 策略问题，应独立批次治理（推荐：隔离到 `test:integration:network` 单独串行跑，或预生成 woff2 缓存避免拉网络）。

**Reviewer 复验范围（裁决后）：**

- ✅ 视 `pre-commit-hook.test.ts` 失败为 BL-052 范围外，不计入评分；该 test 单文件隔离跑 PASS 已构成 reliability "可恢复" 信号
- ✅ 复验 BL-052 引入的测试：`tests/integration/kpi-snapshot.test.ts`（F003 × 3 IT）+ Part B 的 EmptyState/useNetworkStatus/NetworkStatusBanner 单测 + Part A 9 component test
- ✅ 复验 staging smoke（已 PASS @ 3ba3fe2 / 不需重做）
- ✅ 复验 acceptance 表逐项（spec §3.10 + §4.5 各项）
- ✅ Test Readiness 评分按"BL-052 引入代码测试覆盖"维度重打（预期 4-5/5）

**裁决理由复用价值（v0.9.15 +）：**

未来 Planner 遇到 Reviewer 评分把"既有 framework flaky test"计入当前批次时，可援引本裁决：**批次验收的 acceptance 边界 ≠ 全套测试普遍绿门槛**，外部网络依赖 / 并发抖动等基建问题应入独立批次治理。framework templates / scripts 类的 reliability 缺陷不属于业务批次（除非该批次明确改动了相关文件）。

**同步 ops（Planner 5/8）：**

1. ✅ 新建 `BL-054-flaky-network-test-isolate` backlog 条目（priority=medium，~2-4h Generator）
2. ✅ 同 commit 更新 `.auto-memory/project-status.md`（BL-054 立项 + BL-052 verifying 卡点解锁）
3. ✅ 不影响 5/13 上线时间线（BL-052 done → 5/8 prod redeploy 含 BL-049+BL-051a+BL-052 不变）

**BL-052 Reviewer 复验后预期 grade：B+ 以上（Test Readiness 4-5/5 / Reliability 4/5），Readiness=Ready。**
