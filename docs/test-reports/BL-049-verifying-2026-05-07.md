# BL-049 测试基建系统性升级 验证报告 2026-05-07

> 状态：**Reviewer verification partial**
> 触发：`BL-049` 处于 `verifying`，Reviewer 执行 L1 + staging smoke 验收
> Reviewer：Codex

## Summary

- Scope: 7 个 feature，覆盖 `vitest.coverage-exclusions.ts` 抽离、`vitest.integration.config.ts` 并发、Playwright visual project 拆分、Dependabot、Material Symbols / database-fidelity 两个预存在失败点、dead code 评估、framework v0.9.15 沉淀。
- Documents: [`docs/specs/BL-049-test-infra-systematic-upgrade-spec.md`](/Users/yixingzhou/project/joyce/docs/specs/BL-049-test-infra-systematic-upgrade-spec.md), [`docs/audit-reports/test-infra-audit-2026-05-07.md`](/Users/yixingzhou/project/joyce/docs/audit-reports/test-infra-audit-2026-05-07.md), [`progress.json`](/Users/yixingzhou/project/joyce/progress.json)
- Environment: 本地 L1 `npm run lint` / `npx tsc --noEmit` / `npm run test:coverage` / targeted vitest integration + staging `https://staging.kol.guangai.ai`
- Result totals: PASS 4, FAIL 0, BLOCKED 1, NOT RUN 0

## Test Cases

- BL-049-TC-01 L1 lint / typecheck / coverage - PASS
- BL-049-TC-02 L1 targeted integration guards - PASS
- BL-049-TC-03 Staging browser smoke - PASS
- BL-049-TC-04 Full local Testcontainers integration suite - BLOCKED

## Execution Results

### BL-049-TC-01 L1 lint / typecheck / coverage

Result: PASS
Evidence:
- `npm run lint` -> `0 errors / 3 warnings`（保持既有 warning，不是新增错误）
- `npx tsc --noEmit` -> PASS
- `npm run test:coverage` -> `146/146` files PASS
- Coverage summary:
  - Statements `84.81%`
  - Branches `75.87%`
  - Functions `82.63%`
  - Lines `86.04%`
Observed Behavior:
- Unit / coverage 基线稳定，没有新增静态错误。
Mismatch vs Spec:
- None in BL-049 scope.
Defect Link / Reference:
- None.

### BL-049-TC-02 L1 targeted integration guards

Result: PASS
Evidence:
- `npx vitest run --config vitest.integration.config.ts tests/integration/material-symbols-coverage.test.ts` -> `1/1` file, `7/7` tests PASS
- `npx vitest run --config vitest.integration.config.ts tests/integration/pre-commit-hook.test.ts` -> `1/1` file, `6/6` tests PASS
- `vitest.integration.config.ts` 当前配置可直接读到 `fileParallelism: true` + `maxWorkers: 4`
Observed Behavior:
- Material Symbols 复发性守门与 pre-commit hook 路径均能在独立测试文件中通过。
Mismatch vs Spec:
- None.
Defect Link / Reference:
- None.

### BL-049-TC-03 Staging browser smoke

Result: PASS
Evidence:
- `https://staging.kol.guangai.ai/api/health` 返回 `{"status":"healthy","checks":{"database":{"status":"ok"},"redis":{"status":"ok"}}}`
- `/zh/dashboard` 渲染仪表盘 shell、workflow cards、AI 推荐 KOL、周边导航
- `/zh/discovery` 渲染搜索区、筛选栏、KOL 网格与 `1,195` 匹配结果
- `/zh/campaigns` 渲染 3 条活动、筛选器、AI 建议区
- `/zh/roi` 渲染 ROI 追踪 shell、总支出 / 总收入 / 平均 ROI 区块
- `/zh/weekly-report` 渲染 AI 周报、语言切换、导出 / 分享 / 重新生成按钮
Observed Behavior:
- Staging 侧关键路由均能正常打开，且健康检查通过。
Mismatch vs Spec:
- None.
Defect Link / Reference:
- None.

### BL-049-TC-04 Full local Testcontainers integration suite

Result: BLOCKED
Evidence:
- `docker info` 报错：`failed to connect to the docker API at unix:///Users/yixingzhou/.colima/default/docker.sock; check if the path is correct and if the daemon is running`
- 这会阻断 `tests/helpers/db.ts` 依赖的 Testcontainers PostgreSQL 启动
Observed Behavior:
- 当前机器没有可用 Docker daemon，因此无法在本机完整执行全量 integration suite。
Mismatch vs Spec:
- 这是环境阻断，不是实现缺陷。
Defect Link / Reference:
- None.

## Defects

- None in BL-049 implementation scope.

## Coverage Gaps

- 本机无法跑完整的 Testcontainers integration suite，因此还不能把 BL-049 签成全 PASS。

## Open Questions

- 是否需要更换到带 Docker daemon 的测试机，再复跑全量 integration suite？

## Final Decision

- Ready: Not yet
- Readiness: Partial
- Final: `BLOCKED` on local Docker/Testcontainers environment
