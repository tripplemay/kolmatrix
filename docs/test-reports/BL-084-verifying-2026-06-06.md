# BL-084 Verifying 2026-06-06

> 范围：BL-084 `F009` Reviewer 首轮验收（L1 + 经授权的 L2 staging/prod 只读）
> 结论：`FAIL`，进入 `fixing`

## 本轮完成

- L1 环境前置：
  - `.nvmrc=20`，本机原始 Node 为 `v25.7.0`，切到 `v20.20.2`
  - `bash scripts/test/codex-setup.sh` 拉起本地 3099 + PG/Redis + migrate/seed
- 测试域修补（仅测产物）：
  - `tests/e2e/match-flow.spec.ts` 收窄 BL-084 toggle 断言，避免 `match-view-toggle` 重名 strict-mode 误报
  - `tests/e2e/visual-regression.spec.ts` 将 `?campaignId` baseline 固定到 `&view=full-pool`

## L1 结果

| 项 | 结果 | 证据 |
|---|---|---|
| `npx prisma validate` | PASS | schema valid |
| `npx tsc --noEmit` | PASS | `TSC_OK` |
| `npm test` | PASS | `1505/1505` |
| `npm run lint` | PASS / Soft-watch | `0 errors / 3 warnings`，均为 unused-vars 类，不阻断 |
| i18n key 覆盖 | PASS | `ok 5 locales x 22 keys`（按实际命名空间 `campaigns.detail.suggestionStatus.*`） |
| BL-084 Playwright toggle | PASS | `tests/e2e/match-flow.spec.ts --grep "BL-084-F007|AI panel toggle"` → `2 passed`（setup + case） |
| migration 本地库核对 | PASS | `kol_campaign` 已有 `suggestion_status/suggested_at/decided_at/match_score` + `kol_campaign_suggestion_status_idx`；本地 `null_status_rows=0` |

## L2 staging 结果

环境：`https://staging.kol.guangai.ai`

### 已验证通过

1. 默认 AI 入口：
   - campaign A `2d11dd71-1a98-4ee0-b15d-314dae9fcd3c`
   - campaign B `382f014c-a9f9-4fde-bcad-d5bb10ed2045`
   - 两者访问 `/zh/match?campaignId=<id>` 均默认落 AI 三列页，而非 full-pool。
2. 三列渲染：
   - campaign A 初始：`推荐池 30 / 已接受 0 / 候补池 0`
   - campaign B 初始：`推荐池 29 / 已接受 5 / 候补池 0`
3. Accept 闭环：
   - campaign A 首条 `제미니의 게임채널` 从 `推荐池` 进入 `已接受`
   - toast 出现：`제미니의 游戏채널 已加入已接受` + `撤回`
   - 随后用页面脚本在 5s 窗口内点击 Undo，列数恢复到操作前状态
4. Skip 闭环：
   - campaign A `왕군의 游戏实验` 点击 `跳过` 后从推荐池移除，计数 `29 -> 28`
   - force refresh 后该名称未重新出现，满足“下次 getCampaignSuggestions 不再推”
5. Swap / Re-add 闭环：
   - campaign A `게임통` 点击 `候补` 后：`推荐池 28 -> 27`，`候补池 0 -> 1`
   - 点击 `重新加入` 后回到 `推荐池`，`候补池` 清空
6. Toggle 全池：
   - campaign B 点击 `全部 KOL` 后 URL 变为 `view=full-pool`
   - full-pool 工作台正常渲染：筛选侧栏、保存搜索、Match 列表、AI 建议侧栏仍在

### 失败项

1. `Why` 详细解释不可用：
   - 点击 `推荐理由` 后弹窗可打开，但内容为：`详细解释暂时不可用，请稍后重试。`
   - 未获得 BL-067 DetailedExplanationDialog 预期的详细 explanation 内容。
2. force refresh 走了 fallback：
   - refresh 完成后页面出现：`AI 重排暂不可用 — 按相似度排序显示。`
   - 这说明 staging 当前至少在 refresh 路径上未拿到正常的 LLM rerank；代码降级生效，但不满足“完整 AI explainability”口径。

### staging 健康

- `curl https://staging.kol.guangai.ai/api/health | jq .`
  - `status=healthy`
  - `database.latency_ms=44`
  - `redis.latency_ms=17`
- `/api/health` 当前未返回 `git_sha`，因此本轮只能确认健康，不能从公开 health JSON 直接确认部署 SHA。

## prod 只读核验

prod 应用健康：

- `curl https://kol.guangai.ai/api/health | jq .`
  - `status=healthy`
  - `database.latency_ms=32`
  - `redis.latency_ms=10`

prod 主机只读 SSH：

- `ssh tripplezhou@34.180.93.185 'cd /opt/kolmatrix && git rev-parse --short HEAD'`
  - 返回：`96ca150`
- `.env.production` 解析：
  - `db_name=kolmatrix`
  - `db_host=localhost`

prod DB 只读 SQL：

- `_prisma_migrations` 最近 5 条不含 `20260605160000_bl_084_add_kol_campaign_suggestion_status`
- `information_schema.columns` 中：
  - `kol_campaign.match_score` 存在
  - `suggestion_status / suggested_at / decided_at` **不存在**

结论：

- BL-084 migration **未落到 prod 数据库**，因此 `legacy accepted backfill` 也无法成立
- F009 的 prod 只读 acceptance 当前明确 `FAIL`

## Reviewer 判定

- `F009`: `FAIL`

### 失败原因摘要

1. staging `Why` 详细解释不可用，只能看到错误态弹窗。
2. prod DB 尚未应用 BL-084 migration，`suggestion_status/suggested_at/decided_at` 缺失。

## Soft-watch

| ID | 描述 | 风险等级 | 建议处置 |
|---|---|---|---|
| S1 | lint 仍有 3 个 unused-vars warning | low | 下批次顺手清理，不阻断 |
| S2 | campaign B 的建议池为 `29` 而不是 `30`（已接受 5） | low | Generator 复核“已决策行是否应补位到固定 top-30”产品语义 |

