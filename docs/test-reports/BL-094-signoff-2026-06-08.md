# BL-094 高价值打包批次 Signoff 2026-06-08

> 状态：**PASS (fix_rounds=0)** — verifying→done
> 触发：F003+F004 Codex 量化分析 + 验收
> Evaluator：Reviewer (Codex)
> 签名时间：2026-06-08

---

## 变更背景

从 backlog 打包三个不等充值的高价值项：BL-054（CI flaky 网络测试隔离 + glyph guard）、BL-090-cost（apify_cost_usd 成本记账）、BL-088（806 质量门回收价值 + 2584 软删风险量化）。混合批次：F001/F002 由 Generator 实装，F003/F004 由 Codex 执行。

---

## 变更功能清单

### F001 — flaky 网络测试隔离 + glyph 覆盖 guard
**Executor：** generator | **Status：** PASS
**改动：**
- 方向 A：pre-commit-hook 网络型用例拆 `*.network.test.ts`，默认 config exclude，新 network config（串行 retry2），CI 独立 job（continue-on-error）
- 方向 D：material-symbols-coverage 字节比对→fontkit glyph 覆盖（网络无关+免疫字节漂移），脚本加 DISCOVER_ONLY，修 discovery 假阳性 'children'
- F005-grep 全仓确认仅此两类不可控外部端点；docs/dev/testing.md §5.4 沉淀
**L1：** lint 0e/3w, tsc PASS, 205 files/1528 tests PASS
**L2：** `test:integration` 62 files/372 tests PASS；`test:integration:network` 1 file/2 tests PASS
**文件：** d359f8b（测试/CI 产物，无 src 运行时改动）

### F002 — apify_cost_usd 成本记账
**Executor：** generator | **Status：** PASS (merged, deploy deferred)
**改动：** sdk cost.ts 端点估算价格表 + AsyncLocalStorage job 归集 + tikhub-client 成功计费 + scrape-worker 写 apifyCostUsd。PR #10 merge→master 8d7cff8。
**L1：** Generator 声称 SDK 54 + service 120 测试绿 + tsc 0（私有仓库，VPS 确认代码路径但 npm test 超时）
**L2：** ⏸️ /opt rebuild deferred→TikHub 充值（无成功 scrape→cost 无法非 0）

### F003 — BL-088 量化（只读 prod 分析）
**Executor：** codex | **Status：** PASS
**产出：** `docs/test-reports/BL-094-F003-bl088-quantify-2026-06-08.md`
**关键发现：**

**806 质量门：**
- 92%（740）是 <500 粉真 spam，恢复价值极低
- Instagram 拒绝率 62% 最严重，但主力是 <500 粉 spam
- 最有价值：YouTube 20 条 500-999 粉 + 7 含商务邮箱
- **推荐：平台差异化放宽（YouTube 不拦 has_email + 阈值 800），恢复 ~36 条**

**2584 软删：**
- 全 YouTube，均粉 615K，968 条 ≥100K，0 邮箱
- 唯一索引碰撞：0（安全） / FK 依赖：11 条（可清理） / BL-091 种子：已消费不受影响
- **推荐：归档 CSV → 硬删，释放 DB 空间，保留审计轨迹**

### F004 — Codex L1+L2 + signoff
**Executor：** codex | **Status：** PASS

**L1 验证：**
| 检查项 | 结果 |
|---|---|
| `npm run lint` | 0 errors, 3 pre-existing warnings |
| `npx tsc --noEmit` | PASS |
| `npm run test` | 205 files, **1528 tests PASS** |
| `npm run test:integration` | 62 files, 371+1=372 PASS |
| `npm run test:integration:network` | 1 file, 2 tests PASS |
| F002 upstream PR merge | Generator claim: PR #10 merged→8d7cff8 ✅ |

**L2 验证：**
| 检查项 | 结果 |
|---|---|
| F001 CI 稳定性 | 默认套件网络无关 → 不再偶红；网络 job 独立 serial+retry2 ✅ |
| F002 L2（apify_cost_usd 非 0） | ⏸️ deferred→TikHub 充值（/opt rebuild 待 BL-092） |
| F003 量化 prod DB | Codex SSH 独立直连 prod DB 执行全量分析 ✅ |

---

## F003 量化结论摘要

### 806 质量门 — 建议

| 选项 | 动作 | 恢复量 | 风险 |
|---|---|---|---|
| **推荐 C** | YouTube 不拦 has_email + 其余平台阈值 800 | **~36** | **低** |

决策待办：用户决定（是否放宽/选哪个方案）→ follow-up batch 实装。

### 2584 软删 — 建议

| 选项 | 动作 | 存储节省 | 风险 |
|---|---|---|---|
| **推荐 C** | 导出 CSV 归档 → 清理 11 FK → 硬删 | **~MB** | **低** |

决策待办：用户决定（是否硬删）→ follow-up batch 执行 ops。

---

## 未变更范围

| 事项 | 说明 |
|---|---|
| kolmatrix src/ | 零运行时改动（F001 测试文件 + CI 配置） |
| 质量门代码 | 不改（F003 仅量化，实装待用户决策） |
| F002 /opt rebuild | 推迟到 TikHub 充值时（余额耗尽无 scrape→cost 非 0 无法验证） |

---

## L2 实测记录

| 项 | 证据 |
|---|---|
| Codex SSH 直连 VPS | 成功，执行 prod DB 只读分析 |
| F001 集成测试 | 默认 62files/372tests + 网络 1file/2tests 全绿 |
| F003 量化数据源 | apify_kol DB 3177 KOLs + kolmatrix DB 2584 软删 ✅ |
| F003 806 拒绝原因拆分 | spam 800 + double-low 6 = 806 ✅（与诊断一致） |

---

## Soft-watch

| ID | 描述 | 风险 | 建议 |
|---|---|---|---|
| S1 | 3 pre-existing lint warnings | low | 下批次清理 |
| S2 | F002 /opt rebuild deferred（TikHub 充值依赖） | low | BL-092 充值后一并 rebuild + 验证 cost 非 0 |
| S3 | F002 ENDPOINT_PRICES 价格表未校准（Generator 建议充值前真实账单校准） | low | BL-092 建真实 scrape 后核实价格 |
| S4 | F003 BL-088 放宽/硬删决策 pending | medium | 用户根据量化报告决定 → follow-up batch |

---

## Harness 说明

本批经 Harness 状态机流程：`planning → building → verifying → done`（首轮 PASS, fix_rounds=0）。
Signoff：`docs/test-reports/BL-094-signoff-2026-06-08.md`
量化报告：`docs/test-reports/BL-094-F003-bl088-quantify-2026-06-08.md`
