# BL-012 apify-kol Integration Verifying 2026-05-08

> 状态：**PARTIAL / 未达 signoff**
> 触发：`origin/main` 已包含 BL-012 Stage 1.5 产物，Reviewer 复验 preview 页与数据隔离守门。

## 范围

- F001 `/[locale]/admin/apify-preview` 路由 + admin auth gate
- F002 server-side fetch fork apify-kol-service GET `/kol` + zod parse
- F003 `PreviewTable` client component
- F004 `StatsCards` 4 维度决策门
- F005 i18n 5 locale × 55 leaf coverage
- F006 integration test + data-flow isolation

## 执行结果

### PASS

- `npm run lint`
  - 0 errors / 3 warnings
- `npx tsc --noEmit`
  - 通过
- `npx vitest run --config vitest.config.ts 'src/app/[locale]/admin/apify-preview/__tests__/page.test.tsx' 'src/app/[locale]/admin/apify-preview/__tests__/StatsCards.test.tsx' 'src/app/[locale]/admin/apify-preview/__tests__/PreviewTable.test.tsx' 'src/lib/admin/__tests__/apify-preview-client.test.ts'`
  - 4 files / 18 tests passed
- `npx vitest run --config vitest.integration.config.ts tests/integration/admin-apify-preview.test.ts tests/integration/material-symbols-coverage.test.ts tests/unit/i18n-locale-coverage.test.ts`
  - 2 files / 10 tests passed
- `npm run test`
  - 158 files / 1107 tests passed

### FAIL

- staging 实地复验未能进入 `/en/admin/apify-preview` 真页
  - 登录 `admin@kolmatrix.local / KOLMatrix@2026!` 后，浏览器会回到 `/en/dashboard`
  - 直接访问 `/en/admin/apify-preview` 也会被重定向回 `/en/dashboard`
- 根因是权限门与真实 seed 角色不一致：
  - preview 页代码要求 `session.user.role === "admin"`
  - 但 seed 里 admin 账号写入的是 `role: "tenant_admin"`

## 证据

### 1. 代码层

- [src/app/[locale]/admin/apify-preview/page.tsx:78-84](/Users/yixingzhou/project/joyce/src/app/[locale]/admin/apify-preview/page.tsx#L78-L84)
  - `if (session.user.role !== "admin") redirect(\`/${locale}/dashboard\`)`
- [prisma/seed.ts:268-277](/Users/yixingzhou/project/joyce/prisma/seed.ts#L268-L277)
  - `admin@kolmatrix.local` 的角色是 `tenant_admin`
- [docs/dev/architecture.md:120-126](/Users/yixingzhou/project/joyce/docs/dev/architecture.md#L120-L126)
  - 真实角色枚举是 `platform_admin / tenant_admin / marketer`

### 2. Spec 对照

- [docs/specs/BL-012-apify-kol-integration-spec.md:61-65](/Users/yixingzhou/project/joyce/docs/specs/BL-012-apify-kol-integration-spec.md#L61-L65)
  - spec 决议写的是“仅 admin role 可见”
- [docs/specs/BL-012-apify-kol-integration-spec.md:89-95](/Users/yixingzhou/project/joyce/docs/specs/BL-012-apify-kol-integration-spec.md#L89-L95)
  - preview 页权限设计也写的是仅 admin role

### 3. 实际 staging 行为

- 登录后页面落在 `/en/dashboard`
- 访问 `/en/admin/apify-preview` 后仍回到 `/en/dashboard`
- 说明当前 staging 上的 demo/admin 账号无法直接看到 preview 页

## 结论

- 本轮 `verifying` 结论：**FAIL**
- BL-012 Stage 1.5 的测试覆盖、i18n、数据隔离守门、统计计算都已通过。
- 但真实 staging 访问被权限门挡回 dashboard，导致无法完成可见性验收。
- 当前不满足签收条件，`docs.signoff` 仍应保持 `null`。
- 建议状态流转：保持 `verifying`，先修正 `tenant_admin` / `admin` 角色门的实际对齐，再复验 staging。
