# BL-076 verifying report — FAIL

> Sprint: `BL-076-apify-numeric-overflow`
> Phase: `verifying`
> Reviewer: `codex: Reviewer`
> Date: `2026-05-27`
> HEAD: `cc6cf45`

## Summary

BL-076 cannot be signed off in this round. The prod-side numeric-overflow hotfix evidence is real, but the required staging `kol-sync:daily` run did not satisfy F005 L2 acceptance.

## Findings

### High — staging `kol-sync:daily` does not pass the L2 acceptance chain

Spec `F005` requires a real staging `npm run kol-sync:daily` run with `stats.inserted > 0` and `stats.failed = 0`.

My staging run:

```bash
ssh tripplezhou@34.180.93.185 \
  'cd /opt/kolmatrix-staging && set -a && source .env.staging >/dev/null 2>&1 && set +a && AI_DAILY_COST_USD_PER_TENANT_MAX=500 npm run kol-sync:daily'
```

Observed evidence from the live run:

```text
[kol-sync-daily] starting (dryRun=false)
[kol-embed] batch size=100 failed: http: aigcgateway responded 413
[aigcgateway full] embedBatch status=413 body={"error":{"type":"server_error","code":"provider_error","message":"Provider returned 413"}}
[enrichment-stage] tenant=753ee82c-0a5b-4795-859a-b56d97be56e5 scanning 4478 KOLs (concurrency=5, llm_gap_ms=2100, dryRun=false)
[runAigcAction] action=kol_country_enrichment status=429 body={"error":{"type":"rate_limit_error","code":"rate_limit_exceeded","message":"RPM limit exceeded on key (limit=30). Please retry after 60 seconds.","retryAfterSeconds":60}}
[enrichKol] LLM country inference failed: aigcgateway responded 429
```

This is enough to reject the staging acceptance:

- numeric overflow is no longer the active failure mode
- but the staging chain still does not meet the reviewer acceptance bar for a successful real run
- I stopped the run after repeated `413` and `429` failures to avoid burning more quota on a failing path

## Passing evidence

### L1 local checks

- `npm run lint` → `0 errors / 3 warnings`
- `npx tsc --noEmit` → PASS
- `npm test` → `188 files / 1368 tests PASS`
- `npx prisma migrate status` → `Database schema is up to date!`
- local `prisma/schema.prisma` confirms `engagementRate ... @db.Decimal(7, 2)`

### Prod-side hotfix evidence

- prod code at `/opt/kolmatrix` is on `3449ead`
- prod `prisma/schema.prisma` also confirms `engagementRate ... @db.Decimal(7, 2)`
- prod `.env.production` points `APIFY_KOL_BASE_URL=http://localhost:3004`
- prod health is healthy and now reports:
  - `total_active_kols=1871`
  - `country_fill_rate=0.006413682522715126`
  - `language_fill_rate=0.1742383752004276`

Superuser SQL on prod:

```text
above_old_max=15
outlier_flagged=157
import_failed=0
new_after_cutoff=474
max_rate=9137.06
```

Sample `engagement_rate > 999.99` rows from prod:

```text
tiktok  beyond2known   9137.06  true
tiktok  zyph209        8730.80  true
tiktok  allengngy      5870.02  true
tiktok  joshfvfv       4229.14  true
youtube UCeCd_dX9...   3371.70  true
```

This confirms the core BL-076 fix is effective on prod:

- values above the old `Decimal(5,2)` ceiling now persist
- outlier flagging works
- the import-failure audit count remains `0`

## Notes

- `/var/log/kolmatrix-kol-sync.log` still only exposed pre-fix structured overflow entries up to `2026-05-26T15:41:52Z` during my review, so I used prod DB-level evidence instead of relying on the missing post-deploy log tail.

## Conclusion

Result: `FAIL`

Reason:

- prod hotfix evidence passes
- staging reviewer acceptance run does not pass

Next step for Generator:

- stabilize the staging `kol-sync:daily` chain so the real run can complete without `413` embed failures or `429` enrichment throttling
- then return the batch to `reverifying` for a fresh F005 review
