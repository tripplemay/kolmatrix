# BL-065 Match 页内部 UI 实质重写 — Signoff Report v1

> **日期：** 2026-05-13 北京
> **批次：** BL-065-match-page-internal-rewrite（Phase 2 第一批 / ADR-013 AI Native 转向）
> **范围：** Match 页内部 UI 实质重写 + 合并 /discovery + /database 为统一工作台 + AI 推荐 sidebar (campaign-context mode)
> **预估 → 实际：** ~5-7 day Generator + 1 day Reviewer → 3 day Generator（含 F006 三轮 CI 修复 / F001 双轮 CI / 0 fix-round）
>
> **状态：** F007 building → verifying — prod deployed @ `c5b5c31`，audit PASS=7 FAIL=0 WARN=1，handing to Reviewer for reverify。24h pm2 monitor 由 Reviewer 自行评估（用户 ack 5/13）。

---

## §1 部署信息

| 项 | 值 |
|---|---|
| Staging git_sha | `abdec9f`（5/13 14:35 BJT；3 轮 F006 CI 修复后绿）|
| Staging health URL | `https://staging.kol.guangai.ai/api/health?token=…` verify git_sha=abdec9f ✓ |
| Prod git_sha | `c5b5c31`（5/13 14:45 BJT 用户 ack 立即触发；包含 F007 audit script + signoff draft commit）|
| Prod deploy run | GitHub Actions run [25783092395](https://github.com/tripplemay/kolmatrix/actions/runs/25783092395) — 2026-05-13 06:45 UTC（北京 14:45），4 min completed |
| Prod health verify | `https://kol.guangai.ai/api/health?token=…` → git_sha=c5b5c31, status=healthy, db+redis ok ✓ |
| Backup | 自动 pg_dump on deploy via deploy-prod.sh（路径 `/opt/kolmatrix-backups/`） |
| 视觉 baseline regen | GitHub Actions run [25783090333](https://github.com/tripplemay/kolmatrix/actions/runs/25783090333) — 仅 `dashboard.png` 微调（168 → 164 KB）；deleted /discovery + /database baselines 早在 F006 已删；no en-match.png 自动生成（match-fidelity.spec.ts 是功能型 E2E，无 toHaveScreenshot 调用）|
| 24h pm2 monitor | 由 Reviewer 自行评估（用户 ack 5/13 决策） |

## §2 Features 落地（6/7 done，F007 in-progress）

| ID | 标题 | 状态 | Commit |
|---|---|---|---|
| F001 | Match 页 layout + 双视图 + ?campaignId AI sidebar shell | ✅ done | `c3b6b48`→`3c2d70c` |
| F002 | Filter + Search + SaveSearchControls + ActiveFilters 合并 | ✅ done | `aeb69c7` |
| F003 | bulk actions（MatchBulkActionBar + ConfirmDeleteDialog + bulkSoftDeleteKolsAction）+ ImportCsvDialog → /admin/kol-csv-import + admin entry link | ✅ done | `5426b84` |
| F004 | AddKolDialog mount in /match header | ✅ done | `c6ec0f3` |
| F005 | AI 推荐 sidebar 升级为 AiSuggestionsClient 包装（campaign-context）+ tenant-scoped lookup | ✅ done | `d523de6` |
| F006 | 删除 /discovery + /database 整页 + match.* i18n 完整化 + e2e 迁移 | ✅ done | `ad76eb1`→`46a4840`→`abdec9f` |
| F007 | staging dogfood + prod redeploy + 视觉 baseline regen + 24h 监控 + signoff | ⏳ Generator 完成 prod deploy + audit；Reviewer 复验 pending | `c5b5c31` |

## §3 L1 全绿

| 检查 | 结果 |
|---|---|
| `npm run lint` | ✅ 0 errors（2 pre-existing warnings unchanged） |
| `npx tsc --noEmit` | ✅ 0 errors |
| `npm test` 全量 | ✅ 通过（最近 CI run [25782189342](https://github.com/tripplemay/kolmatrix/actions/runs/25782189342) 全 8 jobs PASS）|

## §4 关键决策（6 + #G 全 lock）

| ID | 决策点 | Lock |
|---|---|---|
| #A | 双视图保留（卡片默认 + 表格 alternate） | ✅ F001 |
| #B | AI sidebar 起步（BL-066 升级为主面板） | ✅ F005 |
| #C | /admin/kol-csv-import 单子路由（不建 /admin 索引页，留 BL-070） | ✅ F003 |
| #D | BulkActionBar 全操作集（加 campaign / soft-delete + 确认 modal / export CSV）| ✅ F003 |
| #E | SaveSearchControls 保留 | ✅ F002 |
| #F | 默认卡片视图 | ✅ F001 |
| #G | MatchSearchBar 去 BL-044 AI chips（free-text 语义搜索归 BL-068） | ✅ F002（新决策） |

## §5 不可逆操作（F006 删除清单）

| 类别 | 数量 | 详情 |
|---|---|---|
| 路由文件夹 | 2 | `/[locale]/(app)/discovery/` + `/[locale]/(app)/database/` 整体 |
| 整文件夹文件数 | 29 | 11 (/discovery) + 16 (/database) + 2 baselines |
| 整合测试 | 5 | kol-discovery / kol-database / discovery-ai-search / database-intelligence / b5-discovery-filter-combinations |
| E2E specs | 2 | discovery-fidelity.spec.ts + database-fidelity.spec.ts（新 match-fidelity.spec.ts 6 case 替代）|
| 视觉 baselines | 2 | en-discovery.png + en-database.png（F007 update-visual-baselines workflow 后 + en-match.png）|

**净 -4658 lines**。git mv 保留 7 文件 blame：SaveSearchControls / AdvancedToggleCookie / AddKolDialog / AddToCampaignDialog / QuickStats / stats.ts / addKolAction.test.ts。

## §6 Staging dogfood spot check（TODO 团队执行）

预定 spot check 清单（来自 spec §3 F007 + Planner 后期建议）：

- [ ] /en/match 卡片视图：列出 KOL 卡片，valueScore desc 默认排序
- [ ] /en/match?view=table 表格视图：切换可工作，checkbox + 多行选择
- [ ] 选中 ≥1 行 → 浮动 MatchBulkActionBar 出现，3 actions 可点
- [ ] BulkActionBar → "Delete" → ConfirmDeleteDialog 二次确认 → soft-delete 落地（行从工作台消失）
- [ ] BulkActionBar → "Export CSV" → 浏览器下载 `kol-match-export-{timestamp}.csv`
- [ ] BulkActionBar → "Add to Campaign" → AddToCampaignDialog → 提交后 router refresh
- [ ] Filter sidebar：状态 pills 切换、basic 4 + advanced 11 dimensions 工作
- [ ] Search Bar：keyword search + platform 切换工作
- [ ] SaveSearchControls：保存 + 加载 named search 工作
- [ ] AddKolDialog：手动添加单 KOL 工作（form 提交 + revalidate）
- [ ] 5 locales（en/zh/ja/ko/es）：i18n 全部正确显示 match.* keys
- [ ] /en/match?campaignId={valid-uuid} → AI sidebar 出现 + 加载 AiSuggestionsClient
- [ ] /en/match?campaignId={invalid-uuid} → 2-column 工作台 fallback（sidebar 不显）
- [ ] /en/admin/kol-csv-import：admin role → 渲染；marketer role → 302 到 /match
- [ ] Match 页头部 admin entry link：admin role 才显（紫色 chip 含 admin_panel_settings icon）
- [ ] 旧 /en/discovery + /en/database 跳转：登录后访问 → 重定向到 /en/match

## §7 视觉 baseline regen（已执行）

GitHub Actions run [25783090333](https://github.com/tripplemay/kolmatrix/actions/runs/25783090333) 5/13 14:45 BJT 触发，唯一变更 commit pushed to main：
- `dashboard.png` 微调（168818 → 164303 bytes）— 该 baseline 自上次 regen 后小幅漂移
- en-discovery.png + en-database.png 早在 F006 已删
- en-match.png 未生成（match-fidelity.spec.ts 是功能型 E2E，没有 `toHaveScreenshot()` 调用；BL-066 升级主面板时可加视觉 baseline）
- en-kols-detail.png 保留（page 本身未变，click-path 在 visual-regression.spec.ts 已 retarget 到 /match + match-kol-card）

## §7.1 Prod audit 结果（已执行）

```
PASS=7  FAIL=0  WARN=1
AUDIT PASSED
```

7 sections（详细 colored output see `scripts/bl065-f007-prod-audit.sh`）：
1. /api/health git_sha=c5b5c31 = HEAD ✓
2. /en/match → HTTP 307 (auth gate fires, route alive) ✓
3. /en/discovery + /en/database → HTTP 307 (BL-064 redirect chain alive) ✓
4. /en/admin/kol-csv-import → HTTP 307 (role gate fires) ✓
5. Source residual /discovery + /database references = 0 ✓
6. pm2 logs last 500 lines: no BL-065-related errors ✓
7. Kol soft-delete pattern — 实测 total=3941 live=1357 soft-deleted=2584（pattern intact；scripts 中表名 case 修正后 WARN→PASS，本仓库 c5b5c31 之后 bump 已落地）

## §8 Reviewer reverify（TODO 复验全部 acceptance）

待 Reviewer 拉 abdec9f 跑：
1. L1 PASS 复验（lint / typecheck / vitest / playwright）
2. F001-F007 acceptance 逐条对照
3. Prod read-only audit via `scripts/bl065-f007-prod-audit.sh`
4. 团队 dogfood spot check 结果汇总

## §9 最终结论（TODO）

_本节由 Reviewer 在 reverifying → done 跃迁时填入。包含：_
- _Prod git_sha + deploy run link_
- _24h pm2 monitor 通过_
- _audit script PASS=N FAIL=0 WARN=N_
- _spot check 全部通过_
- _Phase 2 第一批完成确认_

---

## 附：Phase 2 后续依赖（spec §8）

- **BL-066** Campaign 详情页 AI 推荐主面板（依赖本批次 ?campaignId=xxx mode + AI 推荐 sidebar）
- **BL-067** C3 双向 explainability
- **BL-068** B3 自然语言 refine（接管 free-text 语义搜索）
- **BL-048** valueScore 优化（Phase 2 并行）
- **BL-070** 二次清理（旧 discovery.* / database.* i18n 键 + /api/database/export-csv 路由 + KEEP_AS_EN_PATHS 旧条目）
