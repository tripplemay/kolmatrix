# BL-063 isSaved Decommission · Prod Sign-off Report (v1) — 2026-05-11

> **Generator：** johnsong (CLI)
> **Prod git_sha：** `92b4957` (deploy-prod run 25643437421, 2026-05-11T00:02:26Z UTC)
> **Audit 时间：** 2026-05-11 01:26 UTC (immediate post-deploy) / 待 ~24h 后第 2 轮 (reverifying)
> **报告版本：** v1 (immediate post-deploy)
> **Final 签收：** Codex Reviewer 在 reverifying 阶段写 v2 (含 24h pm2 logs §7 evidence)
> **关联：** docs/specs/BL-063-isSaved-decommission-spec.md / docs/test-reports/BL-063-F005-staging-dryrun-2026-05-11.md / docs/specs/BL-063-F005-staging-dryrun-audit.md

## 0. TL;DR

BL-063 isSaved decommission 已成功部署到生产环境。F006 acceptance 8 项 immediate 检查（§1-§6, §8）全 PASS，§7 (24h pm2 logs 监控) 留给 Codex Reviewer 在 reverifying 阶段（部署后 24h 起）跑 second-pass audit 验证。

**关键证据：**
- prod git_sha = `92b4957` (含 BL-063 全部 6 features 的代码 + scripts + audit tools)
- migration `20260511000000_bl063_remove_is_saved` finished + not rolled back
- `is_saved` 列 + `kol_tenant_saved_idx` 索引均已从 prod schema 移除
- engagement_rate 分子 = **107** ≥ 95 baseline (无 BL-063 stats regression；prod 数据比 staging 95 行更稳)
- pre-deploy backup: `db-20260511-000236.sql.gz` 22M (deploy-prod.sh step 2/8)
- prod /campaigns/[id] UI walkthrough: 用户实地 5/5 PASS

## 1. F006 Acceptance 逐条核对

| # | acceptance | 状态 | 证据 |
|---|---|---|---|
| 1 | prod 数据 backup（pg_dump）| ✅ PASS | §2.1 (db-20260511-000236.sql.gz, 22M, deploy step 2/8) |
| 2 | prod redeploy（GH Actions UI dispatch HEAD = main，由用户手动触发）| ✅ PASS | §2.2 (run 25643437421 success, sha=92b4957) |
| 3 | prod /api/health 返回 git_sha = main HEAD | ✅ PASS | §2.3 (health.git_sha=92b4957) |
| 4 | deploy script 自动跑 prisma migrate deploy | ✅ PASS | §2.4 (_prisma_migrations 含 20260511000000_bl063_remove_is_saved finished_at=t) |
| 5 | prod SQL audit: is_saved 列不存在 / 行数无非预期变化 / engagement_rate non_null_pct 不退化 | ✅ PASS | §2.5 (col 0 / 3827 total / 1243 active / non-null=107 ≥ 95) |
| 6 | prod /campaigns/[id] 实地：按钮 enabled + dialog 含全量池 | ✅ PASS | §2.6 (用户实地 5/5 PASS @ 2026-05-11) |
| 7 | 24h 监控：sentry / pm2 logs 无 isSaved 相关错误 | ⏳ PENDING | §2.7 (immediate run = clean；24h second-pass 由 Reviewer 跑) |
| 8 | docs/test-reports/BL-063-signoff-2026-05-XX.md | ✅ PASS | 本报告 v1（v2 由 Reviewer 24h 后写）|
| 9 | Reviewer 复验全部 acceptance + signoff + status verifying → done | ⏳ PENDING | reverifying 阶段 Codex Reviewer 接手 |

## 2. 详细证据

### 2.1 Pre-deploy pg_dump backup

```
$ ls -lh /opt/kolmatrix-backups/ | tail -3
-rw-rw-r-- 1 tripplezhou tripplezhou  22M May 10 12:53 db-20260510-125347.sql.gz
-rw-rw-r-- 1 tripplezhou tripplezhou  22M May 11 00:02 db-20260511-000236.sql.gz   ← 本次 deploy step 2/8
-rw-rw-r-- 1 tripplezhou tripplezhou 4.9K May 11 00:02 manifest.log
```

backup 时间 `00:02:36 UTC` = deploy run 25643437421 createdAt `00:02:26 UTC` + 10s — 完全吻合 deploy-prod.sh step 2/8 `backup-db.sh` 的执行窗口。durable backup 路径 `/opt/kolmatrix-backups/`（轮换 30 天，cron `0 4 * * * find ... -mtime +30 -delete`）。

**回滚预案：** 如 24h 内发现 BL-063 引入未预期 regression，可走：
1. `gunzip db-20260511-000236.sql.gz` → 恢复 pre-migration schema (含 is_saved 列 + 历史数据)
2. 或按 migration SQL §ROLLBACK 注释手动 `ALTER TABLE kol ADD COLUMN is_saved BOOLEAN ...` + `UPDATE` 从 backup 表恢复
3. `scripts/rollback.sh` 可回滚 git checkout + npm rebuild + pm2 reload (但 schema 需手动恢复)

### 2.2 Deploy-prod workflow run

```
gh run view 25643437421
- conclusion: success
- headSha:    92b49579e920e8098665b178588c7500b0ee4fa0
- event:      workflow_dispatch
- createdAt:  2026-05-11T00:02:26Z
```

8 steps 全 PASS：`record HEAD → pg_dump backup → git fetch + checkout → npm ci → prisma generate → prisma migrate deploy → npm run build → pm2 reload → healthcheck.sh × 5`。

### 2.3 /api/health git_sha 一致性

```bash
$ ssh prod 'cd /opt/kolmatrix && git rev-parse --short HEAD'
92b4957

$ curl "https://kol.guangai.ai/api/health?token=<HEALTH_DETAIL_TOKEN>"
{
  "status": "healthy",
  "uptime_seconds": 4756,    # ≈79 min — deploy 后正常 reload 范围
  "checks": {
    "database": { "status": "ok", "latency_ms": 20 },
    "redis":    { "status": "ok", "latency_ms": 5 }
  },
  "version": "0.1.0",
  "git_sha": "92b4957"        # ← matches deploy HEAD
}
```

### 2.4 Migration applied

```sql
SELECT migration_name, finished_at IS NOT NULL AS finished, rolled_back_at
FROM _prisma_migrations
WHERE migration_name LIKE '%bl063%' OR migration_name LIKE '%is_saved%'
ORDER BY started_at DESC LIMIT 3;
```

```
            migration_name            | finished | rolled_back_at
--------------------------------------+----------+----------------
 20260511000000_bl063_remove_is_saved | t        |
(1 row)
```

由 `deploy-prod.sh` step 6/9 `npx prisma migrate deploy` 自动应用，无人工干预。

### 2.5 Prod SQL audit (4 项)

#### 2.5.1 is_saved 列已删

```sql
SELECT COUNT(*) FROM information_schema.columns
WHERE table_name='kol' AND column_name='is_saved';
-- 0
```

#### 2.5.2 kol_tenant_saved_idx 索引已删

```sql
SELECT COUNT(*) FROM pg_indexes
WHERE tablename='kol' AND indexname='kol_tenant_saved_idx';
-- 0
```

#### 2.5.3 engagement_rate 分子 ≥ 95（acceptance §3 修订后）

```sql
SELECT COUNT(*) AS total_kol,
       COUNT(engagement_rate) AS non_null,
       ROUND(COUNT(engagement_rate)::numeric * 100 / NULLIF(COUNT(*), 0), 2) AS non_null_pct
FROM kol WHERE deleted_at IS NULL;
```

```
 total_kol | non_null | non_null_pct
-----------+----------+--------------
      1243 |      107 |         8.61
```

**对比 staging:** prod 107 行 > staging 95 行（baseline）；prod non_null_pct 8.61% > staging 2.44%（prod 数据质量好，daily sync 回填更全）。BL-063 不动 stats，符合预期。

#### 2.5.4 kol 行数 sanity

```sql
SELECT COUNT(*) AS total_kol_all,
       COUNT(*) FILTER (WHERE deleted_at IS NULL) AS active_kol
FROM kol;
```

```
 total_kol_all | active_kol
---------------+------------
          3827 |       1243
```

migration 不丢行；total - active = 2584 行 soft-deleted（与 BL-051a soft-delete pattern 一致，BL-063 不影响此数）。

### 2.6 Prod /campaigns/[id] UI 5 项 walkthrough

用户 2026-05-11 实地（与 staging 5/5 PASS @ 2026-05-11 ~10:00 BJT 一致）：

| # | 检查 | 结果 |
|---|---|---|
| 1 | /campaigns/[id] 「添加 KOL」按钮 enabled + tooltip + dialog 显完整池 | ✅ |
| 2 | /discovery KOL 卡片无 Save 按钮（A 路径同 staging） | ✅ |
| 3 | /kols/[id] actions 区无 SavedToggleButton | ✅ |
| 4 | /database 全量池视图正常（不再只显 saved，QuickStats 显大数）| ✅ |
| 5 | 整体无 page crash / console error / 数字异常 | ✅ |

### 2.7 24h pm2 logs (acceptance §7) — PENDING

**Immediate run（部署后 ~80 min）：**

```bash
pm2 logs kolmatrix --lines 1000 --nostream --raw | grep -iE 'issaved|is_saved'
# (no matches)
```

**Reviewer reverifying 任务：** 部署后 ~24h（即 2026-05-12 ~00:00 UTC 之后），重跑 `bash /opt/kolmatrix/scripts/bl063-f006-prod-audit.sh`，验证 [9] pm2 logs 仍无 isSaved-related entries（覆盖完整 24h 流量窗口），写 signoff v2。

### 2.8 src/ 残留 grep（非 acceptance 但 sanity check）

```bash
grep -rn 'isSaved\|is_saved' src/ --include='*.ts' --include='*.tsx' \
  | grep -v node_modules | grep -v '__tests__' | grep -v '.next'
```

5 处全是 explanatory comments（`* BL-063 F00X: ... decommissioned ...`），无 functional 代码残留。与 staging 同状态，与 F003 acceptance 「src/ 路径全清」一致。

### 2.9 _bl063_is_saved_backup TEMP（acceptance §6 of F005，本批次 cross-ref）

```sql
SELECT to_regclass('public._bl063_is_saved_backup');
-- (null)
```

预期 — F002 用 `CREATE TEMP TABLE`，session-scoped，post-deploy 即销毁。durable backup 责任由 F006 §1 pg_dump 承担（见 §2.1）。

## 3. 状态机切换

- 本会话提交后：features.json F006 status pending → done / completed_features 5 → 6
- progress.json status fixing → reverifying（fix_rounds 仍 1）
- evaluator_feedback 清空（旧的 5/11 spot check 留作历史，新内容由 Reviewer 写）
- generator_handoff 写本 signoff v1 路径 + Reviewer 24h 任务

## 4. Reviewer reverifying 任务清单

1. 部署后 ~24h 起（2026-05-12 ~00:00 UTC 之后）SSH prod 跑 `bash /opt/kolmatrix/scripts/bl063-f006-prod-audit.sh`
2. 验证 [9] pm2 logs 仍 clean（覆盖完整 24h 流量窗口）
3. 全部 9 项仍 PASS（生产数据无显著漂移）
4. 写 signoff v2: docs/test-reports/BL-063-signoff-2026-05-XX-v2.md（或追加到本文件 §5）
5. progress.json status reverifying → done / completed_features 6 / framework_reviewed 处理
6. .auto-memory/project-status.md BL-063 行从 fixing → DONE，加入 ✅ 历史 batch 列表

## 5. 关联文档

- `docs/specs/BL-063-isSaved-decommission-spec.md` — 主 spec
- `docs/specs/BL-063-F005-staging-dryrun-audit.md` — F005 partial-pending audit + Planner ruling
- `docs/test-reports/BL-063-F005-staging-dryrun-2026-05-11.md` — staging dry-run report
- `prisma/migrations/20260511000000_bl063_remove_is_saved/migration.sql` — F002 schema 实装
- `scripts/bl063-f006-prod-audit.sh` + `scripts/sql/BL-063-F006-prod-audit.sql` — F006 audit tooling
- `features.json` F005/F006 acceptance — 已修订（Planner ruling 0ea747d）
- `backlog.json` BL-062 — engagement_rate 5/17 weekly growth-curve check 跟进项

---

**报告版本：** v1 immediate post-deploy（2026-05-11 01:29 UTC）— 待 Codex Reviewer 在 reverifying 阶段写 v2 final signoff
