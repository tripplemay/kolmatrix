# BL-086 KOL 抓取加速批次 Signoff 2026-06-07

> 状态：**deployment-ready PASS**（progress.json status=verifying → done）
> 触发：F006 Codex 部署就绪验收（充值前半段），5 generator features done + F006 codex executed
> Evaluator：Reviewer (Codex)

---

## 变更背景

提升新 KOL 入库速率（积累模式）。根因：TikHub 余额耗尽（6/04 起静默空转）+ refresh:discovery 配比失衡（TIER_INTERVAL_MS hot 1d 使 refresh 占 ~90% 抓取量产 0 新增）。
解决方案：refresh 积档 hot 14d/warm 30d/cold 30d（−87% 负载）+ discovery 拉满（扩种子 + 收割 2535 旧源 id）+ 余额可观测。
路径 B：上游 guang-tech/apify PR → merge → fork-sync 部署。

---

## 变更功能清单

### F001 — refresh 配比积累档
**Executor：** generator | **Status：** PASS
**变更：** `TIER_INTERVAL_MS`: hot 24h→14d / warm 3d→30d / cold 7d→30d
**证据：** Generator PR #3 merged (claimed @ 811415a), /opt sync @ 8f9320a, 补丁交付物 @ b5005c9

### F002 — discovery 种子扩充 + 砍空转
**Executor：** generator | **Status：** PASS
**变更：** +18 手游种子, −8 空转 IG/YT, TT 高产 limit 100→300, schedules 30→48
**证据：** `scripts/bl086-f002-discovery-seeds.ts` + snapshot 30 entries in git, Generator applied on prod fork @ e80ca16

### F003 — manual_seed 收割 2535 旧源
**Executor：** generator | **Status：** PASS (dry-run)
**变更：** 收割脚本 `scripts/bl086-manual-seed-harvest.ts`, dry-run 2535 UC ids verified
**证据：** Script in git @ 195239a, 11 单测 PASS. 真实投喂 deferred → BL-092

### F004 — 余额 / inserted=0 静默空转告警 + 成本记账
**Executor：** generator | **Status：** PASS
**变更：** `src/lib/kol-sync/log.ts` classifyDailyRun 扩展: Insufficient balance → ALERT + inserted=0 with effort → WARN. 上游 /admin/stats 暴露 tikhubBalanceUsd.
**证据：** Code verified @ 242f693, tsc PASS, 27 单测 PASS. Generator confirms tikhubBalanceUsd:0.0005 on prod @ 811415a

### F005 — Instagram hashtag 发现 0 产出排查修复
**Executor：** generator | **Status：** PASS (root cause + fix)
**变更：** 根因=68 jobs per-author 突发触发 IG 账户级 429 限流, 修复=TikHubClient minIntervalMs 350ms (PR #4)
**证据：** Generator PR #4 merged (claimed). 真实 IG 产出验证 deferred → BL-092

### F006 — Codex L1+L2 + signoff
**Executor：** codex | **Status：** PASS (deployment-ready scope)

**L1 验证：**
| 检查项 | 结果 |
|---|---|
| `npx prisma generate` | PASS |
| `npm run lint` | 0 errors, 3 pre-existing unused-import warnings (Soft-watch S1-S3) |
| `npx tsc --noEmit` | PASS (0 errors) |
| `npm run test` | 205 files, **1526 tests PASS** |
| kolmatrix 产品代码改动 | F004 `src/lib/kol-sync/log.ts` + F002/F003 scripts — 全部 type-safe + tested |

**L2 部署就绪：**
| 检查项 | 结果 | 证据 |
|---|---|---|
| kolmatrix prod health | healthy, 2383 KOLs | `curl https://kol.guangai.ai/api/health` |
| kolmatrix staging health | healthy, 5031 KOLs | `curl https://staging.kol.guangai.ai/api/health` |
| F001 tier.ts deployed | Generator claim | State commit 811415a (3 PR merged, /opt 8f9320a) |
| F002 schedules updated | Generator claim | Commit e80ca16 (30→48 applied on prod fork) |
| F003 dry-run 2535 | Generator claim | Commit 195239a (script + 11 tests PASS) |
| F004 /admin/stats balance | Generator claim | State commit 811415a (tikhubBalanceUsd:0.0005) |
| F005 IG throttle | Generator claim | PR #4 merged upstream |

> **Access constraint notes:** guang-tech/apify is a private repo — PR merge status could not be independently verified via API. VPS apify-kol-service internal state could not be independently verified (no SSH from this Codex environment). Generator claims are backed by detailed state commits with specific deployment timestamps and balance values. kolmatrix-side code changes independently verified via L1 checks.

---

## 未变更范围

| 事项 | 说明 |
|---|---|
| kolmatrix 主应用逻辑 | 除 F004 `src/lib/kol-sync/log.ts`（+28 行告警扩展）外无改动 |
| Prisma schema | 无变更 |
| UI 组件 | 无变更 |
| CI/CD pipeline | 无变更 |
| TikHub 充值 | 用户侧动作，不在本批次范围 |

---

## 预期影响

| 项目 | 改动前 | 改动后 |
|---|---|---|
| refresh 日负载（理论） | ~944 KOL | ~123 KOL (−87%) |
| TIER_INTERVAL_MS hot | 1d | 14d |
| TIER_INTERVAL_MS warm | 3d | 30d |
| TIER_INTERVAL_MS cold | 7d | 30d |
| discovery 种子数 | 30 | 48 (+18 SEA/手游) |
| 静默空转检测 | 人工发现（6/04 + 5/14） | 自动 ALERT（insufficient_balance + inserted=0） |

---

## 类型检查 / CI

```
npm run lint → 0 errors, 3 warnings (S1-S3, all pre-existing unused-import)
npx tsc --noEmit → PASS (0 errors)
npm run test → 205 files, 1526 tests PASS
```

---

## L2 实测记录

| 项 | 证据 |
|---|---|
| Prod health | `curl https://kol.guangai.ai/api/health` → healthy, uptime 1233s, 2383 KOLs |
| Staging health | `curl https://staging.kol.guangai.ai/api/health` → healthy, uptime 973s, 5031 KOLs |
| Upstream PR merge | Generator claim: PR #3(F001), #4(F005), #5(F004) merged to guang-tech/apify master @ 811415a |
| /opt deployment SHA | Generator claim: synced to 8f9320a @ 811415a |
| /admin/stats balance | Generator claim: tikhubBalanceUsd=0.0005 confirms F004 balance exposure |

> **RSC server action 限制：** N/A — 本批次无 UI/RSC 改动，纯 infra + config + upstream 部署。

---

## Ops 副作用记录

本批次无 Reviewer 执行的数据库 ops。

---

## Deferred to BL-092（充值后）

| 项 | 当前状态 | BL-092 待办 |
|---|---|---|
| F003 真实投喂 2535 UC ids | dry-run PASS | 充值后 POST /admin/seeds, 验证 96% 历史命中率复现 |
| F005 IG 发现真实产出 | root cause + fix deployed | 充值后验证 IG hashtag inserted > 0 |
| 真实入库速率 | 部署就绪 | before/after 量化: refresh 负载实测 / 新增速率 / 告警触发性 |
| F004/F005 按真实数据调优 | 初始值设定 | 充值后按 real traffic 微调 classifyDailyRun / minIntervalMs |

---

## Soft-watch（不阻塞 done，需后续跟进）

| ID | 描述 | 风险等级 | 建议处置 |
|---|---|---|---|
| S1 | `src/app/api/health/__tests__/route.test.ts:18` — `afterEach` unused import | low | 下批次顺手清理 |
| S2 | `src/lib/queue/explain-recommendations-worker.ts:81` — `_ctx` unused | low | 下批次顺手清理 |
| S3 | `tests/integration/db-platform-admin-nullif.test.ts:13` — `beforeEach` unused import | low | 下批次顺手清理 |
| S4 | VPS apify-kol-service 部署状态依赖 Generator 自报（无独立 SSH 验证） | low | Codex 环境配置 SSH key 后可在 BL-092 补验 |
| S5 | guang-tech/apify PR merge 状态依赖 Generator 自报（私有仓库，无 API token） | low | BL-092 验收时一并复查 |
| S6 | Node v25.7 vs .nvmrc v20 版本不一致 | low | 非 jsdom 批次无影响；下批次前建议 `nvm use` |

---

## Framework Learnings

### 新规律
- **跨仓库 + VPS-only 部署批次验收模式**：当批次改动主要在外部仓库（guang-tech/apify）且部署在无 SSH 可达的 VPS 时，Evaluator L1 可验 kolmatrix 本地代码 + L2 依赖 Generator 的状态 commit 证据链。此模式与纯 kolmatrix 代码批次验收路径不同，需文档化验收边界。
  - 来源：BL-086 F006 verifying
  - 建议写入：`framework/harness/evaluator.md` §10 L1 前置 或 新 §验收边界

### 新坑
- **TikHub 充值前投喂会被 worker 消费成 succeeded-0**：Generator 在 F003 dry-run 中发现此行为并沉淀为 proposed-learning。Evaluator 确认此约束在 F006 acceptance 已解耦（deferred → BL-092），无需在本轮重复处理。
  - 来源：BL-086-F003 (commit 600b5d6 proposed-learning)

---

## Harness 说明

本批改动经 Harness 状态机完整流程交付：
- `planning → building → verifying → done`（首轮 PASS, fix_rounds=0）
- `progress.json` 已设为 `status: "done"`，signoff 路径已填入 `docs.signoff`
- 6/6 features PASS（deployment-ready scope），real-traffic verification deferred → BL-092
