# BL-060 soft-delete-ui-filter-hotfix Verifying 2026-05-09

> 状态：**Reviewer verification PARTIAL**
> 触发：BL-060 `verifying` 完成 L1 + staging 复验，但 staging database-fidelity 仍有超时抖动
> Reviewer：Codex

## 总体结论

- BL-060 的代码层修复本身通过了 L1 和关键 grep 复核。
- staging health 与登录 cinematic 流程通过。
- 但 `/database` fidelity 这组 staging 复验未稳定通过，`login()` 后 `waitForURL(/dashboard/)` 在多个用例里超时。
- 因此本轮结论是 `PARTIAL`，不能签收，`progress.json.docs.signoff` 仍应保持空值。

## 验收结果

- L1 通过：
  - `npm run lint` 0 errors / 2 warnings
  - `npx tsc --noEmit` 通过
  - `npm run test` 156 files / 1101 tests PASS
- 关键 grep 通过：
  - `src/app/[locale]/(app)/dashboard/data.ts`
  - `src/app/[locale]/(app)/database/stats.ts`
  - `src/lib/dashboard/kpi-snapshot.ts`
  - `src/lib/campaigns/list-kpis.ts`
  - `src/lib/campaigns/detail.ts`
  - `src/lib/crm/overview.ts`
  - `scripts/sql/BL-060-F003-cleanup-kpi-snapshot-leak.sql`
  - `scripts/sql/BL-060-F005-cleanup-is-saved-leak.sql`
- staging 通过：
  - `https://staging.kol.guangai.ai/api/health` -> `healthy`
  - `tests/e2e/login-cinematic.spec.ts` -> PASS
  - `tests/e2e/marketer-dashboard.spec.ts` -> PASS

## 关键阻断

- `tests/e2e/database-fidelity.spec.ts` 在 staging 上不稳定：
  - `Quick Stats KPI strip renders all four cards` 的单跑 PASS
  - 但 `filter bar surfaces 7 dimensions` 与 `Tier and Game filters are live enabled controls` 在 `beforeEach` 的 `login()` 内 `page.waitForURL(/\/dashboard/)` 超时
  - 将 worker 降为 `1`、timeout 提到 `60000ms` 仍复现
- 这更像是 staging 登录/导航等待的 suite-level 抖动，而不是本批次 query filter 逻辑的 L1 回归。

## 说明

- 这轮没有执行数据库 ops。
- `BL-060` 仍留在 `verifying`，等 staging database-fidelity 稳定后再决定是否签收。

## 最终结论

- Final grade: `B`
- Readiness: `Not ready`
- `progress.json.docs.signoff` 未写入。
